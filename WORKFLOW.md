# FACET — Workflow and Session Plan

How Jerom and Claude work together on FACET, and the full run of sessions from
here to a pilot.

Written to be pasted into a fresh chat as a starting brief.

---

## 1. Who does what

**Jerom** is the founder and owner of Technopanel's FACET project. He is a
semi-beginner developer working with AI assistance — comfortable running
commands and reading code, not a professional engineer. Explain the *why* in one
line, not three paragraphs. Never assume a term is known; never over-explain
either.

**Claude Code (VS Code)** writes the code. One task per session.

**Claude (this chat)** is the planning and review layer. It holds context,
reviews plans before they are approved, decides what a slice contains, and keeps
`SPEC.md` and `DESIGN.md` honest. **It has no access to the repository** — it
sees only what is pasted or uploaded, which is what `§9`'s status block is for.
`§8` is the exception and names when it applies.

---

## 2. The authority files

At the repo root:

| File | What it decides |
|---|---|
| `CLAUDE.md` | the always-loaded INDEX — judgment rules and where everything lives |
| `SPEC.md` | what the system does — rules numbered `S1`… |
| `DESIGN.md` | how it looks and behaves — rules numbered `D1`… |

**Since session 50 the rules are layered** (the full map is
`docs/archive/28-fixation/`): path-scoped rules in `.claude/rules/*.md` load
when their files are touched; procedures are skills (`facet-ui`,
`facet-verify`, `facet-audit`, `facet-register`); the mechanical workers are
`.claude/agents/`; and the prohibitions that can be enforced ARE —
`.claude/hooks/` + permissions, deterministic, listed in
`docs/archive/28-fixation/00-prohibitions.md`. Authority is unchanged: SPEC
and DESIGN decide, everything else points at them.

`docs/design/facet-concept-v5-premium.html` is the visual target.
`docs/archive/` and `docs/design/archive/` are history, never authority.

**Everything is cited by rule number.** "Per S74" and "per D28", never "as we
discussed".

---

## 3. The loop, every session

1. **New Claude Code session.** `/clear` if reusing a window. Opus.
2. **State the task by rule number.** One task. Nothing else in the session.
3. **Plan mode** (`shift+tab`) for anything structural.
4. **Paste the plan into this chat before approving it.** This is the step that
   was missing before, and it is the cheapest defect-catching in the project.
5. **Approve in Claude Code.** It builds.
6. **Run the checks:** `typecheck` · `lint` · `build` · `check:messages` ·
   `verify:routes`. All five, every time. `build` is not optional.
   CI (`.github/workflows/checks.yml`) re-runs the first four on push and on
   pull request, so a session that forgets them is caught at main rather than
   two sessions later. The ten verify scripts stay the session's own to run.
7. **Update `SPEC.md` / `DESIGN.md`.** When a `[CHANGE]` or `[BUILD]` rule
   ships, delete its marker — the rule is now simply true. **The spec is a
   living file, not a record of intent.** If it drifts from the code, the whole
   arrangement fails the way it did last time.
8. **Commit and push.** Every session, no exceptions.
9. **Append anything discovered and deferred to §5.** A defect that exists
   only in a session report does not exist.
10. **Paste the status block (§9) into this chat.**

---

## 3b. Model, mode and effort

Jerom works in **VS Code with the Claude Code plugin**. His global settings are
already `opus[1m]` at `xhigh` effort, which is the right default — the old
project-level override that forced Sonnet has been deleted.

| Session type | Model | Mode | Notes |
|---|---|---|---|
| **Investigation / debugging** (session 1) | Opus | normal | Plan mode blocks writes, so it cannot fix anything. Let it work. |
| **Structural slice** (schema, new screens, deletions) | Opus | **plan mode** | `shift+tab` until the input says plan mode. Plan is reviewed in chat before approval. |
| **Small edit** (a skill file, a config line) | Opus | normal | Too small to plan. |
| **Audit** | Opus | **plan mode** | Plan mode is read-only, which is exactly what an audit should be. It physically cannot change anything. |

Leave effort at `xhigh`. It costs latency, not correctness, and every session in
this plan is judgement work rather than typing.

**Never lower the model to save allowance mid-slice.** A slice half-built by a
smaller model is worse than a slice not started.

---

## 4. The session plan

Some rows will take more than one session. That is expected — split, never
merge. A slice that grows past 800 words of plan is two slices.

### Phase 0 — Unblock

| # | Session | Done when |
|---|---|---|
| 1 | ~~**Fix the Docker build.** Fails collecting page data for `/follow-ups` in-container, passes on host.~~ **Done** — `d7a7e7a` + `c00bcc9` | `docker compose build` succeeds |
| 2 | ~~**Rewrite the `facet-ui` skill** to teach `DESIGN.md` — effect tokens and their named uses, per-object row anatomy, view-mode URL pattern, `D21` as a pre-flight checklist.~~ **Done** — `31f3bdb` + `7a8823d` | skill loads and cites `D` numbers |

### Phase 1 — The model

Schema and data layer first, because the UI renders whatever the model says.

| # | Session | Rules |
|---|---|---|
| 3 | ~~One name field on companies and contacts; phone mandatory; country added~~ **Done** — `6725201` + `a486009` | `S12` `S13` `S14` `S19` |
| 4 | ~~Drop project role labels — participants only~~ **Done** — `a759999` | `S25` |
| 5 | ~~Quotation project optional — a quotation may be raised without one.~~ **Done** — `061a28d`. **The write-back half moved to session 13**, where approval exists: `S74` fires when the coordinator approves, and that cannot be built before approval can | `S50` |
| 6 | ~~VAT fixed at 15%; readable product fields.~~ **Done** — `cd02a78`. **Validity is no longer a note** — it leaves FACET entirely in session 10 (`S67`) | `S53` `S57` |
| 7 | ~~Report splits into shared half and private note; same-day edit only; author keeps their own~~ **Done** — `50745cc` | `S38` `S39` `S40` |
| 8 | ~~Coordinator sees projects and contacts.~~ **Done** — `ff8e8dd`. **The sight half is built here**; the edit right `S76` now mentions (`S62`) arrives in session 13 | `S76` |
| 9 | ~~Drop dead structure — `accounts`, `verificationTokens`, both snapshot tables, `project_companies.role`~~ **Folded into session 22.** Both were dead-structure sweeps citing `SPEC §15`, and 22 runs last so it catches Phase 1b's orphans too. The verify-routes.ts re-cite goes with it | — |

> **AUDIT 1 — the model.** See §6.

### Phase 1b — Dispatch and payment

The dispatch and payment model changed after a session with the sales
coordinator. It comes before any interface work, because the interface renders
whatever the model says.

| # | Session | Rules |
|---|---|---|
| 10 | ~~Validity and delivery period leave FACET~~ **Done** — `5444a89` | `S67` |
| 11 | ~~Stock on the quotation~~ **Done** — `d67a9e8` + `37ca283` | `S118` |
| 12 | ~~Dispatch lines and the three modes~~ **Done** — `bbbf048` | `S116` `S75` `S126` |
| 13 | ~~Request, approve, refuse; the `canDispatch` split~~ **Done** — `71af2a3` | `S72` `S124` `S125` `S122` `S62` |
| 14 | ~~Payment method, shipment, SMAC number~~ **Done** — `eee6020` | `S70` `S71` `S73` `S119` `S121` |
| 15 | ~~The difference flag~~ **Done** — `dfb8d26` | `S120` `S77` |
| 16 | ~~Won derived from dispatch; committed~~ **Done** — `f14dc9e` | `S28` `S29` `S31` |
| 17 | ~~Told — refusals, cancellations, credit~~ **Done** — `e64e4a0`. `S73`'s cancellation came in with it: `S128` requires a cancellation to reach the rep and there was nothing to tell them about without one. `S31`, `S73` and `S128` lost their markers; `S129` keeps its, because the moment it names — `S80`'s confirmation at approval — does not exist, so the telling hangs on `setCreditSplit` instead | `S128` `S129` `S92` `S73` |
| 18 | ~~Compliance metric~~ **Done** — `da6509d` | `S123` `S127` |

Then AUDIT 1's remaining fixes, which are independent and small.

| # | Session | Rules |
|---|---|---|
| 19 | ~~Primary rep on handover and reassignment~~ **Done** — `71bae78` | `S18` |
| 20 | ~~Coordinator's comment access; the cancellation reason~~ **Done** — `e1b8f8c`, reason half `e64e4a0` | `S62` `S114` |
| 21 | ~~Handover recipients narrowed to the roles `S9` names~~ **Done** — `f400886` + `cfc6dbe`. `S9` was wrong as well as the code: marketing holds companies as a rep does, so the rule gained a fourth recipient and lost its marker. The test is a deliberate proxy — no flag means "holds a company book" — and AUDIT 1 F8 records why `sees_all_reps` was the one chosen | `S9` |
| 22 | ~~Dead-structure sweep — AUDIT 1 F9–F18, plus everything session 9 carried: `accounts`, `verificationTokens`, both snapshot tables, `project_companies.role`, and re-citing verify-routes.ts assertion labels to S numbers~~ **Done** — `8dc759b` | `SPEC §15` |

Session 22 runs **last** of these, so it sweeps up whatever Phase 1b orphans.

### Phase 2 — The rebuild

**Phase 2 is closed.** `27b` was the last row and it shipped in `fc4500c`;
everything here and in `2b` is done, with its commit on its own row.

**What shipped, in this order**, including the slices that were never rows —
`34b` and `35` ran out of order on purpose:

| | Slice | Commit |
|---|---|---|
| | the token slice | `10d43c8` |
| | the dashboard's block model | `c9b360b` |
| `23` | the shell | `79663fd` |
| | `seed:demo` | `28141cd`, `a8b8ab3` |
| | three dashboard slices `D32`–`D34` `D65` `D69` | `f4b71b4`, `043f8c5`, `df0762c` |
| | the companies list | `d74f0c6` |
| | the company detail `D70` | `f301bf6` |
| `34b` | the verify-residue slice | `0710b1b` |
| `35` | the projects board and table | `56b44a0` |
| | the board's six rules `S132`–`S135` | `f95fa8c`, `1301c85` |
| | one name field on projects `S26` | `341fd02` |
| | `S50`'s second half | `326921a` |
| | the `D20` rewrite | `10c6d19` |

**Five of those were never sessions in this plan** — the token slice,
`seed:demo`, the dashboard, the companies screens and the board's rules. They
are recorded here rather than given numbers after the fact, because a number
invented backwards is one nothing else cites.

**`40` ran first**, and that was the point: no more screens were built on the
rewritten `D20` until there was an assertion behind it.

| # | Session | Rules |
|---|---|---|
| 23 | ~~The shell — seven-item rail, tokens, layout cap, theme, permission boolean~~ **Done** — `79663fd`. It carried eight until `28b` deleted `/performance`, and carries `D49`'s seven now; `§5` records why moving the item early would have been the thing that broke | `D5`–`D23` `D49` `D50` |
| 40 | ~~**`D20`'s enforcement.** The scripts-off **operability** section in `verify:routes`~~ **Done** — `46a15b7`, on the rule rewrite `10c6d19`. `§23` scans inside `get()`, the choke point the message-key scan already uses, so all 909 fetches are covered at no extra cost: **5,179 forms, 0 required fields behind JavaScript, 1 note.** `checkbox.tsx` is native and `combobox.tsx` is deleted. **The section proves itself before it is believed** — it is fed the two shapes this slice removed and asserts it catches both, because a section that has only ever been green has not been shown to work | `D20` `S15` |
| 26 | ~~**The quotations list** — grouped, per-object lead cell, whose-move column~~ **Done** — `28abf40`, `4c8233d`. Three groups became **two plus one `D25` does not name**: `S133` left `withCustomer` as the only rep-owed thread position, so *your move* and *waiting on the customer* select the same rows and `D25` names one pile twice (`AD16`). The **turn line came off the row** — it rendered identically down a whole pile, and `D29` had already settled that shape for the board: the pile's name answers `D2` once, and the row says which of the coordinator's three moves is owed, which a header cannot. **`Total · SAR` became `Quoted m²`** `S6`, and the table went `table-fixed`: every cell carries `whitespace-nowrap`, so under auto layout a 56-character company name pushed two columns off a 1366 screen. The list also **could not reach `readyToShip` or `won` at all** — it passed no dispatch flag, so a shipped thread read *chase the customer* here and *won* on its own page; `verify:routes` §11 now pairs the two at every row. Carried `quotations.ts:261`'s search and closed `AD10` | `D25`–`D27` `D2` |
| 41 | ~~**The dispatches list and detail**~~ **Done** — `e44bac9`, `aef80f1`. The list was flat, date-led and behind two filter rows; it is now `D25`'s three piles — *waiting on the coordinator* `S72` `S88`, *your move* `S125`, *nothing outstanding* — ordered in SQL before the LIMIT, with `D26`'s **square-metre lead cell**, which had never been built. **`D26`'s clause was two of five states, not two of four** `AD33`. The **difference badge came off the row** `D66` and `S77`'s three figures came out of the Source card into their own above the lines — they had always rendered, as the last rows of a card called *Source* under two line tables, and the gate moved off a nullable column. Eight chips became four. Thirteen *internal sales* strings became *the coordinator*, in both locales. Carried `dispatches.ts:372`'s search — the company half only — and closed `AD31`. **`verify:routes` §24** is new and §22 was widened to assert the blank half of `D2`'s name columns, which presence alone could not. `sql<Date>` shipped a string past typecheck, lint and build, and 500'd every request until §24 drove it | `D24` `D25` `D26` `D66` `S72` `S77` |
| 28 | ~~**The project detail and the quotation detail.**~~ **Done** — `eef013a`. Both were one column of stacked cards with no related records and no caps; both are `D24`'s Detail archetype now, in the concept's two-column grid `D70`. **The project's turn panel read a different clock from its own strip** — the line said *waiting on the owner* on every open project while the strip below labelled the current step the coordinator's, which is the company panel's own defect found again; it reads `chainTurnKey` now, the key `/projects` renders, and its tone comes off `S89`'s stale boolean rather than off won-or-lost `D6`. **`getProject` carries `chainByProject`'s answer**, so the screen no longer builds a position out of ONE thread plus two dispatch reads: a project won by the direct route `S75` or by a second thread was won on the board and blank here, and the strip is unconditional now because `S132` gives a project one position however many threads it holds. **The `quotedSqm` sum went** `S68` — see the `§5` row below. A project's threads were unreachable from it and now have a card; `?projectId=` and `?threadId=` are the scopes `D70`'s *way to the rest* needed. `thread-lines.tsx`'s four forms are native `<details>`, bound at the call site, and `§23` sees 3,920 forms where it saw 3,686. **`verify:routes` §8 was rewritten** — it asserted a strip-or-flag dichotomy that this slice deletes, so it would have gone green over a project reading `new` while its threads were `won`; it pairs the board's column against the project's own strip and the project against the furthest of its threads `S132` | `D24` `D2` `D26` `D27` `D20` `D70` `S132` `S68` |
| 27 | ~~**The stream** — replaces `/reports` and `/activity`~~ **Done** — `f9a8b26`, **and split.** `/reports` is deleted and `/activity` IS the stream: `?view=stream` default and `?view=by-rep` beside it, `D45`'s four filters plus a range and a search, **filters down the side** as `D45` asks — the one screen in the product with a filter column. **The one query is `gather()`**, which was already `D45`'s three kinds over six sources; `listReports`, `reportsInRange` and `defaultRange` are gone, and `by-rep` now COUNTS the events `?view=stream` lists rather than re-reading them, which is what `reportsInRange`'s own docblock claimed to prevent while being the second reading. `verify:routes` **§25** asserts the thing that separates one query from three that agree by accident: **typed + observed + said equals the unfiltered total**, in both locales. Carried `reports.ts:527`'s search and `D48`'s marker `AD7`. **Comments narrowing moved to `27b`** — a migration with a disclosure decision attached does not belong in a screen slice | `D45`–`D47` `D30` `D31` `D3` |
| 27b | ~~**Comments narrowed to quotation threads and projects** `S114` `D48`~~ **Done** — `fc4500c`. `0032` narrowed the CHECK, `COMMENT_RECORD_TYPES` went five to two, `visibleCommentsFilter` went **five branches to two** — counted from the code — and three `CommentBox` call sites came out. **The rows were three, not nine**: `§5` carried a 27 Aug figure and said to re-measure, and the 29 Aug count was `3 company · 0 contact · 0 dispatch`, all three of them `verify:routes` residue naming themselves in their own bodies. **`seed:demo` wrote none** — its dataset has been two-kinds-only by its own TypeScript type since it was written, so the prompt's seed-writer hunt found nothing; the writers that would have died on the CHECK were `verify:routes` §9 and §21, `verify:comments` and `verify:sharing`. **0 mentions and 0 notifications** pointed at a deleted comment; the three `audit_log` rows STAY, by founder decision — `S112` never shows one without joining back to the real record, and `§7`'s *a migration clears* is about data, not history. **`0024`'s trap does not apply and the header says why**, so nobody copies a cast that would break it: no type is rebuilt and `record_type` keeps all five values for its four other tables. **The contact and the dispatch lost the whole card, not the composer** — neither has a derived event, so comments were the entire contents and what was left was a shell `D70`; the company keeps its card. **The trap the slice introduced is its own**: a company scope mapped to `{type:"company"}`, and the obvious repair — falling through to `null` — means *no anchor term*, which would have put every comment a viewer can see on one company's timeline. Hence `commentAnchor`'s third answer. **Both new assertions were fed the defect**: three raw inserts refused by constraint name while a project insert is still allowed, and `CommentBox` put back on the company screen, rebuilt and driven — 4 red, nothing else moved. §9 now registers a project **through the form, with its participant** `S27` rather than by direct insert, and **leaves one project per run behind where it left none**, recorded in place. §21's turn-panel probe is deleted with what it no longer proves stated. Takes both markers off `S114` and `D48`, and closes `AD7` | `S114` `D48` `S131` |
| 28b | ~~**Targets and Performance merged**~~ **Done** — `79164f6`, **and it was mostly a deletion.** `/targets` is the surviving route and `/performance` is gone; of its three blocks **one is the table**, one moved and one was deleted. **Coverage went** — `S88` says it in those words, *there is no separate coverage screen* — and the capability did not go with it: `/companies` already lists every company with its meter grouped quiet-first `D25` and **gained the Log action per row**, the only company list that lacked one. **`S123`'s figures moved to `/users/[id]`**, because three figures under one heading needed a paragraph saying they could not be combined, and two of the three DO combine — `raised` is `raisedForThem`'s own denominator — so it is a fraction and a standalone figure now, with no second clock beside them to explain away. `AD20` settled: **`D58` means it**, and the editor is `Disclosure` in a row of its own, which came out of `thread-lines.tsx` at its second call site rather than its third. The table is **flat** — `D49` over `D24`, the row below. `verify:routes` **counts the rail** now, off the `<nav>` alone: a rule whose whole content is a number was one nothing was checking | `D49` `D58` `S88` `S123` |

### Phase 2b — What every screen then needs

| # | Session | Rules |
|---|---|---|
| 42 | ~~**Auto-refresh.**~~ **Done** — `5dd2e7e`. One count route at `/api/updates`, four scopes, sixty seconds, polling only while the tab is visible, and **nothing beneath the line moves**. Each scope's count is one aggregate over the **same `where` builder its list function runs** — extracted in `quotations.ts`, `dispatches.ts` and `projects.ts` so the two cannot drift — against the stamp that list already orders by: `lastMovedAt` on a dispatch, the thread or its live version on a quotation, `projectMovement().movedAt` on a project, `event.at` in the stream. `/activity`'s four enum checks moved into `timeline.ts` as `parseStreamFilters`, so the screen and the route share one definition of the scope rather than two. **The line renders nothing until a poll answers above zero**, so with scripts off there is no line and the page is unchanged `D20`, and it is an `<a>` to the screen's own URL — the ordinary server render an F5 would give. **`D72` names two screens this cannot serve and they are not built**: no follow-up row records *when it joined the list* — every `S89` condition is a day-threshold crossing, so the earliest a new row appears is Riyadh midnight — and `followUpScope` is ~15 queries, which would run on every open screen every minute. So the waiting list and the rail's Today count poll nothing, there is **one request per page**, and the conflict is recorded rather than half-built. **`D73` was written here**, out of the `28b` bidi defect, with its five live breaches fixed | `D72` `D20` `D73` |
| 38a | ~~**Mobile, the shell** — the rail becomes `D56`'s bottom sheet~~ **Done** — `b360458`. `AD8` was decided first and settled **three** clauses, not two: on the rail **the code was wrong** — a horizontally scrollable **top** strip, ~891px of `flex-none` links inside 351px, with Targets and Team two swipes off every screen — and its defence, *"no Sheet component, no new dependency, no JavaScript"*, named a Radix `Sheet`, an obstacle `D20` as rewritten no longer has. It is a native `<details>` whose `<summary>` is a bar fixed to the bottom edge and whose panel is its **peer**, not its child: every engine hides a closed `<details>`' non-summary children through a slot or `::details-content` and author CSS cannot reliably re-show them, so nesting would have left `D49`'s seven links unreachable at `md` and up. `flex-col-reverse` keeps the bar still when it opens; a full page load closes it. **`D74` is new** — 44px on a phone, as a floor on `Button`'s base rather than on its seven sizes, because every `FilterNav` chip and both pager buttons were `xs`, 24px. **The find nobody would have reported:** `SelectField` was `text-sm` with no `md:` step while `Input` and `Textarea` both had one, so **iOS Safari zoomed the page on focus for every `<select>` in FACET** — eight on the quotation form alone. Closed the rail-footer row below ≈487px, which the sheet does **not** make moot: it needs `md` width and a short window, and an iPhone in landscape is 852×393 | `D55` `D56` `D74` `D49` `D20` |
| 38b | ~~**Mobile, the row** — `D56`'s first clause across the lists~~ **Done** — `1bd164f`. The kept set is **lead cell, name, and the one column the list's own anatomy needs**, written per list into `D56`, and `D56`'s `[CHANGE]` comes off with all three clauses shipping. **It is six lists, not nine** — the founder's call: `/users`, `/targets` and `/activity?view=by-rep` are laptop-first `D55`, and `D49` is the second reason `/targets` is out, because it asks for the goal and the attainment TOGETHER while one kept column carries one of them. Those three keep `Table`'s scroller and **declare it at the call site**, which is the row below. **One DOM, two arrangements**: the markup is identical at every width and CSS re-lays it out, so the phone view cannot drift from the laptop's, `verify:routes` reads the same markers on both, and no script is involved `D20`. **The kept column and a row action are the same grid slot**, so *the one column* is enforced by the layout rather than remembered — a list annotating two collides visibly and `§27` fails it. `truncate` became `md:truncate` at four sites: the fixed layout's answer to a narrow column, which below `md` is a column that no longer exists. `/contacts`' name cell gained the `dir="auto"` it was the only list-name cell in the product without `D62`. The board's *current column* had no referent and now has none by design — every column snaps to the inline start, `min-w-72` below `md` so one fills a 375 screen, and `?view=table` `D28` is already the way to rows. `ms-auto` closed, third sighting | `D56` `D26` `D2` `D29` `D55` |
| 38c | ~~**Mobile, the log form** — `D46`, `D55`, `S32`: the screen a rep uses in a car~~ **Done** — `58820b3`. `S32` makes it the main entry point. Carries `ChainStrip` at 375, where six `flex-1` steps in ~290px truncate *With the customer* to four characters. Carries the project chips, which are `<button onClick>` over a hidden input — the one `useState`-only control on a phone-first screen. **`§23` does not report them**, and that was checked rather than assumed: its single note is `/quotations/new`'s disabled `contactId` select. The chips are optional, so no required field stands behind script and `D20`'s sentence is satisfied — but a control that cannot be changed with scripts off is still the question `38c` has to answer, and the fact that nothing flags it is half of why | `D46` `D55` `D24` `D63` |

### Phase 3 — Trust

**This is the next block.** Phases 0, 1, 1b, 2 and 2b are closed, each row
carrying the commit that closed it — `27b` was the last of them and shipped in
`fc4500c`. Every table from here reads in the order it is to be run, and **no
number is reused or reassigned** — `39` and `D18` set that precedent, and
renumbering would shift citations that already resolve.

Nothing below is optional before real users touch it.

| # | Session | Rules |
|---|---|---|
| 24 | ~~**The waiting list's other half — a deletion, 3,445 lines overdue.**~~ **Done — `S91`'s machinery deleted, and the row's own premise was wrong.** It read *"the dashboard replaced them and the old mechanism never came out: `follow-ups.ts` (1,522), `notifications.ts` (1,190), `/follow-ups` (248) and `/notifications` (385) all still ship beside it"*. **Nothing shipped beside anything.** The dashboard READS `follow-ups.ts` — `page.tsx` → `shellCounts()` → `followUpScope()` — and LINKS INTO `/follow-ups`, which is where `D33`'s four tiles point and what `D49` already calls *the waiting list, filtered*; `verify:routes` had been asserting both since Phase 2. `notifications.ts` has seven live `raise` callers and `/notifications` is the only surface `S92`'s bell has. **All four stay.** What `S91` actually deletes is inside them, and it came out: the two tiers, the `is_persistent` flag, the per-anchor resolution conditions, `sweepNotifications` with its two resolve steps and `generateDigests`, `RESOLUTION_RULES`, and the `followup.digest` type with its nine rows — **326 lines out of `notifications.ts`**, the screen's two-card split, and `followUpsForRecipient`, whose only production caller was the digest. Migration **`0033`** drops four columns, two partial unique indexes and the `notification_tier` type, and adds `AUDIT 1 E2`'s **`notifications_record_pair` CHECK**, which took the `?? "company"` / `?? ""` defaults off the screen. **The badge counts UNREAD**, a defect the deletion itself created: nothing writes `resolved_at` once the sweep is gone, so the old query could only ever rise. `S90` and `S122` amended per the founder's two decisions; `S91`, `S92` and `S122`'s markers off. **`verify:routes` §29 is new** and asserts the machinery ABSENT — fed its defect in both locales and shown to go red — and **no verify script was deleted**: `verify-phase10a` and `verify-followups` both verify things that are still true, so the suite stays at ten. `seed:demo`'s clock named the two dropped columns and its own pre-flight guard refused the run until they came out — the one check in the tree that noticed the schema had moved without being told | `S86`–`S95` `S91` `S92` `S122` `S128` |
| 25a | ~~**The team table** — `D64`'s fourth block, the first of the two `sees_all_reps` adds.~~ **Done.** **The finding that sized the slice: four of `D39`'s five figures were ALREADY computed per rep, and not one visibility-scoped computation was widened.** `achievementForPeriod` returns a row per measured rep and the dashboard already called it; `followUpScope` is scoped by the SESSION rather than by *me*, so a manager's copy already held every rep's rows and `shellCounts()` had already derived it; `dailyActivity` already folds per rep and takes a range. Only *quiet companies* was new, and it is **one grouped aggregate** over the `companySilence` subquery `/companies` already joins — not `follow-ups.ts`'s third copy, which `§5` records as the one that has drifted. `scopeForUser()` was **not** called: it has zero production callers and looping it is the 15-queries-per-rep N+1 this row was most at risk of. **Eight new queries, constant in N; zero for anyone without the flag**, the block being gated before it fetches. **Three things `D39` did not say are now in it.** *Which reps are rows* — an active visible user with a **target row**, which is `D64`'s own first-block condition read wider `D38`, so a rep with no target is absent rather than a blank bar `D32`; on the fixture that is 4 rows, and it includes the **manager**, who carries a target, because `S83` makes targets independent of role. *The two counts* — the split the waiting list already ships, `ageDays > 0` against `ageDays === 0`, whose parameters are already named `overdue` and `dueSoon` in `turn.tsx`; **no new window, and NOT `D25`'s `OPEN — not chosen` due-soon**. *The week* — Sunday, derived from `S93`'s weekend rather than picked. **The footer drops the rule's example name.** *Saad* names a real seeded rep and a static claim about who logged most would be false on most days; **deriving** it instead would be the ranking `D39`'s last clause forbids, and that option was refused in writing rather than silently not taken. **`verify:routes` §35 is new**, driving the manager and a rep in both locales: presence as a **biconditional** so the absence is half the claim, the row set against the target set, one pace tick checked against working days the script counts **itself**, and — standing alone — **each rep's m² against the dispatch records**, guarded by a credit-split count so a legitimate difference reports NOT MEASURED instead of a wrong red `S45-1`. Fewer than two rows with figures is **NOT MEASURED**: a one-row table proves nothing about a table. **Fed the defect twice, and the first did double duty**: an over-counting fold turned the waiting assertion red in both locales while the m² cross-check stayed GREEN, which is the evidence the two claims have independent origins rather than moving together; then the precondition removed — a one-row table — and §35 declined to report, **zero of twelve assertions run, 1742 → 1730, accounted by diffing labels**: 16 out and 4 back, and 4 of the 16 were `/activity`'s stream-kind checks changing their label on fixture drift rather than anything this injection did. Commit `acb568b` | `D39` `D64` `D32` `D38` `S83` `S93` |
| 25b | ~~**The company target, and `D38`** — the panel a manager reads, and what it is measured against.~~ **Done.** **The gap was never arithmetic.** `D38` said what is DISPLAYED and nothing anywhere said what it is measured AGAINST: no company target in `SPEC.md`, in the schema or in the code, which is why the manager's panel honestly read his own `0 of 400` beside a team table of thousands. The founder's answer is now **`S136`** — *a company target is one figure for one month, set as a decision of its own*, and **neither it nor the reps' targets derives from the other**. That sentence is load-bearing and the schema states it: **`company_targets` is its own table**, not a nullable `user_id` on `targets`, because a nullable FK meaning *this row is the company* makes one column mean two things, has no invariant for the CHECK it would need, and drops the company row **silently** from any `innerJoin` onto `users`. **The flag is NEW and `S136` says so on its own face** — `can_set_company_target`, super admin alone. It had to be new: `can_set_targets` is held by the **Sales Manager**, so gating on it lets the person who divides the work across the reps set the figure he is himself measured against — `S82` one register up — and `can_export` / `can_impersonate` exclude the right people while meaning other acts, which is the proxy shape `authz.ts:1147-1162` warns against in its own words. Widening it is one line in `ROLE_SEED`, no code `S7`. **Abdulrahman's 400 goes and is migrated into nothing**: `S136` says a person who sells and holds `sees_all_reps` carries no target of their own, and converting his row into the company figure would assert the very derivation the rule forbids. **Consequence, recorded rather than discovered: `D39`'s team table falls from four rows to three**, and `§35`'s floor is two. **One component, not a copy** `D38`: `TargetPanel`'s prop narrowed from `AchievementRow` to the two fields it actually read, so `AchievementRow` and `CompanyAttainment` both satisfy it; the per-row disclosure is reused for the company control with its handles parameterised. **The company figure is derived over the DISPATCHES, never by summing the per-person rows** — those keep only shares whose user is active and visible, so a deactivated rep's `S111` credit is missing from every one of them. **The Drizzle qualifier trap is made unreachable rather than navigated**: no new correlated subquery is written at all, `dispatchesInPeriod` already encoding *approved, in period, summed from the lines* `S72` `S116`. **`verify:routes` §36 is new**, 23 assertions, driving the manager, a rep and — signed in inside the section, so `§2`'s three-identity loop is untouched — the flag holder: presence as a **biconditional** on `data-scope`, so the absence is half the claim; the m² against the dispatch records; the denominator against the **superseding** row of two seeded for the month; the company figure asserted **not** to be the reps' targets summed (5,000 against 6,000, `NOT MEASURED` on a fixture where they agreed); and the real POST replayed with a **measured restore** — `§17` avoids a real write because it would move every later figure, so this writes, proves the panel moved, writes the original back and puts the restore's status on screen rather than in a `finally`. It needs **no credit-split guard where `§35` needs one**: a split moves metres between reps and cannot change a company total, so this comparison holds under exactly the condition that suspends `§35`'s. **Fed the defect twice.** The first did double duty: dropping the direct route `S75` from the company fold turned §36 **red in both locales** while `§35` stayed **GREEN** through the same injection — that green half is the evidence the two claims have independent origins rather than moving together `d66e1a0`. Then the precondition removed — the month's `company_targets` rows deleted — and §36 **declined to report**, **zero of 23 assertions run, 1715 → 1698, accounted by DIFFING LABELS**: **72 departures and 55 arrivals**, of which only **23** were §36's; the other 49 out and 55 in were `/activity`'s stream totals (425 → 445), the bell (19 → 25), `/follow-ups`' groups (67 → 73), `§35`'s own m² and four `§17` service-line forms — fixture drift between two runs, not this injection, which is `b7dfac7` on its face. **Two runs also caught a shared label**: `§35` and `§36` printed an identical setup line, which makes a diff ambiguous about which moved, so §36's now names its own subject. Commit `9d48bbd` | `SPEC.md` `S136` `S83` `S84` `S110` `S82` `S111`, `DESIGN.md` `D38` `D32` `D64` `D49` |
| 25c | ~~**The manager and coordinator remainder** — `D40`'s *Waiting on the coordinator*, `D41`'s *Needs a decision*, and `D65`'s day count.~~ **Done for `D40` and the day count; `D41` is split out to `25e` on a finding, and `D64` goes to FIVE of six.** **`D41` rests on two tables nothing writes, which no amount of screen work fixes.** `duplicate_flags` and `delete_requests` have **no writer, no reader and no rows** — `grep -rn "duplicateFlags\|deleteRequests\|nonDuplicates" src scripts` returns nothing outside `src/db/schema.ts`, the demo seed inserts none, and both sit in `SPEC §15`'s *kept because a rule still asks* list beside three others. `S21` says in its own text that nothing flags a duplicate; `S106` says the rep's request is the only missing half of the archive review. **A card reading two empty tables is a reader for something nothing fills** — the mirror of `CLAUDE.md`'s prohibition on a column without its writer, and `AD4`'s finding one register on: that row found half of `D41` resting on rules that do not exist, this finds the other half resting on tables nothing writes. Building it anyway would have satisfied `D64`'s count while shipping a block that can never render. **`D40` is two counts and two ways in, and each of its three unstated parts is a decision with a reason.** *The aggregate, not the list* — the identity it appears for holds neither `can_approve_quotation` nor `can_dispatch`, so rows carrying Issue and Decide would be rows the reader may not take, which is `D65`'s *a column nobody may act on is worse than no column* one step out. *The two sets are `D65`'s own* — `awaitingIssue` and `status=submitted`, no new predicate — and deliberately **not** `chainGroup`'s `coordinator` pile, which also holds `readyToShip` and would count one request once in each tile. *No age on either tile*, which is a refusal rather than an omission: `quotation_versions` has no `issued_at`, so the issuing side could offer only the thread's own age — `quotations.ts`' own words, *how old the DEAL is, not how long this position has been owed* — against a true wait on the other, and the clock that would answer properly is `follow-ups.ts`'s, the second ladder `D27` pins to one file. **Each tile is suppressed where the reader holds that chain's own flag**, so a Super Admin gets no card at all rather than a weaker copy of `D65`'s block directly above it. **Two calls, gated before it fetches** — the `RequestsBlock` shape — so a rep and the coordinator pay nothing, and nothing fires per row. **The day count is ONE query over the audit log, and two of its three figures exist nowhere else.** A dispatch approval has `approved_at` and `approved_by_user_id`; a **refusal** and a **quotation issue** have no columns at all (`schema.ts:1699-1706`, `dispatches.ts:302-306`, `quotations.ts:403-408`), and `TimelineEventKind` has no refusal kind — so `tallyDays`, `dailyActivity` and `/activity` are each blind to one of the three. Reading `approved` off its column and the other two off the log would be **two definitions of one figure**, and the column is the wrong subject anyway: the `dispatched` stream event keys on `dispatch_date`, a freely backdated typed day, and credits `recorded_by_user_id` rather than whoever approved it. *Her day* is the **Riyadh calendar day** — `WORKFLOW §5`'s alternative, *since she last cleared the queue*, needs a cleared-state, which is the persistence machinery `S91` deleted and which a queue worked by ranking never reaches — and the acts are **hers**, `coalesce(acting_as_user_id, actor_user_id)` `[07 A6]`. **The cost claim was proved by RUNNING it**, not read off the code: **2,131 audit rows, a seq scan, planning 0.102 ms and execution 0.242 ms**, so no index — `schema.ts:2562`'s *`S123`'s measure adds an index with its query if it needs one* is a condition, and it is not met. **No schema change and no migration.** **`verify:routes` §37 and §38 are new.** `§37` asserts presence as a **four-identity** biconditional and the fourth is the point: manager present, coordinator absent, rep absent, and **admin absent** — a check driving only the manager and a rep would be green over `D64`'s bare flag reading, and the super admin is the only identity that can tell the two rules apart. Its two counts come from raw SQL over the facts the screen's calls select on, asserted as the manager alone, whose visibility filters return `undefined` (`authz.ts:670`, `:728`), so a scope difference cannot produce a wrong red. `§38` reads the three figures against **this script's own** audit-log SQL and then **creates the state it measures**: a fixture where the day happens to be empty makes every equality `0 === 0`, so it drives a real **refusal** over HTTP — chosen over an approval, which credits a target and moves every m² figure after it — and takes **two observations of the one act, kept apart**: her `refused` figure up one, and the MANAGER's `D40` tile down one, two identities over two derivations. The restore is `S122` working rather than a repair — revive returns the request to the rep as a **draft**, so the queue is whole only once rep-a re-submits, and `submitDispatchRequest` accepts nobody else — with **each of the three statuses on screen as a `check`, never a `finally`**. A fifth assertion says the day KEEPS the refusal afterwards `S107`, so a later reader cannot take the surviving figure for a failed cleanup. **Fed twice each, and both firsts did double duty.** (1) The tile counting the fetched page: `§37` **red in both locales**, *saw 25 on the card against 40*, while every `§38` line stayed **green** — the evidence the two have independent origins. And the **deciding** tile stayed green through the same injection, because 19 is under `PAGE_SIZE` 25: *a page-local count is invisible below the page size*, which is worth knowing before anyone trusts one. (2) The day window widened to thirty days: `§38`'s cross-check **red**, *54 · 39 · 18 on screen against 44 · 35 · 15 in the records*, while all eight `§37` claims stayed **green** — and `§38`'s own movement check ALSO stayed green, because a refusal moves a wrong window by exactly one too. That is why the cross-check stands beside the movement rather than instead of it. **Both preconditions removed, and both deltas ACCOUNTED by diffing labels rather than subtracted.** `§37` blinded to both piles printed `NOT MEASURED`, ran **zero** of its twelve count claims, **1822 → 1810**: 20 departures against 8 arrivals, of which exactly **12** were `§37`'s — the other 8 out and 8 in were the run's own harvested ids moving between runs, since the run writes. `§38` with no submitted dispatch recorded by rep-a declined the movement, ran **zero** of its five, **1816 → 1822** the other way: 10 arrivals and 4 departures, **6** of the arrivals `§38`'s and the rest the same id drift. **`§0` fired for real, twice over, and the second shadow it cannot see is now a `§5` row**: a stale holder of port 3000 (stopped by PID, as its own message instructs), and then the compose **app container**, which publishes `127.0.0.1:3000` — more specific than `next start`'s `0.0.0.0:3000`, so on Windows localhost reaches the container and `§0`'s own health probe answers from it. **One repair to this file, found while placing the rows and not looked for.** **Six `§5` defect rows were sitting at the foot of `§10`'s snapshot table**, which is **two columns** — so every one of them lost its *Disposition* cell when rendered, including the day-count row's own *what does her day mean*. They are moved into `§5`, where their three cells fit, and `§10` ends at `Blocking` as it reads. A seventh row there (`27b`'s comments row) carried an **unescaped pipe** inside a code span, splitting it into four cells and dropping its *Done*; escaped. Both tables now hold rows of one width, checked rather than eyeballed. **Closing run: all 1822 checks passed, and all ten verify scripts are green.** The two new sections' figures, as read: the issuing pile **42**, the deciding pile **19**, and her day **approved 52 · issued 39 · refused 21**. **`SPEC.md` is untouched** — nothing here needed a rule that does not exist — and `CLAUDE.md` stays at **twelve**: no check was found wrong in the direction that matters. Commit `a9e455c` | `D40` `D64` `D65` `D53` `D70`, `S72` `S88` `S112` `S122` `S124` |
| 25d | ~~**The payment-method label**, split out of `25b` so the row it was buried in could close.~~ **Done, riding with `25c`.** `enums.paymentMethod.handled_by_finance` alone: EN *Credit or handled by finance*, AR *آجل أو تُدار من قِبل المالية*. **The enum value does not change** — `PAYMENT_METHODS` and `payment_method` both still read `handled_by_finance`, which is why the names live in `messages/*.json` and rewording one costs no migration `enums.ts:129-155`. **No key is added or removed**, so `check:messages` parity is untouched (913 keys, en and ar agree), and **no verify script asserts any payment display string** — searched exhaustively: every assertion is on the token (`data-payment="bank_transfer_full"`, `method: "handled_by_finance"`), and `verify-slice3.ts:807`'s only mention is a prose comment. **`S71`'s optional note already existed and was already doing this work**: the demo seed's three `handled_by_finance` dispatches carry `تساهيل — المرجع في سماك`, `عقد شركة — التسوية في سماك` and `حساب ائتماني — المرجع في سماك`. The label was the gap, not the capability. Commit `a9e455c` | `S71` |
| 25e | **`D41` — *Needs a decision*, and what it is BLOCKED ON.** Split out of `25c`, which found both objects it names to be tables nothing writes. **It is not blocked on a founder decision** — `AD4` was, and `AD4` is closed; this is blocked on **two build paths**, and it should not sit in a queue waiting for an answer. **(1) `S22`'s detector.** `S21` says in its own text that *nothing flags a duplicate at all*: `createCompany` performs no match, `companies.name_normalized` and `companies.phone` are read only by the `/companies?q=` box, and `companies.merged_into_id` has four `isNull()` readers and **no writer**. So `S22` needs a detector, a queue and a resolver with its three outcomes, plus writers for `non_duplicates` and the tombstone. **(2) `S105`'s rep-request path.** `S106` says the review, the three outcomes and the one record all exist — they are `company_dormancy_reviews` and the three forms on `/companies/[id]`, gated by `canAssign` per `S107` — and that *only the second way in, the rep's request, is missing*. **The vocabulary conflict bites first and is a schema decision, not a form**: `delete_requests.status` is `pending / granted / denied` while `S105`'s outcomes are *archive · keep · reassign*, and `company_dormancy_reviews.outcome` is **NOT NULL** so it cannot hold a pending row — `§5` has carried that since the schema audit and it is still open. **A third question the card itself raises**: `D41` says `can_assign`, while `S8` gives duplicate resolution and delete approval to `can_resolve_duplicate` and `can_approve_delete`, which have **no reader anywhere**; whichever it reads will be their first, which is what `S107`'s `[CHANGE]` asks for. Do **not** build the card before at least one half writes rows: a reader for a table nothing fills is the mirror of a column without its writer. **The archive half shipped in session 54 phase 3 (row 32)** — the section renders inside Stuck on `can_approve_delete` with the open requests; the duplicate half is phase 4 (row 30) | `D41` `D64`, `S22` `S21` `S23` `S105` `S106` `S107` `S8` |
| 43 | **Backups — automated, with a restore proved onto a second machine.** **OPEN — half shipped in `55faa3e`, and the half the row is named for has not.** `§7`: what is true today is `npm run backup` · `restore` · `backup:verify`, and a restore proved TWICE on this machine — into a scratch database beside the live one, and into a throwaway `postgres:17-alpine` on an **empty volume**, the nearest available stand-in for the second machine, since a same-cluster restore silently reuses roles, extensions and settings a real restore must carry itself. `backup:verify` asserts by **identity** — the set of tables, then an exact `count(*)` each, `schema.table: N = N`, plus the ledger's last row — and **fails on a comparison that read nothing**, because 0 = 0 across 43 empty tables is green and proves nothing. Both stages were fed both defects and watched going red. **The first injection was REFUSED by a foreign key before it was moved to a leaf table, which proves more than the check set out to prove**: the restored copy carries the CONSTRAINTS, not merely the rows, and a restore that had silently dropped them would have let that `DELETE` through. 20 of the 42 public tables are leaves and can be injected this way; the other 22 have an incoming FK that refuses it. **What has NOT happened is the restore onto a second machine**, which is what the row asks for: there is one machine here, and the founder does it by hand from `README.md` § *Host-side steps*, along with `BACKUP_DIR` and the Task Scheduler entry. The premise the brief carried was also wrong — **the NAS's file-level sweep does not cover PostgreSQL**, because the data directory is copied while being written, so this adds one consistent dump into a folder the NAS already sweeps rather than a backup system. Host scripts are **tsx TypeScript, not PowerShell**: `status.ps1` is the only `.ps1` and is read-only, and PowerShell 5.1's `>` re-encodes binary, so a `-Fc` dump through it is corrupt and exits 0. **The row closes when the second machine has restored one** | — |
| 44 | **Security review** — a Cloudflare tunnel exposing an internal system. What is reachable, what authenticates, what is logged, and the auth bridge re-checked, which fails **silently** `CLAUDE.md`. **OPEN — the review shipped, the fixes are not chosen.** `§7`: the review half is done and produced seven rows in `§5`, `S44-1`–`S44-7`; none of them was fixed here, deliberately, because a session that finds an auth defect and patches it in the same breath produces an unreviewed patch on the code that decides who sees what. **What was proved rather than read.** The **auth bridge is INTACT**, run end to end over HTTP for the first time: a real login wrote one `sessions` row, the cookie carried a 36-character uuid rather than a JWT, `deactivateUser` left zero rows and the very next request was `307 -> /en/login` — `S101` holds against the running system, not merely against the rule. `S102` refused with `team.errors.cannotDeactivateSelf`. An expired session is refused and its row deleted. The four packages are `next 16.3.0`, `next-auth 5.0.0-beta.32`, `@auth/core 0.41.3`, `@auth/drizzle-adapter 1.11.3`, installed exactly as locked, and **`package-lock.json` has not moved since `da2cfc5`** — the bridge has not been disturbed since it was built. A throwaway probe drove **86 requests as nobody**: 81 protected, 3 answered (Auth.js's own public endpoints), 2 unproven and since resolved by construction. **36 of 36 `(app)` pages and every mutating action gate themselves** rather than trusting the layout, so there is no middleware-matcher hole to find — `proxy.ts` does locale only. **`authz.ts:222` remains the only mutation in `src/lib` outside `withAudit`**, confirmed by enumerating every `tx.insert/update/delete` and every `withAudit` block: not one writes without logging. **Nothing logs a secret** — zero `console.*` in `src/`, `createUser` strips the hash from its audit payload, and no script prints a password. **No secret is in the repository or its history**: `.env` was never committed, no dump ever was, `/backups/` is git-ignored with two real dumps in it and nothing tracked, and no literal assignment of `AUTH_SECRET`, `POSTGRES_PASSWORD`, `DEV_FIXTURE_PASSWORD` or `BOOTSTRAP_ADMIN_PASSWORD` exists in any revision — a search proved able to reach 1,954 blobs and to match a synthetic assignment before its emptiness was believed. **Six things this repository cannot answer, and the founder must check at the machine.** `S44-U1` **the Cloudflare Tunnel** — open to the whole internet, or behind Cloudflare Access? `cloudflared` is configured on the Windows host and is invisible from here; **this is the largest single question in the row**, and `S44-2` makes it sharper rather than softer, because Access in front of the tunnel does not cover port 3000. `S44-U2` **Windows Firewall on the office PC** — is inbound TCP 3000 allowed on the office network? Confirmed open on *this* machine; the office PC is a different machine. `S44-U3` **Docker Desktop on the office PC** — does `docker ps` there also report `0.0.0.0:3000->3000/tcp` and `127.0.0.1:5432->5432/tcp`? `S44-U4` **the office `.env`** — is `PUBLIC_URL` set to the https tunnel hostname (`S44-3`), is `AUTH_SECRET` a real random value, and does `POSTGRES_PASSWORD` still carry `.env.example`'s placeholder? Reads of `.env` are denied to Claude by `.claude/settings.json`, so this is unanswerable from here by construction, not by omission. `S44-U5` **`BACKUP_DIR`** — the NAS-swept folder holds dumps containing every password hash; does it carry the access control the database has? `S44-U6` **whether the office deployment carries the same eleven-day image drift** `S44-5`. **Three of the seven have since shipped.** `S44-2` and `S44-3` were configuration and went in the deployment-hardening commit: the app publishes `127.0.0.1:3000:3000` — checked against the one thing that could break the office PC first, and `cloudflared` cannot be in the compose network on either machine — and `.env.example` carries `PUBLIC_URL`, with the `Secure` mechanism proved by measuring the same login under an `http` and an `https` `AUTH_URL`. `README.md` records Cloudflare Access as decided, states in one place that Access covers the tunnel and **not** port 3000, and adds the tunnel-ingress check that is the one way loopback-binding breaks the deployment. **`S44-1` has since shipped too — three of the seven.** `verify:routes` §30 signs in over HTTP as a subject it creates, reads the `sessions` row its cookie names, proves the live session renders a real page, revokes through the real screen and asserts the row is gone and the next request refused `S101`. It cost `verify:routes` its pure black-box property — four names, two SELECTs, measured as free at module scope — and **feeding it its own defect found a defect in the check**: the redirect half stayed green with revocation removed, because `authz.ts:221` refuses an inactive user regardless, and the row count only pins the mechanism when read before `authz.ts:222` clears it. Both halves are kept, in that order, with what each proves written at the line. **`S44-4` has since shipped — four of the seven**, as `verify:routes` §34: a permanent fourth identity, **nobody**, driving 130 surfaces at **126 PROTECTED / 4 answering / 0 UNPROVEN**, with the positive control that came across with it proved by pointing it at a dead port. **`S44-5` to `S44-7` stay open**, and they are now the only code rows left. `S44-U1` to `S44-U6` stay unchecked: no session can see the office machine from here. **The row closes when the fixes are chosen and shipped, not when the review was read** | `WORKFLOW §5` `S44-1`–`S44-7`, `SPEC.md` `S101` `S102` `S107` `S111` `S112`, `CLAUDE.md` *Auth bridge* |
| 45 | ~~**Error pages, and a way for a person to report a problem.** What a 500 says, what a 404 says `D53`, and where a rep's *"this is wrong"* goes when the alternative is telling nobody~~ **Done — session 55** (row 60). A stray URL under a locale now reaches `[locale]/not-found.tsx` through a catch-all page — the reader's language, direction and theme, one way home, a 404 for an anonymous visitor rather than a redirect that lists routes (§6 drives all three). A screen that throws reaches `(app)/error.tsx` — or `[locale]/error.tsx` outside the shell, or `global-error.tsx` when the layout itself failed — which says nothing was saved, offers *Try again* and Today, and asks the person to tell the manager and quote Next's digest, the one handle that pairs the screen with the server log. Looked at by a temporary throw (rep en/ar, dark/light, 1366/375/1440; `audit-shots/errors55`), not committed. **And a finding on the way**: every 404 in the product — the permission 404 since session 23 — was Next's error shell with the page only in the script payload, blank with scripts off `D20`, because the `<html>` shell lived under the locale segment and a not-found boundary renders inside the nearest ROOT layout; the shell moved to `src/app/layout.tsx` (locale from `getLocale()`), the styled 404 to `src/app/not-found.tsx`, and §6 now reads the DOM before the first `<script` — the **eighteenth wrong-red**, a green that read the right string from the wrong place (`facet-verify`). **The report-a-problem half is recorded, not built**: whether *"this is wrong"* should be sendable from the screen is the founder's (`SPEC §16`) | `D52` `D53` |

> **AUDIT 2 — the interface.** See §6. **`§6b`'s first rule review runs beside
> it**, over `DESIGN.md`, because the audit that has just read every rule is the
> cheapest moment to ask whether each one is still right.

| # | Session | Rules |
|---|---|---|
| — | ~~**AUDIT 2 — the visual and data audit** `§6`~~ **Done** — `1dc307b`. **The interface was driven as pixels for the first time**: `@playwright/cli` (devDependency) + `scripts/audit2/capture.mjs` captured **936 states** — 56 declared states × the identities that reach each × en/ar × dark/light × 1366/1440/375 — against the **built** server on `:3100` (the compose container held `127.0.0.1:3000`, `§0`'s shadow, live for the whole run; every shot asserts `location.host`), on a fresh `seed:demo`; sessions were persistent per identity, signed in once. **The light theme rendered light, first time ever.** Eight passes — typography, surfaces, light, RTL, 375, archetypes, motion, the `facet-ui` skill — and the data half re-derived ~40 rendered figures against SQL written fresh from the S-rules over `docker exec psql`. Output: **`§5` "Added by AUDIT 2"** — a ranked top ten and rows `A2-1`–`A2-27`, each carrying its shot filename and one of the brief's four verdicts; two unreachable states named with reasons, not skipped. Two real data defects (`A2-5`, `A2-6`) among ~40 figures that reconcile exactly; the founder's five reports each traced to a mechanism or a rule. Harness notes for reuse: `run-code` has no `URL`/`process` globals and `--raw` double-encodes; **`networkidle` never settles on force-dynamic pages** (link prefetch) — wait on `load`; a full-page shot paints fixed bars at scroll position; a submit-click must scope to `main form` or it finds the rail's sign-out. **No `src/` change, no `SPEC.md` change, no `DESIGN.md` change**; typecheck · lint · build · check:messages unmoved | `§6` `§6b` `D5` `D12` `D15`–`D17` `D24` `D34` `D56` `D62` `D64` `D68` `D73` |
| 46 | ~~AUDIT 2's fixes~~ **Done for the four slices the run named** — `4ed64af` (A2-1, the ar dates: thirteen removals, `D73` rewritten, `verify:routes` §39), `106ff98` (A2-5, the dispatch-blind Quoted tile: the two flags, §40, `CLAUDE.md`'s wrong-red count → thirteen), `42e921e` (A2-4, the light theme: five tokens re-chosen by eye, `D5` amended), `a39422c` (A2-15, the glass acquitted by OS-level pixel measurement). **Left deliberately**: A2-2 and the rest of the deviates-bad rows (not named in the run), A2-3/A2-7/A2-8/A2-9 (`§6b`'s rule review or founder decisions), A2-6 (OPEN founder decision, row sharpened with costs). **Found**: `S46-1` — three verify scripts red on any day-1-of-month seed, pre-existing | — |
| 47 | ~~**The `§5` register, part 1 — the rule and citation rows.** This is what `§5` has been calling **Phase 1c**, which this section never defined: `AD18` `AD21` `AD24` `AD25` `AD26` `AD27` `AD28` `AD31`, plus the ~28 `dir="auto"` sites and the eight `SelectField`s~~ **Done** — `df25d85` (S46-1) · `e3fe989` (the `§6b` proposals) · `52c8b79` (the slice). It opened with `§6b`'s first rule review (seven proposals into `§5`, none applied) and with `S46-1`, whose mechanism was two reader defects in `src`. Of the named rows: `AD18` and `AD26` are folded into the `D62`/`D17` proposals (a citation fix inside a rule under review is the proposal's to carry); `AD21` is `D52`'s `[CHANGE]` marker, applied; `AD24` rewrote `D9` to what ships; `AD25`'s code half conformed to the current `D12` (body base, one title size, one tracking, four floor breaches to 10.5px); `AD27` deleted what was really dead and named what the audit's list got stale; `AD28` decided none earns a new rule — hexes to tokens, two citations, two recorded; `AD31` was already closed in 41. The `dir` sweep closed the register's whole direction family — `A2-11` `A2-12` `A2-13` `A2-19` (measured not-reproducing) `A2-20`, the notifications alignment sighting, and the option labels — and the `required` eight turned out to be a different eight than counted, three of them impossible (the spare-row sentinel). Every fix pixel-read on re-shots; suite green at every step | `D9` `D12` `D52` `D62` `D73` |
| 48 | ~~**The `§5` register, part 2 — the code rows with no other owner:** `companyQuiet`'s third copy of the silence derivation, `verify:schema25`'s `S121` assertion, `B1`–`B4`'s assertions that can pass without reading anything, `§17`'s six-runs-per-seed non-idempotency, and the seed's long-name RTL gap~~ **Done — `7ff889b`, ledger rider `a33fc75` — and half the named rows were found ALREADY CLOSED by earlier sessions**, which is a register lesson of its own: `S121` was `f301bf6`'s, `B1` was `05b15e7`'s, `B3` was sessions 26/27's, and the `§24` pile-sum row was superseded by the scope-walking rewrite — four rows describing defects the tree no longer held. What 48 actually built: the `companyQuiet` merge (one silence derivation, at last), `B2`'s id-pinned mention, `§17`'s paged fixture scans (proven past the old six-run budget by a natural experiment), the long quiet seed name (as a rename, so no clock moved — and it caught `/follow-ups` widening within the hour), and the remainder of the deviates-bad register: `A2-2` (RecordRow's phone grid), `A2-10` (rare outcomes fold, the turn panel's *here* becomes true), `A2-16`'s money grouping, `A2-17` (record dedup + the grid-minimum side-scroll, measured 460 → 375), `A2-21`'s shell. The idempotency row stays open, re-scoped: the teardown is its own session | `S111` `D20` `D56` `D62` `D70` `D73` |
| 49 | ~~**`§6b`'s first rule review, applied.**~~ **Done** — `c703b65`. Six of the eight proposals the founder approved: `D12` (rewritten — the 12.5px row-value floor, the 4.5:1 contrast floor, the `body` base and one page-title size written into the rule), `D15`/`D16` (six uses → **two** and **one**, the measured truth), `D17` (rewritten to *FACET is still* — the deviates-GOOD case), `D26` (rewritten to what a row can know), `D62` and `D73` (the block-and-container clauses). **Two HELD and untouched**: `D24`'s sort clause and `D68`/`D64`; the `D52` rider keeps its `[CHANGE]` marker. **`D12` is the only one with code behind it** — `--text-faint` lifted in both themes (light **3.07:1 → 5.08:1**, dark **3.66:1 → 4.94:1** on the card, clearing 4.5:1 on all four grounds) and **26 row-unique values** raised to 12.5px across 15 files; the `body` base and the one title size were already session 47's. The other five needed no `src/` change — measured, not assumed — and applying them **found three**, recorded in `§5` as FINDINGS rather than fixed: 102 Arabic numeric cells whose heading and number resolve `text-align:end` against different directions, 416 block-level `dir="auto"` carriers most of which are the CORRECT truncation fix the new `D62` would condemn, and the bottleneck card's two figures still carrying `A2-13`'s defect. Plus the **fifteenth wrong-red**, caught in-slice: §26 asserted a verbatim class string. `CLAUDE.md` gains the work-splitting clause and the wrong-red count → fifteen; `§5` gains the FORM FIELD ORDER row and a sharpened `A2-6` | `D12` `D15` `D16` `D17` `D26` `D62` `D73` |
| 50 | ~~**THE FIXATION — the rule architecture rebuilt from the ground up.**~~ **Done — this session.** Phase 0: every prohibition enumerated and eleven hook classes + seven permission rules made deterministic, each fed its defect (`docs/archive/28-fixation/00`). Phase 1: the 210-rule inventory and the citation census that forbids renumbering (124/136 S and 70/74 D cited in code). Phase 2: **seventeen founder decisions in his own words** (`28-fixation/02`). Phases 3–4: `CLAUDE.md` 332 → 125 lines as an index; six path-scoped rules in `.claude/rules/`; `facet-verify` absorbs the verification discipline; `facet-audit` and `facet-register` new; three agents; `S8` `S22` `S76` `S94` `S105` rewritten, **`S137` `S138` `D75` new**, **`S115` `S135` `D35` `D36` `D71` deleted as tombstones**; closed `§5` rows to `docs/archive/29`. Phase 5: README as the entry document; the facet plugin. Phase 6: every hook and rule proven, the suite green, measurements in `§10` | all five files, `.claude/**` |
| 51 | ~~**`S137` — the chase pauses while a dispatch is processing.** One `notExists` term in `quotationNoResponse` plus the resume/finish semantics; **re-measure the pool first** — the 3-of-15 figure is `S133`-era~~ **Done — session 55** (row 60): the term sits in `silentIssuedThreads`, so the chase and the Reports tab read one predicate; pool re-measured at 106 of 131 company-wide (verify residue) before the term went in; three checks in `verify:phase10a` — a draft pauses nothing, submitted pauses, refused returns | `S137` `S31` `S122` |
| 52 | ~~**`D64`/`D68` — the dashboard narrows to book-holders.**~~ **Absorbed into 56** — the approved overseer drawings have no waiting list, counts strip or greeting on an overseer's screen, so the narrowing is a precondition of building them and lands in that slice, `A2-8` copy pass and the §35–§38 re-read included | `D64` `D68` `S9` |
| 53 | **`S138` — never-contacted companies.** The settings row (start proposed 14, `OPEN — not chosen`), the rep reminder grouped or capped, the manager's per-rep count | `S138` `S89` |
| 54 | **Form field order — one form at a time.** Current vs proposed side by side; the founder corrects; his answers land in `DESIGN.md` as the order rules | SPEC §16, `D24` |
| 55 | **`D75` — plurals and trimmed factors.** CLDR plural forms in both locales' messages; the dimension display trim; no stored value moves | `D75` `S5` |
| 56 | ~~**The overseer surface, part 1 — the twelve answers and the Today tab.**~~ **Done — session 51**, ran ahead of rows 51–55 on the founder's instruction. Phase 1 `64244af`: the twelve answers recorded (`docs/archive/30-overseer-answers.md`), `S139`–`S142` and `D76`–`D81` written, seven rules amended, SPEC §16's overseer conversation closed. Phase 2 `e31e4f6`: lead source mandatory at creation (`S17`) — required at the form and the action, never-clear in the data layer, the 261 blanks untouched, the demo seed reborn with the pre-rule world (76 of 121 blank), §13 grew nine S17 checks fed both injections. Phase 3: the Today tab built to the approved drawings and **looked at** — five identities × en/ar × dark/light × 1366/1440/375 × empty/realistic/heavy/first-run, every shot read by eye (`audit-shots/today*/`); `D64`'s narrowing live (row 52 absorbed); `D40` inside Stuck with the way-in links and the oldest line; §19/§35/§37/§38 re-pointed, §41 new (five injections, 11 reds where designed, deltas by label diff) | `S17` `S141` `D64` `D76`–`D81` |
| 57 | ~~**The overseer surface, part 2 — the Reports tab.**~~ **Done — session 52**, on the founder's instruction ahead of the week with Today. Phase 0 answered his *the meter reads zero* report from the records: nothing approved in September, the same zero on the band, the Team tab and `/targets` — correct, and the behind-pace reading on day two filed as a `SPEC §16` question rather than changed. Phase 1 `5cc94ce`: the two channels he approved joined `S17` (online or WhatsApp · consultant or architect), four recent demo companies carry them, §13 holds both selects to `lead_sources` itself (7 of 8 for a rep, 8 of 8 for the manager). Phase 2: the tab built to the approved drawings — the period as URL state, three headings, eight readings, CSS bars in four forms with every figure as text, the not-recorded bar, loss ranked by count with the signal line, quoted-and-silent as a count, the coordinator's piles with their metres, per-rep conversion with sitting time beside it, coming back, real customers; `D42` rewritten to what ships, `D43`/`D44`/`S139`/`S140`/`S142` closed, `D76` gains the pill; **looked at** — two overseers × en/ar × dark/light × 1366/1440/375 × this month/last month/12 months, 76 shots, zero overflow, every shot read (`audit-shots/reports/`); §41 re-pointed to the three-pill strip, §42 new (the gate, the chips, eight readings against the script's own SQL, figures as text, the mirror, the this-month biconditional; the fixture step makes the count ranking distinguishable from a metres ranking on every run). Measured against the drawing: 1,524px at 1366 for last month against 1,382 drawn — 55 of it the app shell's top bar the drawing omits, the rest the 24px card rhythm the register already records against `D22`'s 14 and a coming-back card that carries its total line | `S17` `S139` `S140` `S142` `D42`–`D44` `D76` |
| 58 | ~~**The overseer surface, part 3 — the Team tab.**~~ **Done — session 53. The dashboard finishes here: `D64`'s eight blocks are eight built and `SPEC §16`'s overseer conversation is closed.** Phase 1 `6bdcf02`: day one reads as *the month has just started* — the founder's *keep the number, change the words* on his meter-reads-zero report; the line at one working week, derived from `S93`'s weekend, both directions withheld, `D32` and `D79` amended in place, §41 holding the marker to the script's own working-day count. Phase 2: `D39` built to the approved drawings on the founder's answer 12 — every active book-holder with a live company plus anyone carrying a target, the dash rows live (Majed and, because the sentence says book-holder, the coordinator), `S138`'s never-contacted column, waiting-on-them in words, the per-row control as `28b`'s disclosure row (`D58`), *set a target for someone not listed* beneath the table for the case the row set cannot carry; the company target above the table IS Today's band (`D77`, one component) with `S136`'s control for its flag holder; **one person's world** behind `?rep=` — pace, companies by silence, threads by position, dispatch history, target history with the month's metres — gated on `sees_all_reps`; **Targets left the overseer's rail and stays for a book-holder** (`/targets` as their own screen and a deep link to the tab for an overseer — the conditional rail item, the alternative kept on the table, chosen as the smaller change), `D49` and `D76` amended; the **Team/Team collision** resolved by naming the user-management item *Users* / *المستخدمون*; the **archived-companies silence row closed** with one term inside `companySilence` (an archived company is never quiet, so every reader agrees). **Looked at**: 77 states — two overseers × en/ar × dark/light × 1366/1440/375 × the tab and two worlds, plus the coordinator, the rep, the admin's control — zero overflow, every state's height stable across identities; the one-rep, empty, over-target, five-digit and long-Arabic-name states through SQL toggles with a measured restore (`audit-shots/team*/`). **Measured against the drawing at 1366**: band 253px against 180 drawn (the legend row under the bar, the larger figure and the 24px rhythm), table 538 against 217 (five rows against three — two of them answer 12's dash rows the drawing predates — and each person carrying `D58`'s disclosure row beneath, the drawing's *Edit* column; row pitch 81 against 41), page 1,047 with the shell's 55px top bar the drawing omits. §35 rewritten to the row set against the script's own roster and the dash rows, §36 re-pointed (its rep-sum check had read the Today body since session 51 and printed NOT MEASURED on every run — measured now), §2/§17/§27/§32/`MARKERS` re-pointed, §43 new (22 checks); 1838 → 1879 accounted by label diff | `D39` `D76` `D49` `D32` `D79` `S138` |
| 59 | ~~**Session 54 — the five answers, the per-person filter, rep-requested archiving, duplicate detection.**~~ **Done — session 54, four phases, four commits.** Phase 1 (`5477de9`): SPEC §16's five session-53 questions closed on the founder's answers — the opening week stays at five working days, the team table splits into two sections (targeted people, then everyone else holding a live company; `D39` amended), *Users* stands, `/targets` stays for a book-holder — with §35 holding the sections against the roster; and **a sixteenth wrong-red found while placing that check**: §41's attention-row regex had carried a literal backspace since session 53 and read 0 behind rows on every run. Phase 2 (same commit): `D82` new — `/companies?rep=` in the screen's own chip idiom for whoever already sees other people's companies, every control carrying it, one person's world linking through; §44 new (14 checks, fed the dropped filter: four reds). Phase 3 (`503db3d`, row 32): the second way in — `company_removal_requests` replacing `delete_requests` in `0035`, the request answered by the review row that decided it, `S107`'s gate built on `can_approve_delete`, `D41`'s archive half inside Stuck; §45 new (15 lines, fed two defects: the keep no longer answering went red twice; the blank reason did NOT — the action refuses before the data layer, so `verify:phase10a` gained the data layer's own two refusals); the seventeenth wrong-red caught in-slice (a gate test read a 404 for an identity that could not open the page). Phase 4 (row 30): the detector, the queue, the side-by-side, the three outcomes, the merge counted before and after on the real seed; §46 new (16 checks + a setup line), 1941 → 1957 → 1952 → 1969 accounted by normalised label diff at every step. **Recorded rather than answered** (`SPEC §16`): the phone fold's reach, what a merge leaves on the tombstone, and that nobody tells the rep about a flag or a request's outcome | `D39` `D82` `S105`–`S107` `S21`–`S23` `S8` `D41` `D78` |
| 60 | ~~**Session 55 — the rep is told, the calendar, the error pages, `S137`, the register's defects, and the empty database.**~~ **Done — session 55, four commits.** Phase 1: `S128` amended on the founder's *yes* — archive, keep, reassign and merge reach the rep on the bell, five decision kinds beside the four, the company's name carried on `S128`'s own exception, raised inside `dormancy.ts`'s three decisions and `duplicates.ts`'s merge; §45 and §46 read the rows and the rendered items (fed: the keep's raise silenced went red). Phase 2, in this order — the calendar first because the founder named it, the error pages second because they are a day-one screen, `S137` third because its rule was already ruled, the register's defects last: `S94` built (migration `0036`, proven from `information_schema`; `src/lib/calendar.ts` the one reader; `monthWorked` moved into `working-days.ts` with an off-day set; per-person pace on every tick; the two working-day cut-offs walk the reader's own off days; `/calendar` reached from the pace panel, the Team tab and a person's page; §47 moves a real tick with a real leave and puts it back; `verify:phase10a` §4 and §17 — fed: a reader ignoring leave went red three times in-process and twice over HTTP); row 45's error pages; row 51's `S137`; six `§5` rows closed or narrowed above. Phase 3: `npm run db:clear` (below) and the first-run screen seen for real. **Recorded rather than answered** (`SPEC §16`): who enters what on the calendar, whose leave a manager's grouped list honours, the silence clock, the Reports tab following `S137`, and where *this is wrong* goes | `S128` `S92` `S94` `S137` `D32` `D39` `D53` `D52` |
| 29 | Signals and loss reasons unified; loss cascades down | `S43`–`S49` |
| 30 | ~~Duplicate detection and manager resolution — **founder confirmed BUILD, session 50, timed before bulk import (34); `S22`'s shared-means-access clause is his**~~ **Done — session 54, phase 4**, the writer `duplicate_flags`, `non_duplicates` and `companies.merged_into_id` waited for. `src/lib/duplicates.ts`: the key is digits with the Saudi code folded to `0` (`S23`), the detector runs inside `createCompany`'s and `updateCompany`'s transactions after the row and never blocks (`S21`); the queue is `D41`'s duplicate half on `can_resolve_duplicate` (its first reader); the side-by-side at `/companies/duplicates/[flag]`; the three outcomes — false flag remembered as an ordered pair, who continues folding the other in (contacts, live project links with a both-linked project's duplicate soft-removed, threads, dispatches, reports; memberships ended; tombstone), shared folding the newer into the older and adding every folded holder as a non-primary member. **The existing sharing rules were checked first and already read the founder's way** — membership is never a term of project or thread visibility, credit follows the dispatch — so the merge touches no deal. Looked at across manager/executive/rep/coordinator × en/ar × dark/light × 1366/375/1440 (`audit-shots/duplicates54/`). §46 new: two reps register the same number in two formats and both save; the flag and its audit row; the queue and the screen for four identities; false alarm remembered against a later phone change; a phone change on a third company raising two flags; **a merge on the real seed counted before and after in the script's own SQL** — projects, quotations, dispatches, reports, contacts under the survivor equal the two records' sums, the tombstone holds nothing, the folded record's rep loses the customer and keeps his project (`S30`); shared on a fresh pair — both reps hold the survivor, the moved project stays its owner's and closed to the other | `S21`–`S23` `S8` `D41` `D78` |
| 31 | ~~Credit terms — rep requests, manager approves~~ **Removed.** Credit stopped being a company property to request and approve; it is now one payment method among six, recorded on the dispatch — `S71` | — |
| 32 | ~~Archive requests folded into the dormancy review — **founder confirmed BUILD, session 50: the press is a request, never an action, and the reason is required (`S105`)**~~ **Done — session 54, phase 3.** The vocabulary conflict settled as a schema decision: `delete_requests` and its `pending/granted/denied` enum left in `0035`, replaced by `company_removal_requests` — who asked, why, when — which **never names an outcome** and is answered by `review_id`, the `company_dormancy_reviews` row that decided it (`S106`'s one record, its NOT NULL outcome untouched); one open request per company by partial unique index. The rep presses from the company page (a disclosure, a required reason in a textarea) only while holding a live membership; while it is open the turn is the manager's (`D2`) and the rep's own *keep* is withheld; the next of the three decisions closes it inside the same transaction. **`S107`'s gate built**: `archiveCompany` reads `can_approve_delete` — the flag's first reader — and reassignment keeps `can_assign`, so the Executive archives and keeps and cannot reassign, each control on its own flag. `D41`'s archive half renders inside Stuck for the flag holder: a count, five rows oldest first each a way in, *and N more*, one line at zero. Looked at: rep, manager, executive, coordinator × en/ar × dark/light × 1366/375/1440 (`audit-shots/removal54/`). §41 re-pointed from *absent* to *present with its count*; §45 new — the press over HTTP, the blank reason refused as a message, the row and its audit line, the list total unchanged, the second press refused, the manager's decisions ARRIVING with the request, the coordinator replaying the manager's envelope refused, the manager's keep answering the request, the rep asking again, the executive's archive landing with the request answered, two request lines kept in the history; `verify:phase10a` re-pointed to the new key | `S105`–`S107` `S8` `D41` `D78` |
| 33 | Sharing per project; contacts shareable | `S97` `S98` |
| 34 | **Bulk import** — nobody hand-types the customer base. It is the trigger three `§5` rows are explicitly waiting on, the company picker among them | `SPEC §15` |
| 34b | ~~The verification scripts stop leaving accounts and comments behind~~ **Done**, ahead of `35` and on the founder's call. The count that justified it: **158 users, 149 of them `verify-*` residue**, 129 of the 138 active — nine real accounts, and `S111` forbids deleting any of them | `S111` |

### Phase 4 — Complete the picture

| # | Session | Rules |
|---|---|---|
| 35 | ~~The board view for projects~~ **Done**, out of plan order — `D28` makes the board the projects default, so it came with the projects screens rather than after them | `D28` `D29` |
| 36 | The monthly rollup | `D42`–`D44` |
| 37 | Password reset; ~~the **non-working-time calendar** — public holidays AND personal leave, `S94` as widened session 50~~ **the calendar shipped in session 55** (row 60: `non_working_days`, `/calendar`, `src/lib/calendar.ts`, the pace and the two working-day cut-offs reading it); the threshold **edit screen** (the values are already settings rows, SPEC §16); the three flags made live per `S8`'s session-50 holders — export super admin alone. **What remains here: the password reset `S11`, the settings screen, and `can_export`'s reader** | `S8` `S11` `S94` |
| 39 | ~~RTL pass — every screen, both locales~~ **Dissolved.** `CLAUDE.md` makes both locales a per-slice gate and `verify:routes` drives both on every run, so a session for it would describe work that has already happened by the time it starts. The `dir="auto"` sweep it was really about is session `47`. **The number is retired and never reused** — `D18`'s precedent — so no citation shifts | `D57` |

> **AUDIT 3 — pre-pilot.** See §6.

### Phase 5 — Pilot

**Where this sits after session 55 — the founder's plan, in order.** (1) He
uses the **empty product by hand** — `npm run db:clear` left the database
with roles, lookups, settings and his own account and nothing else, and the
first-run screen (`D81`) is what he meets — creating every record himself, as
a customer would on day one. (2) A **customer audit**: the product read as a
customer would meet it. (3) An **overall AI audit** (`§6`, AUDIT 3). (4) The
**move to the office PC** (`README` § Deployment, row 43's second machine).
Until (1) is over, the demo seed stays cleared and **most of the verify suite
is red by design** — `README` § *Day one* says which, and `npm run seed:demo`
brings it back.

Two or three reps. One month. Real companies, real quotations, real dispatches.
Everything after this is decided by what they say, not by this plan.

---

## 5. Known defects and deferred work

Found during a session, correctly not fixed there. A defect that exists only in
a session report does not exist.

**Closed rows live in `docs/archive/29-closed-register.md`** — moved in
session 50: a closed row is history, and history changes address so a
session consulting the register pays for the OPEN rows only. Nothing was
deleted; every citation still resolves at the new address.


| What | Where | Disposition |
|---|---|---|
| **The Team tab counts rows for a person that his own list cannot carry.** `D39`'s *waiting on them* attributes every row to every live holder of its company (`team-table.tsx`, `row.owners`), while a person's own list follows what he may see — and `S30` keeps a project to its owner and whoever it is shared with, never to the company's holders. Measured session 55 in process: the manager attributes **67** rows to rep-a against rep-a's own **65** — four extra, three `project_stage_unchanged` and one `date_due`, all projects owned by another rep under مصنع تبوك الحديثة للألمنيوم, a company rep-a holds since `§46`'s *who continues* merge moved the folded record's projects under the survivor (`S22`), which is exactly the production case a merge creates. `verify:routes` §35's fold check had been green only because the seed had no such project; it now prints NOT MEASURED while one exists, naming the count. **Whose row is a project under my company but owned by somebody else** — the holder's, the owner's, both, on `S88`'s grouped list — is a rule question, not a fix | `src/app/[locale]/(app)/_components/team-table.tsx`, `src/lib/follow-ups.ts` (`repsByCompany`), `SPEC.md` `S88` `S30` `S22`, `scripts/verify-routes.ts` §35 | Open — a founder decision: attribute by company (today), by owner, or both and say so on the row |
| **A `notFound()` thrown from a page is answered with Next's error shell, and the 404 is blank with scripts off.** Measured session 55 against the built server with a signed-in manager: `/en/companies/<zero uuid>` answers 404 with `<html id="__next_error__">` and **237 bytes of markup before the first `<script`** — `(app)/not-found.tsx` arrives only in the flight payload and is drawn by the client. Against `D20` in its own words. It is NOT the route tree: the same session moved the `<html>` shell to `src/app/layout.tsx`, after which an UNMATCHED URL renders `src/app/not-found.tsx` server-side in the reader's language and direction (§6 proves it against the body's markup), while a thrown `notFound()` still gets the shell — so the mechanism is Next 16's rendering of a not-found boundary during a dynamic render, and the fix is a Next question, not a FACET one. Since session 23; nobody saw it because a browser draws the payload and §6 read a class string out of it (the eighteenth wrong-red) | `src/app/[locale]/(app)/not-found.tsx`, `src/app/layout.tsx`, `scripts/verify-routes.ts` §6, `DESIGN.md` `D53` `D20` | Open — **needs a Next answer first**: try `unstable_rethrow`-free shapes (a `not-found.tsx` rendered by returning it from the page with the status set another way is not available in App Router), or a Next release note. §6's check says which half it reads until then |
| **Two of the Reports tab's readings have only ever read one value on fixture data, so their checks' other half is unexercised.** `S139`'s *coming back* reads **0 of 30** on `seed:demo` — no seeded company has an approved dispatch on two projects (`select company_id from dispatches where status='approved' and project_id is not null group by 1 having count(distinct project_id) > 1` → 0 rows, 2 Sep 2026) — so the returned figure has never been non-zero on a screen, and `§42`'s equality holds at zero on both sides. And *quoted — and nothing came back* reads **15 of 15**: every live issued quotation in the seed is unanswered, so silent equals live and the check cannot tell the two apart until an interaction is logged after an issue. Neither is a wrong number — both are what the records hold, and the labels print the figures — but a check that has only ever seen one value is the `D39` due-soon shape again | `scripts/verify-routes.ts` §42, `scripts/seed/demo/` | Open — **whoever next opens the demo fixtures** gives one company a second project with an approved dispatch and logs one interaction after an issue, then re-reads §42's two labels. Not to be done in passing: the dispatch changes every achievement figure `§35`/`§36` read |
| ~~**Archived companies stay inside the silence figures.**~~ `companySilence` ran over every company with no `archived_at`/`merged_into_id` term while `archiveCompany` leaves the `company_reps` rows live — so `quietCountsByRep`, `D39`'s quiet column, would have counted a customer somebody archived as *quiet*. Latent (0 archived in the seed, re-measured 2 Sep 2026: still 0) | `src/lib/coverage.ts` (`companySilence`) | **Closed — session 53.** The term went into the one derivation rather than into every reader: `isQuiet` is false for an archived company or a merge tombstone, so `/companies`' quiet group and order, the rep's queue, the company's own turn panel (which already forced it) and `D39`'s column now agree; `silentDays` still runs so the meter on the archived company's own page keeps its figure. `verify:routes` §43 drives it — archives one of rep-a's quiet companies by SQL, reads the column drop by one while never-contacted holds, and puts it back as a measured check |
| **Two checks that were unexercised on the fresh seed, one closed and one recorded.** (1) `§36`'s *the company target is not the reps' targets summed* read the team rows off the Today body from session 51 — when the table moved behind `?tab=team` — and printed NOT MEASURED on every run since; nobody noticed because NOT MEASURED is honest and the summary line only counts `ok`. **Closed session 53**: it reads the Team tab and measured 6,000 against 800+3,200+1,000 for the first time. (2) `§41`'s six attention checks — the silent and never-contacted tallies and rows — render only when the block does, and with `D32`'s opening week suppressing the behind rows, a **fresh** seed's block is absent through the first working week of a month (the seed has nobody silent and nobody at the never floor); the checks then print an honest NOT MEASURED. The suite's own residue makes the block render on a second run, which is how session 53's run 2 exercised them. Not a wrong green; an honest gap on one week in five | `scripts/verify-routes.ts` §36 §41 | (1) closed; (2) recorded — a fixture that seeds one silent rep would close it, and that is the demo seed's session, not a check's |
| **`D22` says 14px between cards and the product has always rendered 24.** Measured session 51 while accounting the Today tab's height against the approved drawings: **44 sites ship `flex-col gap-6` (24px) across every `(app)` screen, zero ship `gap-3.5` (14px)** — `grep -rn "flex-col gap-6" src/app --include="*.tsx"`. Three audits and every founder review have read the 24px rhythm without one report against it, which is `D9`'s and `D17`'s shape exactly: the rule describes the concept drawing, the product shipped the quieter decision, and nobody missed it. Also part of why the built Today measures ~1,055px against the drawing's 846 — the drawing was drawn at 14 | `DESIGN.md` `D22`, 44 call sites | Open — **`§6b`'s next rule review**: rewrite `D22` to the measured truth (24px, the shipped rhythm) or decide the product moves to 14; not a value to change in passing |
| **A field that renders conditionally on client state is invisible to `verify:routes` §23, and no better assertion fixes that.** §23 reads the HTML and names each field an action requires that has no native focusable control carrying its name. A field gated on `useState` **is not in the markup at all**, so there is nothing to fail on — its absence is indistinguishable from a form that simply never had that field, which is exactly what the markup says. **Found in `signal-fields.tsx`, and it had been shipping on every device:** the four `signalReference.*` inputs rendered on `takesReference && on`, with `on` in client state, so with scripts off a rep could not record a competitor's name at all — `S43`'s reference, which is the only place the business learns *who* undercut it. §23 read **5,179 forms over 909 fetches** and printed one note, on a different form. This is enablement, which `D20` forbids in those words. **It is the fourth check in this project that passed for the wrong reason and the first where the gap is structural.** The other three are wrong assertions and were fixable as such — §17 asserted the *inverse* of `D20` on its own markers (session 28), §24's pile sum is page-dependent and goes red on residue, and §17 eats its own preconditions at six runs per seed. This one is a **correct** assertion that cannot see the case, so tightening it changes nothing: the scan's input is the HTML, and the defect is that the field never reaches it. **What can be named is the shape** — a `useState` gating a *field* rather than a *presentation* — and the codebase already holds the cure in two places: `report-form.tsx` renders `onHoldUntil` always (*"the CHECK and the `RuleError` are the gate, and the UI never is"*), and §17 converted four such forms to native `<details>` in session 28 for the identical reason. A third is now added: `peer-checked` reveals, which is CSS and keeps the field in the DOM | `scripts/verify-routes.ts` §23, `src/app/[locale]/(app)/reports/signal-fields.tsx`, `DESIGN.md` `D20` `D46` | **The instance is closed in `38c`**; the shape is open. §23 cannot catch it over HTTP — it would need to read the SOURCE, not the response — so the candidate is a **lint rule**: no `useState`-derived condition may wrap a control carrying a `name=`. That is a dependency and a new class of check, and neither is this slice's to add. `OPEN — not chosen` |
| **The count route cannot see the two transitions `/quotations` is ordered by.** `D72`'s count on that list is `greatest(quotation_threads.created_at, quotation_versions.created_at)` — a thread raised, or a version revised `S66` — and it is blind to a version **issued** and a thread **accepted**, which are the two moves that actually reorder `D25`'s piles. So the screen most about waiting cannot say when the two events that matter most on it arrived. **This is `quotation_versions` having no `issued_at`, not a defect of the count route**: the table carries `created_at` and `status` and nothing else, issuing and accepting are status writes, and `S112`'s audit row is the only record of the moment — `timeline.ts` already reads it for the stream's `quotation_issued` event. Reading the audit log from the count route would be a **second definition of *new***, which `D72` forbids in those words, so the honest fix is a column and **its writer in the same slice** `CLAUDE.md`. **Second clause, same cause:** the count does not exclude the reader's own work, so a rep who raises a thread on a tab he left open reads *1 new* about himself — `D72` says a screen showing only your own typing polls nothing, a shared list is not that, and filtering on `raised_by_user_id` is a predicate no rule asks for | `src/lib/quotations.ts` (`threadArrivedAt`), `src/db/schema.ts` `quotation_versions`, `DESIGN.md` `D72` `D25`, `SPEC.md` `S112` | Open — needs a column with a writer, which means the issuing screen's session, not a list slice |
| **The board scrolls its tall columns instead of capping them, and that is what keeps the projects table ungrouped.** `D70`'s *cap and state the total* was the other answer and was declined in the board slice as the cheap option — take it now, see whether it bothers anyone. **The cost is not on the board, it is on the table.** A cap's way out has to land somewhere scoped (`D59`), which means `/projects?view=table&chain=waitingPayment`, which means **the chain ladder written a second time in SQL** so the filter and the per-column count resolve before pagination (`CLAUDE.md`). That same SQL is the only thing that would let the table be **grouped and ordered by chain position**, which `D24` (*grouped, never flat*) and `D25` (*projects group as your move / moving*) both ask for and it still does not do — the table is ordered by attention now `D25`, and grouped by nothing. So the two are one decision, not two: whoever caps a column writes the ladder, and whoever writes the ladder gets the grouping for nothing. The second ladder would need `verify:schema25` holding it to `chainState()` row for row, because a ladder that disagrees with `chain.ts` is the `21 §7` trap at a new address | `src/lib/projects.ts` (`chainByProject`, `listProjectBoard`), `projects/project-board.tsx`, `DESIGN.md` `D29` `D24` `D25` `D70` | Open — deliberately deferred, with the trade recorded rather than rediscovered |
| **`D26`'s project lead cell is not built, and half of it cannot be.** *"a **six-dot mini-chain** showing chain position, plus a quoted-vs-dispatched bar"*. The first half is buildable — `ChainStrip` draws six nodes from `chain.ts`, and since session 28 the project detail draws it from a **project-level** position `S132`, so a `mini` variant for the list is the whole remaining work. **The second half is refused by `S68` — *quotations are never summed*, in those words.** *Quoted* for a project means summing its threads' latest live versions, and a project with more than one live thread is the same square metres counted twice, so there is no numerator for the bar to have. **The citation moved in session 28.** This row used to point at `projects/[id]/page.tsx`, which summed them once *"only to show that summing them is meaningless"* — that code is **deleted**: the rule's plain text and the screen contradicting it could not both stand, and `§6b` was written the same week to catch exactly that. The conclusion is unchanged and never rested on the code; it rests on `S68`. `sqm_expected`-versus-dispatched IS producible per project `S29` `S26`, and no rule names that pair, so it is not built either. The board slice shipped a `Turn` cell instead — whose move plus the position, the idiom `/quotations` uses — which answers `D2`, and left `D26` unbuilt rather than shipping half a lead cell and calling the rule done. `DESIGN.md` has 26 rules describing things that do not exist; this is a deliberate 27th rather than a silent one | `SPEC.md` `S68`, `DESIGN.md` `D26`, `src/app/[locale]/(app)/projects/page.tsx`, `_components/chain-strip.tsx` | **PROPOSAL WRITTEN — the rewrite drops the bar on `S68`'s refusal and states the shipped lead cell; see the `§6b` block below, awaiting the founder** |
| **`D25` names three groups for the companies list and only two can be built.** *Gone quiet* and *recently touched* are the thresholds `07 D5` gives; **no rule anywhere says where *due soon* starts**, and `D6`'s amber band therefore goes unused on that screen — red past the threshold, faint otherwise. Three readings were measured against rep-a's 125 companies before the group was dropped rather than guessed at: a **7-day** window before the threshold holds **4**; a **14-day** window holds **6**; the rep's own `next_follow_up_at` — *"you said you'd do this"* rather than *"the system got worried"*, and the more interesting reading — holds **3**, which says the behaviour does not exist yet. A window nobody chose becomes the number everyone believes in six months later, so it is `OPEN — not chosen` | `DESIGN.md D25`, `src/lib/companies.ts` (`COMPANY_ATTENTION_GROUPS`), `_components/silence-meter.tsx` | Open — needs a `SPEC.md` or `DESIGN.md` line, not a value picked in code |
| **`D39`'s due-soon count is asserted and has never been exercised on real fixture data.** The team table's second badge is `ageDays === 0` — a follow-up date the rep set for **today**, which is the only way a row on that list reaches zero days `D34` — and on `seed:demo` **rep-a reads `65+0`**: every row waiting on him has already slipped. So the column renders, `verify:routes` §35 reads its `data-due-soon`, and **the only run in which the figure was ever non-zero was an injection**, where the deliberately over-counting fold showed `191+9` and proved the number can be produced company-wide at all. **Nothing is broken and the assertion is not weak** — §35's claim is that the pair folds inside the rep's own scope, which `65+0 <= 67` tests honestly. What is missing is *evidence from data* that the red/plain split is drawn on the correct side, because the plain badge has only ever shown zero for the identity §35 measures. **Not to be fixed by editing the seed on the way past.** Giving rep-a a date due today changes what every other follow-up assertion reads — `§31`'s chip counts, `§33`'s pager scope and `D33`'s four tiles all run off the same derivation — and a fixture edited to make one badge non-zero is the shape that quietly moves four other numbers. It belongs to whoever next opens the follow-up fixtures deliberately. **And the same table's row count is now three, not four** (`25b`): `S136` took the manager's personal target away, and `D39` shows only people who carry one. `§35` reports **NOT MEASURED** below two rows — *a one-row table proves nothing about a table* — so the margin is one. A fixture edit that removes another rep's target stops `§35` reporting at all, and it will say so rather than go green. Same table, same fixture, same owner as the due-soon column above. | `DESIGN.md` `D39` `D34`, `scripts/verify-routes.ts` §35, `scripts/seed/demo/activity.ts` | Open — **whoever next touches the follow-up fixtures closes it**, by giving one rep a `next_follow_up_at` of the seed's own today and re-reading the four sections above. A column asserted but never exercised on real data is a gap worth naming even though nothing is wrong |
| **Two sections can print the IDENTICAL label, and a label diff then cannot attribute a departure.** `§35` and `§36` both printed `<locale>: the manager's dashboard answers 200`, and it surfaced while diffing labels for `25b`'s second injection — **the exact activity the collision breaks**, since `CLAUDE.md` now requires a delta to be accounted by diffing labels rather than subtracting. Nothing is asserted wrongly by it: both checks are correct and both run. What is lost is attribution, which is what makes a `NOT MEASURED` delta readable. `§36`'s was disambiguated in `25b` | `scripts/verify-routes.ts` `§20` `§22` `§24` `§31` `§33`, `CLAUDE.md` § Verification | **Audited rather than opened, and the answer is three more pairs.** Read statically rather than off a run, which catches a collision the fixture happens to hide by putting different numbers in two labels. **513 call sites, 507 with a literal label, 501 distinct templates — and the six unread are accounted, not assumed**: `check`'s own definition, three mentions of it in prose, `§36`'s comment-prefixed setup, and one ternary in `walkRecords` whose two branches embed a unique `${path}` and appear once each. **Five templates are shared across sections**, in three pairs. **Two collide byte-for-byte on every run.** `§22` and `§24` both print `ar: *** the DOM order is identical to English — the mirror is CSS *** [D57]`, a plain literal with **no interpolation at all** (`:5779`, `:6271`); `§31` and `§33` both print the setup `<locale>: /follow-ups answers 200` (`:7879`, `:8309`), four indistinguishable lines a run. **The third pair collides in SHAPE and is separated only by fixture numbers**: `§20` and `§24` share three `[D24]` pagination templates — *the walk read every row of the scope*, *a header outruns its page*, *the card's own total does not move between pages* — distinguishable today only because `/companies`' scope reads 67 and `/dispatches`' 51, and identical the moment those two totals agree, which nothing prevents. Open, deliberately: this is legibility in the harness rather than a wrong assertion, so **whoever next edits one of those five sections prefixes its labels with its own subject**, the way `§36`'s now names the company panel. A sweep touching five sections to fix a defect nobody has been misled by yet is the shape `CLAUDE.md`'s simplicity rules refuse |
| **The rep half of `/dispatches`' search is a control that lies about what it does.** The box says *Search by company or rep*. Measured against the seeded fixture: `Al Harbi` finds **0**, `Al-Harbi` finds **25**, `Faisal Al Harbi` finds **0**. **7 of 9 active users carry a hyphen**, so the surname typed the way a person types it finds nothing for any of them — only the exact stored form, or a bare fragment like `Harbi`, works. It is not that the half is broken; it is that it only keeps its promise if you happen to type the punctuation. The company half folded in session 41 through `name_normalized` and this one could not: `users` has no such column, `CLAUDE.md` forbids landing one without its writer — the user form, not a list slice — and no rule asks for it. **The label may have to shrink before the column is built**: *Search by company* is honest today and is one string, where the column is a migration, a backfill and a writer. `OPEN — not chosen` | `src/lib/dispatches.ts` (`searchFilter`), `messages/{en,ar}.json` `dispatches.searchPlaceholder`, `src/db/schema.ts` `users` | Open — a founder decision between shrinking the promise and building the column. Whoever touches the user form is the cheapest moment for the second |
| **How often a dispatch differs from its quotation cannot be measured from this database, and two sessions have now quoted a number as though it could.** `D66` rests on *roughly half of all dispatches already differ*, which no document sources. Session 41 set out to confirm it and produced **61 of 87 linked, 70%** — then re-measured after `seed:demo` and got **14 of 34, 41%**. Neither is evidence. The 70% was a fixture contaminated with verify residue, and `§5`'s own row on `dispatch-fixture.ts` already records that those rows write the flag directly. The 41% is worse: `scripts/seed/demo/plan.ts` carries **11 `editedByRep` and 3 `editedByCoord`** entries, and 14 dispatches differ — **the rate is a number somebody typed into the fixture**, reproduced by counting it back out. A synthetic dataset cannot answer a question about how a company behaves. **Nothing in session 41 depended on the figure**: the badge came off the list on `D66`'s own reasoning, which is a rule and stands without it. What is open is the claim underneath `D66`, and the only thing that can settle it is real dispatches at the pilot | `DESIGN.md` `D66`, `scripts/seed/demo/plan.ts`, `scripts/dispatch-fixture.ts` | Open — **measure it at the pilot, and until then quote no rate.** It is also the evidence anyone would want before *forcing a revision when a dispatch differs*, which no rule says and nothing builds |
| **`D52` covers an empty list and nothing covers an empty fact.** `/dispatches/[id]` rendered *How the customer is paying — —* on a request not yet approved, which is correct per `S73` — no route to `approved` bypasses a payment method, so an unapproved request has none for exactly as long as it is unapproved — but a dash reads as *missing*, which is a different claim from *not yet asked*. Session 41 replaced it with a sentence, `Recorded when it is approved`, on `D52`'s instinct: *an empty list says what would make it non-empty*. **That is an extension of `D52`, not a citation of it** — `D52` says list, and a Facts grid is not one. Every detail screen has fact cells that are absent by rule rather than unfilled, so this recurs | `DESIGN.md` `D52`, `src/app/[locale]/(app)/dispatches/[id]/page.tsx`, `src/components/page-header.tsx` (`Fact`) | **PROPOSAL WRITTEN** — `D52` widens; see the `§6b` block below, awaiting the founder. The `AD21` marker is session 47's |
| **`D26` has now promised something the data cannot give three times, and that is one rule rather than three defects.** The **project** clause's *quoted-versus-dispatched bar* is refused by `S68` — quotations are never summed — and was reported by the company-detail slice. The **quotation** clause's two worked examples both failed in session 26: *"Rawan · signatures"* names a coordinator **the row cannot name** (a thread carries `raised_by_user_id` and no issuer or acceptor, nothing assigns a thread to one coordinator, and `can_approve_quotation` is a flag more than one user could hold), and *"You · confirm payment"* names an act `S133` deleted, whose message key is already gone. The clause is producible in **shape** — an avatar and whose move it is — and in **neither example**. Each was handled in its own slice and none of them asked whether `D26` itself was written against a data model that does not exist. `§6b` names the trigger outright: **a rule argued past twice gets reviewed, and the second argument is the trigger.** `D26` reached three | `DESIGN.md` `D26`, `WORKFLOW §6b` | **PROPOSAL WRITTEN — `D26` rewritten to what a row can know; see the `§6b` block below, awaiting the founder.** Not a code row |
| ~28 sites need `dir="auto"` per D62. **The count is the only record of them** — it came from a session report, and `§4` defines no session `3a` for anyone to go back to, so session 47 re-takes the inventory rather than working from this number. **One site was found scrambled in the wild and fixed ahead of the sweep — `98f1e2e`.** `/ar/dispatches/[id]`, the dispatch-date cell, rendered `292026/08/` for `2026/08/29`. **The fix was to REMOVE `dir="ltr"`, not to add it**, and that inverts the assumption the sweep would otherwise carry: `Intl` formats an `ar` date as `29<U+200F>/08<U+200F>/2026`, embedding RIGHT-TO-LEFT MARKs that place the segments correctly inside an RTL run, and `dir="ltr"` fights them — the digits resolve to three embedding levels and the day jumps to the front. The header two elements up and the submitted banner both render the same formatter correctly **carrying no `dir` at all**, which is what the cell now matches. **`D62` names *dates* among the things the `dir="ltr"` rule covers, and on this evidence that grouping is wrong**: a reference (`Q-9280`) and a decimal are raw LTR content and want the attribute; a locale-formatted date is not raw and does not. Said once and not acted on — no rule was amended. **Nine more sites carry the identical shape and are NOT fixed here**, deliberately, because this slice was one attribute: `companies/[id]/page.tsx:~613`, `follow-ups/page.tsx:~202` (on the `TableCell`), `notifications/page.tsx:~368`, `(app)/page.tsx:~173`, `reports/[id]/page.tsx:78` and `:125`, `users/page.tsx:~157`, `users/[id]/page.tsx:~119`, `_components/stream.tsx:~295`. `/dispatches` itself is clean — it renders relative days, not dates. **`verify:routes` §24 now asserts the absence** on `data-fact="dispatchDate"` in both locales, guarded on a non-empty read and printing what it saw; it was fed the defect and went red in both before being believed. No script can see the scrambling itself — bidi reordering happens at render and the suite executes none — so the markup is what is asserted. **A THIRD SIGHTING, and it is the ALIGNMENT half rather than the attribute half.** `/ar/notifications` at 1366, dark, as a rep, observed by eye in Brave after `05b15e7`: each notification row's **record name** — the Latin-script value under the sentence, e.g. `verify-routes ar colour unavailable`, `ar-3228237 the customer went elsewhere` — is thrown to the far inline-start edge of the card, several hundred pixels from the sentence it belongs to, leaving the middle of every row empty. `/en/notifications` puts the same value directly under its sentence. **Not introduced by session 24**: the element predates the narrowing and the narrowing did not touch it. The element is `notifications/page.tsx:261-263` — `<p className="text-start … whitespace-pre-wrap" dir="auto" data-decision-reason>` carrying `S128`'s reason — **named as the candidate and not confirmed here**, because the sighting is by eye and nothing was run against it. **A candidate mechanism, for session 47 to confirm or discard rather than inherit:** `dir="auto"` resolves a Latin paragraph to LTR, and `text-start` compiles against the element's **own** direction, so it becomes `text-align: left` inside an RTL card. That is `CLAUDE.md`'s recorded trap — *a logical utility is not protection when something nearby sets a direction* — reaching `text-align` where its three recorded sightings were all `ms-auto`. If that is the cause, the fix is the same shape: move it onto a neighbour carrying no `dir`, or use a `gap`, which has no side to get wrong. Never onto the element itself. **So this row now carries three concrete sightings rather than an abstract count**, which is what makes it workable — session 47 reasons from cases rather than from a number a session report left behind. **One line reference above is already stale, which is the argument for re-taking by grep:** `notifications/page.tsx:~368` was written when that file was 385 lines; after `05b15e7` it is 338 and the date span is at `:321`. The site survived the narrowing untouched — only its line moved. | list cells, option labels, panels; the nine date sites above; plus `notifications/page.tsx:261-263` (the alignment sighting) | **DONE — session 47, the whole remainder.** The inventory was re-taken by an eight-reader pass over the tree, not from the ~28 count, and the genuine gaps were narrower than the number: most list name cells already carried `dir="auto"` — what lacked it was `/users`' name, `/follow-ups`' COMPANY and REPS columns, `/notifications`' record label, the rail footer's two truncating spans, and **every name-bearing `<option>`** (15 sites, 11 files). All carry it now, each on the RUN. **The alignment sighting's mechanism confirmed and fixed**: `dir="auto"` + `text-start` on a BLOCK resolves alignment from the value — the reason `<p>` and its family (both dispatch reasons, the report narrative, the comment excerpt, the dispatch-form project labels) now keep the page's direction with the value in a `<bdi>`; re-shot `rep-a/notifications-ar-dark-1366.png` and READ: every record value sits with its sentence. **One legitimate block carrier found and written into the `D62` proposal**: a TRUNCATING single-value block (`A2-20`'s rail name) wants `dir="auto"` on itself so the ellipsis lands on the value's own end. `/users`' email came off the CELL onto the run (the column sat misaligned in Arabic). **No sweep touched a locale-formatted date** — the trap held |
| `follow-ups.ts` filters in memory after the fetch. **Corrected by AUDIT 1: it is the whole function, not one line.** `followUps()` fetches every row with no SQL limit, then applies four in-memory filters, a sort and `.slice()` for pagination. **Re-homed and re-stated in session 24, which found the file very much alive** — it is the dashboard's engine, `D33`'s tiles and `D34`'s two sections both read it through `followUpScope()`. **It is also less severe than the disposition implied, and that matters for whoever fixes it:** this is NOT `CLAUDE.md`'s silently-empty-screen failure. `gather` returns only rows already past their threshold in SQL, `followUps()` narrows the WHOLE scope before paging, and `total` and `counts` are computed over the scope — so the screen is correct and the cost is unbounded memory on a queue that is small by construction. The three sources are merged in TypeScript because each composes its own visibility filter, which a union would have to flatten — the same shape `timeline.ts` uses for six | `src/lib/follow-ups.ts` `followUps` | **Open, no owner.** It is a scale question, not a correctness one, and nothing measures the queue's size yet. Revisit when a rep's scope is large enough to time |
| `listQuotationFormOptions` returns every visible company into one `<select>`. Fine at ~90 | `src/lib/quotations.ts` | Session **34**, bulk import — revisit when the number changes |
| `listDispatchableThreads` is unpaginated, and since `S116` each offered thread also carries its issued version's lines so the form can pre-fill them. About 320 bytes per line — a twenty-line quotation is ~6 KB, fifty threads averaging three lines ~48 KB on `/dispatches/new`. **`S72` widened it**: the payment filter moved to the approval, so the list is now every issued thread the viewer can see rather than every paid one, and every rep loads it rather than one coordinator. The cost is the missing pagination, not the lines | `src/lib/dispatches.ts` | With the row above — same question, same answer |
| ~~**The pace tick counts no public holiday**~~ `D32`. `working-days.ts` skips Friday and Saturday only and records a holiday calendar as `OPEN — not chosen` `[21 §8]`, so through Eid the tick advances while nothing ships and every rep reads *behind pace* for the remainder of that month. **Correct as written** — `D32` inherits that `OPEN` rather than answering it a second time, and a calendar-day denominator would be equally blind, so this is not a cost of counting working days. It will read as a bug the first Ramadan after go-live | `src/lib/working-days.ts`, `D32` | **Closed — session 55**, by `S94`'s calendar (`WORKFLOW §4` row 37's calendar half): `monthWorked` moved into `working-days.ts` and takes the off-day set `src/lib/calendar.ts` builds — public holidays off everyone's month, a person's leave off theirs — and every tick reads it: the rep's panel, the company band, `D39`'s rows, `D79`'s behind-pace. `verify:phase10a` §4 holds the arithmetic and §17 the reader; `verify:routes` §47 moves a real tick with a real leave and puts it back |
| `awaitingSignatureCount` is unpaginated: it reads every visible thread's live version — three small columns — and counts the positions `chainState()` returns. Deliberate, and the alternative is worse: restating the chain ladder in SQL is the second derivation `src/lib/chain.ts` exists to prevent. Grows linearly with the reader's book — fine at one rep's ~60 companies, and a `sees_all_reps` identity reads the whole company | `src/lib/quotations.ts` | With the two rows above — same question, same answer |
| `companies.merged_into_id` has readers (isNull filters) but no writer. **Corrected by AUDIT 1: four modules, not five** — `coverage.ts`, `dispatches.ts`, `dormancy.ts`, `follow-ups.ts`. 0 of 809 companies carry one. **Classified by the `0027` sweep and KEPT**: `S21`–`S23` build duplicate resolution, and four modules already read it, so it is the least dead thing the sweep looked at. `companies_merged_into_idx` stays with it; both are asserted present in `verify:schema25` §2 | `src/db/schema.ts` | **Closed — session 54, phase 4.** `mergeCompany` in `src/lib/duplicates.ts` is the writer: *who continues* and *shared* both stamp the folded record, and `verify:routes` §46 reads the tombstone back from the records and from its own page |
| `report_signal` has nine values. **Corrected by AUDIT 1: `S43` lists eleven, not ten, and two are missing, not one** — *stock shortage* and *customer went quiet*. `loss_reasons` also seeds nine and is missing a different two — *quality concern* and *payment terms* — so the two lists are complementary halves whose union is exactly `S43`'s eleven. Seven shared concepts carry different tokens (`competitor_cheaper`/`lost_to_competitor`, `lead_time_too_long`/`delivery_time_too_long`, `specification_unavailable`/`specification_not_offered`, `project_delayed`/`project_cancelled_or_postponed`, `colour_unavailable`/`colour_or_product_unavailable`), so unifying is not a union. Only two values are in use in 30 signal rows, so the data migration is small | `src/lib/enums.ts`, `src/db/schema.ts`, `scripts/seed/loss-reasons.ts` | Session 29, which unifies the vocabulary |
| **`/contacts/[id]` is a route holding a Details card and an Edit button, and nothing else.** After `27b` took its timeline away it has no content of its own that the company page could not carry: it is reachable only by opening a company, it is not in the rail `D49`, and nothing anchors to a contact — a report anchors to a company `[20 §2]`, a quotation to a thread — so no card can ever be added back to it by a rule that exists today. **Whether the route earns its existence, or a contact becomes a row edited inside the company page, is undecided.** Recorded rather than acted on: deleting a route is a navigation decision `D49` `D24` and this slice was a narrowing. Note what is NOT the argument — the screen is not broken, it renders correctly in both locales and `verify:routes` walks it as three identities | `src/app/[locale]/(app)/contacts/[id]/page.tsx`, `DESIGN.md` `D49` `D24` | **Open — a founder decision, with no session yet.** `OPEN — not chosen` |
| **`canViewRecord`'s `dispatch` case has no caller left, and the value stays deliberately.** The case exists because `25 §9` put comments on every record, so a dispatch id arrived from the comment form and had to be checked before a row was written against it — the docblock in `authz.ts` says so in those words, and `27b` took that caller away. Nothing else reaches it: `SHARED_RECORD_TYPES`, `FollowUpAnchorType` and `CommentRecordType` name every dynamic caller of `canViewRecord` and none carries a dispatch. **It is not removed, and that is a decision rather than an oversight**: `ViewableRecordType` is shared with `canOpenRecord`, and `notifications.ts` records in place that a dispatch is out of `ANCHOR_TYPES` *"for that reason and not for want of `canOpenRecord` support"* — so dropping the value would contradict a decision taken elsewhere for a different rule. The stale justification WAS corrected in `27b`, because a comment claiming a caller that no longer exists is the thing `§7` forbids | `src/lib/authz.ts` (`ViewableRecordType`, `canViewRecord`), `src/lib/notifications.ts` `ANCHOR_TYPES` | Open — needs whoever next touches the notification anchors to decide whether the READ half still wants it. Nothing is wrong today: the case is correct, merely unreached |
| **A company parked with a date has nothing that surfaces when the date arrives.** `on_hold_until` only **suppresses**: `gather` drops every row on that company while the date is still ahead (`follow-ups.ts:1175-1178`), and when it passes the company simply stops being suppressed. Nothing announces the wake — it returns only if an automatic threshold independently holds, so a fortnight's park on a company touched last week ends **silently** and the rep is never told it woke up. `D33`'s *on hold resuming this week* tile was the only thing in either authority file that named this, and it left with `D33`'s rewrite because it was never a `FOLLOW_UP_KINDS` kind and no tile could have counted it. Recorded here so the gap does not leave with the tile | `src/lib/follow-ups.ts` `gather`, `DESIGN.md` `D33` | **Open — recorded, and re-homed in session 24 by founder decision.** Session 24 was to own the list it would join; the list turned out to be already built and `follow-ups.ts` alive, so the row now has a live file to land in rather than one about to be deleted. **Both candidate answers still need a rule nobody has written**: a seventh `FOLLOW_UP_KIND` would need a `SPEC` line and a `D33` tile decision (the strip is four tiles over six kinds), and a bell item contradicts `S92` — *news only, never work* — since a woken company IS work. `OPEN — not chosen` |
| **The rail footer's avatar overlaps the user's name — observed once, not reproduced.** Seen in the founder's screenshot at 1366: Windows, **Brave**, dark, English, usable viewport ≈890–910, name *Jerom*, role *Super Admin*, the circle painting over the name. **Not reproducible here.** The rail was rebuilt against the real compiled stylesheet and measured in headless Chrome: the footer holds a clean **10px gap** — avatar fixed at x=12–44, name at x=54 — across viewport heights 305–945, device scale 1 / 1.25 / 1.5, long / single-word / Arabic names, wide page content squeezing the column, and both the production and dev stylesheets. `md:flex` beats `hidden` and `md:overflow-visible` beats `overflow-x-auto` in the emitted CSS, so neither is a display or overflow failure. **Two hypotheses are eliminated outright**: there is no avatar component anywhere in `src/` — the rail inlines a `<div>` carrying a text node, `flex-none`, measured 32×32 every time, so no image, no fallback and no positioned span — and `mt-auto` is not it either, since the overflow it causes needs a viewport under ≈487px and only ever pushes the footer **below** the aside, never sideways. **What is left and cannot be tested here: Brave against Chrome on `backdrop-filter` compositing.** The rail took `glass` in `10d43c8` and the screenshot is after it; headless Chrome composites in software, Brave on that laptop does not. **Confirmed by the founder in the shell slice, and acted on.** It still reproduces in **Brave, dark, at 1366** — the exact configuration headless Chrome measured clean — which settles it as a **rendering difference, not a layout one**, and leaves the hypothesis above as the only one standing: `glass` gives the `<aside>` a `backdrop-filter`, that promotes it to its own composited layer, and a promoted layer can paint its children at subpixel offsets the layout tree never shows. That is precisely a clean measurement against a visibly overlapping paint. **Fix applied: `relative isolate` on the footer**, giving it its own stacking context so it is not painted by the aside's promoted layer. One class, no rule touched, and the reason is written at the line. **`glass` was never a candidate for removal** — `D8`, `D14` and `D21` make the rail one of exactly two surfaces that may carry `--blur` | `src/components/app-rail.tsx:169-184`, `DESIGN.md` `D8` `D14` `D19` `D21` | Open — **and it cannot be closed from here**, because headless Chrome never showed the bug and still does not, so a passing `verify:routes` is not evidence. The founder checks it in Brave. If it holds, this closes. If it does not, the next thing to try is `relative z-10` on the avatar so it cannot be painted over its sibling — and Brave with hardware acceleration off (`brave://settings/system`) confirms the compositing diagnosis either way, which would put it in `AD34`'s class of browser findings rather than the markup's. **A SECOND symptom on the same footer, in Arabic at 1366: the name truncates to `…l Al-Harbi`.** تسجيل الخروج is wider than *Sign out*, so the sign-out control takes more of the row and squeezes the name column into an ellipsis. **Recorded, not fixed** — it is a layout question and the slice that found it was one attribute. **Note what this does to the evidence above:** the headless measurement that came back clean measured *long / single-word / Arabic names*, but the run itself was **English**, so the Arabic sign-out label was never in the row being measured. The clean measurement is therefore not evidence about this symptom either way, which is part of why the row stayed open |
| **The verify suite is not idempotent against the record tables, and the database is now mostly its output.** Users and comments were the two classes that reached a *person* — a live account in every mention picker, a comment stuck on a demo company `S107` — and **slice 34b closed both**. The rest reaches the *numbers*, and nothing closes it: **a full pass writes ≈485 rows across sixteen tables**, before the ~300 audit rows that follow them. Measured 26 Aug 2026 by attributing every row to its script's stamp: **92% of dispatches** (573 of 621), **86% of quotation threads** (389 of 450), **84% of projects** (251 of 299), **100% of dormancy reviews** (48 of 48), 97% of credit splits, 83% of `company_reps`, 81% of comments, 78% of rep reports, 75% of companies, 66% of `project_companies`, 64% of notifications, 29% of the audit log — and 96% of users, which is what 34b was measured from. Worst writers per run: `verify-phase10a` **17 companies and 24 memberships**, from one `makeCompany` helper called in a loop; `verify-followups` **13 companies**; `verify-slice3` **55 dispatches and 182 audit rows**; `verify-phase9` **31 rep reports**. **`verify:schema25` is not read-only despite the name** — it holds no `insert()` call, which is exactly what made it look inert, and creates 3 companies and 2 projects a run through `createCompany` / `createProject`. **The consequence is not the row count.** Every screen judged from here on is being read against data that is mostly not real, so a figure that looks wrong on a dashboard may be residue rather than a defect, and a threshold tuned against it is tuned against nothing | every `scripts/verify-*.ts`. The counts reproduce by grouping each table on its script's stamp — `verify3-`, `verify9-`, `verify10a-`, `verify11-`, `verifyc-`, `verifyfu-`, `verifysh-`, `schema25-`, `routes-`, `s74-` | **Still open after session 48, and re-scoped by it.** What 48 changed: `§17`'s six-run budget is GONE (its fixture scans walk every page now), so the residue effect that remains visible is `§25`'s stream flooding — heavy suite use pushes `dispatched` events over the stream's first pages and four checks say *re-run seed:demo*, which stays the ROUTINE reset. What 48 deliberately did NOT do: the teardown itself — ten scripts each learning to remove what they wrote is a session of its own, not a register slice, and half-doing it (two scripts of ten) would make the residue accounting harder to reason about, not easier. **Records are not people** — nothing renders one as if it could act — so none of this is a disclosure defect, which is why 34b ended the accounts and only counted these. Needs its own session |
| **`S76` says the coordinator "may not edit" a project or a contact, and the code lets her edit her own.** Measured, not inferred: `canViewRecord(coordinator, "project", …)` is **true** for a project she owns and **false** for another rep's, while `visibleProjectsFilter` is widened for her — so she reads every project and writes only hers. That split is coherent and is what `S76` was reaching for, but the rule's sentence does not say it, and until `seed:demo` no coordinator owned a project so nothing could tell the two readings apart. It matters because `S127` needs her to hold companies and `S9` lets one be assigned to her: a coordinator who may create a project on her own company but not edit the one she just created would be the incoherent reading | `SPEC.md` `S76`, `src/lib/authz.ts` | Open — a `SPEC.md` correction. `S76`'s "she may not edit either" needs *"a project or contact she does not own"*, the way `S30` already carries `S76`'s exception in place |
| **`S30`'s cost on the raise picker is real in production and cannot be produced by the seed.** `S50` offers the rep only *projects he can already see*, which is `ownProjectsFilter` — owner or explicit share, never company membership `S30`. So a rep whose company already holds a project belonging to **another** rep is offered nothing, creates a second project, and nobody finds out until the two are noticed. **Measured on `seed:demo`, and the measurement is the problem: zero.** Every company has exactly one live rep, no project is owned by anyone but a rep of its own company, and `preflight.ts` **refuses a fixture that breaks either**, citing `S30`. The case therefore **cannot be produced by the seed at all**, so no verify run will ever catch a regression in it — which is worse than a case that happens rarely. Not *small and accepted*: unmeasurable | `src/lib/quotations.ts` `listQuotationFormOptions`, `src/lib/authz.ts` `ownProjectsFilter`, `scripts/seed/demo/preflight.ts`, `SPEC.md` `S30` `S50` | Open — either a fixture that deliberately violates the preflight rule, or a rule saying the case is unguarded |
| **The cross-language city search is gone, and what was lost is not a nicety.** `[15 §5]` matched both names through `normalizeName` whatever the interface language. With the `Combobox` deleted, **a rep typing *Riyadh* no longer finds الرياض, and a rep typing الرياض no longer finds *Riyadh*.** In a company that records names in two scripts that is a regression, not a rough edge — it is the same defect `0030` closed on project names from the other direction, where `name_normalized` came from the English half alone and an Arabic-named project could never be found by its Arabic name. What survives is native type-ahead **within** the interface language, and the five region groups. No `S` or `D` rule required the cross-language half, which is why it left with the control rather than blocking the slice. If it returns it may only be an enhancement **over** the native select `D20` — the control may not require script | `DESIGN.md` `D20`, `src/components/city-field.tsx`, `[15 §5]` | **Open, and it has no owner.** It was session **26**, *or whichever form slice comes first*; 26 shipped, and so did `38c`, which is the log form. Neither took it and neither was wrong to — no `S` or `D` rule requires the cross-language half, which is why it left with the control. It needs a rule or a founder decision before it has a session |
| ~~**`/quotations/new`'s contact select is disabled until a company is chosen, and with scripts off it never becomes enabled.**~~ `quotation-form.tsx:216` is `disabled={!companyId}` against client state, so a rep working without JavaScript can attach no contact to a quotation — ever. Non-required, so it degrades rather than blocks, which is why it is a note rather than a failure. **Found by the new §23 rather than by reading**, and it is the ONLY finding on the whole walk: 5,179 forms over 909 fetches, one non-required field not operable. The fix is a server-rendered narrowing or no narrowing at all — the same question `[16 §6]` answers for the project picker beside it | `src/app/[locale]/(app)/quotations/quotation-form.tsx:216`, `scripts/verify-routes.ts` §23 | **Closed — session 55.** `disabled` came off. With a company chosen the list narrows to its contacts as before; with none — which is what a scripts-off page loads with — the whole directory is offered grouped by company in native `<optgroup>`s, and `assertContactOnCompany` refuses a contact that is not the company's `S20`. §23 should now print no note for the walk |
| **`S79` puts shared credit on the quotation; the schema puts it on the project.** `project_credit_splits.project_id` is NOT NULL (`schema.ts:1130`) and `creditForDispatches` keys off `dispatch.projectId` (`credit-splits.ts:401-418`), so an `S50` quotation with no project and an `S75` free entry can carry **no split at all** — not an empty one, none. `S129` treats this as a timing gap, to be closed when the coordinator confirms at approval; it is an **anchor** gap, and moving the anchor is a migration, not a form. Found as `E1` | `src/db/schema.ts:1130`, `src/lib/credit-splits.ts:401-418`, `SPEC.md` `S79` `S129` | Open — with the `S79`/`S80` session, which opens both credit pickers anyway |
| **A participant that has bought can be removed, and its square metres vanish.** `removeProjectCompany` (`projects.ts:1313-1341`) checks only that one link is left `S27` — never whether that company is the one a dispatch went to. The dispatch keeps its `project_id`, so `projectIsWon()` still reads won and the rep stays credited, while `S26`'s per-participant figure loses the buyer entirely. The remove control sits on the very row that renders `data-dispatched` (`project-links.tsx:73, 143`), so the screen already knows. Found as `C3` | `src/lib/projects.ts:1313-1341`, `projects/[id]/project-links.tsx:73,143`, `SPEC.md` `S26` `S27` | Open — one guard in `removeProjectCompany`, but which rule refuses it is not written down |
| **Archiving a company silently removes its open quotation from every queue.** *(Still open after session 54: the rep-requested path lands on the same `archiveCompany`, so a request granted on a company with an open thread takes this defect with it; the founder's ruling did not touch it.)* `archiveCompany` (`dormancy.ts:308-338`) checks nothing about open threads; `visibleQuotationThreadsFilter` has no company term, so the thread stays on `/quotations`, while `quotationNoResponse` filters `isNull(companies.archivedAt)` (`follow-ups.ts:304`) and drops it from the chase. The quotation is then open on one screen, waiting on nobody, and closed by nothing — **`S86` says that state is illegal.** Found as `A3` | `src/lib/dormancy.ts:308-338`, `src/lib/follow-ups.ts:304`, `SPEC.md` `S86` | Open — either archiving refuses while a thread is open, or it closes them the way `S47` closes a lost project's |
| **`S27` has no database constraint and no whole-table assertion**, unlike its structural twin `S18`, which `verify:schema25 §20` counts over every row. **11 violating rows measured** (`WORKFLOW §10`) — projects with no live participant — and every verify script inserts into `projects` directly, which is how they got there. The one guard that exists also **races**: `removeProjectCompany` reads the count and then updates, with no lock, so two concurrent removes both see two. Found as `C1`/`C2` | `src/db/schema.ts`, `src/lib/projects.ts` `removeProjectCompany`, `scripts/verify-schema25.ts` | Open — `S18`'s treatment applied to `S27`: a whole-table assertion first, then decide whether the invariant is a CHECK or a lock |
| **All three of `S77`'s figures are gated on one nullable column.** `quotation_versions.total_sqm` (`dispatches/[id]/page.tsx:496`) decides whether the quoted-versus-dispatched comparison renders, so **a missing comparison looks identical to an `S75` free entry that legitimately has none** — the reader cannot tell *we did not compute this* from *there is nothing to compare*. Latent: no row carries it today. Found as `B3` | `src/app/[locale]/(app)/dispatches/[id]/page.tsx:496`, `SPEC.md` `S77` | Open — latent, and cheap while it stays that way |
| ~~**The quotation readers ignore `companies.merged_into_id` while every follow-up reader excludes it**~~ — so a merged company's quotations stay on `/quotations` and drop off the chase, the same split shape as the archive row above. And `quotationNoResponse` INNER JOINs `audit_log` on an **untyped `entity_id`** (`follow-ups.ts:295`): an issued version with no audit row is invisible to that queue **for ever**, silently, because an inner join drops it rather than failing. The no-writer half of `merged_into_id` is already a row below and is not restated here. Found as `A2`/`E3` | `src/lib/follow-ups.ts:295`, `src/lib/quotations.ts` | **Narrowed — session 55.** Re-measured: `audit_log.entity_id` is **`uuid`**, not text (`information_schema`, 3 Sep 2026), so the join is typed and the *drops rows silently* half of this row does not exist. What stands is the half that is really `quotation_versions` having no `issued_at` — an issued version whose audit row is missing is invisible to the chase — and that is the count-route row above, whose fix is a column with its writer. The reader asymmetry cannot bite through a merge since session 54. **Nothing left here that the `issued_at` row does not already carry** |
| ~~**Should a quotation stop being chased once a dispatch is submitted against it?**~~ `S133` deleted the two `isNull(payment_confirmed_at)` suppressors and **nothing replaced them**, deliberately: measured before the deletion, the payment term removed **0** rows, because every paid thread also carried `end_state = 'accepted'` and `isNull(end_state)` already dropped it. The deletion therefore changed no behaviour, which is what made it clean. But the question the column was reaching for is real and now has nobody answering it. **Measured on a clean `seed:demo`, manager-wide, before the working-days cutoff: the `quotation_no_response` pool is 15 threads, of which 7 already carry a dispatch and 3 carry one at `submitted` or `approved`.** So the answer is worth between 3 and 7 rows on that queue | `src/lib/follow-ups.ts` `quotationNoResponse` `quotationReturned`, `SPEC.md` `S132` `S72` `S88` | **Closed — session 55** (`WORKFLOW §4` row 51): the founder answered in `S137` and the term is in — one `notExists` on `dispatches` at `submitted` or `approved`, inside `silentIssuedThreads`, the one definition both the chase and the Reports tab read. Re-measured first: company-wide 106 of 131 silent issued threads carried such a request (verify residue; 3 of 15 on a fresh seed). `verify:phase10a` drives it — a draft pauses nothing, a submitted request pauses, a refusal brings the chase back |
| **Dropping the payment term from `projectMovement()` reorders `/projects` and moves `S89`'s stage-unchanged clock.** `projects.ts:284` took `greatest(max(thread.created_at), max(payment_confirmed_at))`; with the column gone under `S133`, a project's thread event is its raise date alone, so **every project whose payment stamp was later than its raise ages by exactly that gap**. `D25` orders the table oldest first and `projectStageUnchanged` composes the same function, so the board's within-column order, the table's order and the follow-up queue all shift together. A consequence of `S133`, not a decision — recorded so whoever notices the ordering moved has the answer written down rather than rediscovering it | `src/lib/projects.ts` `projectMovement`, `src/lib/follow-ups.ts` `projectStageUnchanged`, `DESIGN.md` `D25` | None — correct as written. The alternative was keeping a column `S133` deletes, and no rule makes a payment a movement event now that payment lives on the dispatch `S70` |


**Added by AUDIT 1 (§6), ranked by consequence.** Counts are from the live
database on 19 Aug 2026, after the first **sixteen** migrations. There are now
**28**. The counts stay as they are — dated, and honest as history — but read
them against that. In particular **`0017` is the region migration**, so "0 of
414 projects carry a region" below describes exactly the state that migration
was written to change; and rows closed by the **`0027` dead-structure sweep**
carry their own re-count taken on 24 Aug 2026, so where two numbers appear the
later one is the sweep's.

**`0027` is where F9–F18 were answered as a set**, and the answer was not
uniform: eight columns, one enum value, two types and four indexes left, while
five tables, seven indexes, three role flags and two columns stayed because a
rule still names them. `F9` is confirmed closed — `companies.has_credit_terms`
is absent from the database, dropped in `0022`. `F18` turned out to be one
constraint, not a class: `quotation_threads_closed`, which stays with the
`S47` columns it guards.

| What | Where | Disposition |
|---|---|---|
| `projects.region` and `projects.city_id` have **no writer that has ever fired**: 0 of 414 projects carry either, and no `project.created` or `project.updated` row in the audit log ever set one. **The discarded-input half is fixed** — `449a7e8` removed `regionForCity`'s fallback, which left the project form rendering a region select whose value silently vanished; that select, its `readProjectForm` read and `ProjectInput.region` are gone `D51`, and `verify:routes` asserts the form offers no region in both locales. The project's CITY select is deliberately untouched: what a rep picks there **is** stored, so it has something behind it. **What stays open is the columns question**, and it is three shapes: **(a)** a project's city is mandatory and the region derives from it; **(b)** it derives from the project's own city and the city stays optional, so the region is usually null; **(c)** both columns are dropped as unused structure, and a project's region is derived from its participants' cities if it is ever wanted. **It cannot simply mirror the company fix: `projects` has no country column**, so `S15`'s "mandatory when Saudi" has nothing to hang on | `src/db/schema.ts:813-814`, `src/lib/projects.ts` | Own session — needs a founder decision between the three, then one of them |
| **`end_state` is now derivable from `lost_reason_id`.** With the type down to `'lost'`, `projects_loss_state` and `projects_loss_detail` plus `assertLossReason` make the two a biconditional in both directions — a project has an end state exactly when it has a loss reason. The column could go, and the form's Open/Lost select becomes "pick a reason or do not". **Declined three times now**: twice in the `S31` session, as the third widening of one slice's scope after two deliberate ones, and again in the `0027` sweep. The sweep's reason is its own test — *redundant is not dead*. This column has a writer and readers; taking it changes a form, `projectState` and an enforcement path, which is not what a slice that removes what nothing reads should be doing. Whoever takes it should note it has now survived three sessions that each had good reason not to | `src/db/schema.ts`, `src/lib/projects.ts` | Undecided — a simplification, not a defect. Its own session, deliberately chosen |
| **A free-entry dispatch names no project, so it can never win one** (`S75`, `S31`). `requestDispatch` writes `project_id` null on the direct route by design, `/dispatches/new?mode=direct` loads no project options and its form renders no control. `S31`'s derivation is therefore true for the two quotation routes and vacuous for the third. `S75` carries its own marker for this and `verify:slice3` §28 asserts it, so it cannot be mistaken for built | `src/lib/dispatches.ts`, `dispatches/new/page.tsx`, `dispatch-form.tsx` | `S75`'s remaining half — its own session |
| **`projectStageUnchanged` reaches dispatches through the thread**, not through `dispatches.project_id`, so a free-entry dispatch is not a project event on the follow-up clock. Since `S74` the column is the reach every other reader uses — `dispatchedSqmByCompany` and `projectIsWon` both. Harmless today only because of the row above: a free entry has no project to be an event for | `src/lib/follow-ups.ts:588` | With the row above — the two are one question |

| **The quotation's end-state badge is a status→colour map, which `D6` forbids.** `rejected` and `cancelled` render `destructive`, `accepted` renders `secondary` — *colour describes how long something has waited, never how good the outcome is*, and `D6` names `accepted` specifically as where that goes wrong. Found in session 17, which put the dispatch's own cancellation card and status badge deliberately on the other side of the line: no tone at all, asserted by `verify:routes`. Not fixed there — it is a pre-existing design defect on a different screen, and changing it was outside the task | `quotations/[id]/page.tsx:146-157` | **AUDIT 2, and it is now AUDIT 2 alone.** The shell slice shipped without it, and `AD9` below routed the four maps to sessions **26**, **28** and **41** — all three shipped and **all four maps are still there**. Two of them now cite `D6` as their justification rather than their breach: `quotations/page.tsx` reads *"the bad terminal states get the destructive badge; nothing else does `D6`"*. That is a second reading of the rule, not a slice forgetting — so it is a rule question for `§6b`, beside the audit, rather than four code rows |
| **Two pickers name somebody who takes credit, and neither asks whether they may have it.** The **rep on a direct dispatch** (`dispatches.ts:1012`) checks **nothing** — not even `isActive` — only `can_dispatch` to name somebody other than self, and 100% of that dispatch credits the rep named on it `S78`. A **credit split's members** (`credit-splits.ts:205-210`) check **existence only**, so a deactivated colleague can be credited. Both answer *"yes, always"*, and they are **one question — may this person receive credit? — not two unrelated pickers**. That question is also the whole of it: credit reaches a person through `dispatches.user_id` and `project_credit_splits.user_id` and nowhere else, so answering it once answers it everywhere. `sharing.ts` was in this row until the founder settled it and is **not** part of the question: a share grants access to a record somebody else still holds `S100`, so it hands over no book and no credit, and `isActive` is the right test there | `src/lib/dispatches.ts:1012`, `src/lib/credit-splits.ts:205-210` | **One session, with `S79` and `S80`.** Both markers already move this code — `S79` takes shared credit off the project and onto the quotation, `S80` builds the coordinator's confirmation at approval, the moment credit is decided `S72` — so that session opens both pickers anyway and is where the one answer belongs. Deciding it earlier would be deciding it twice |
| **Five tables with no reference anywhere in `src/` or `scripts/`, and 0 rows each.** **Classified by the `0027` sweep, and all five KEPT** — the answer is *each with the slice that would build its writer*, never the second deletion pass this row also offered. `attachments` `S115` · `delete_requests` `S105`–`S107` · `duplicate_flags` and `non_duplicates` `S21`–`S23` · `product_specifications` SPEC §16, which has not decided. `SPEC §15` now names all five under what the sweep examined and left, and `verify:schema25` §2 asserts each present with its rule, so *0 rows, no reference* can never again read as a licence to drop one. `accounts` is the documented sixth — the adapter's TYPE requires it. **One new finding, recorded not acted on:** `delete_requests.status` is `pending/granted/denied`, which is not `S105`'s three outcomes (*archive · keep · reassign*), and `S106` says the record is **one table** shared with the dormancy review. But a `company_dormancy_reviews` row has `outcome NOT NULL` and cannot hold an undecided request, so `S105` genuinely needs somewhere for a pending one to sit. Which vocabulary survives is that slice's to settle, not a sweep's | `src/db/schema.ts` | **Vocabulary settled — session 54** (`0035`): neither survives. The request never names an outcome; `delete_requests` left, `company_removal_requests` arrived with its writer, and `S106`'s review row stays the one record. The remaining two of the five, `duplicate_flags` and `non_duplicates`, gain their writer in the same session's phase 4 |

| **`S92` keeps "never work" while carrying "credit granted to you"** (`S129`). Nothing in SPEC lets a rep query or contest a split — `S82` forbids them setting one — so it holds as written. Untested the first time a rep disagrees | `SPEC.md` `S92` `S129` `S82` | None. Recorded so the pilot watches for it |
| **An unclosed `/**` docblock swallows real code and passes `typecheck`, `lint` AND `build`.** Found in session 24 while narrowing `verify-phase10a` §10: a comment was opened, its closing `*/` omitted, and the next docblock's `*/` closed it — silently commenting out **twenty-six lines of live assertions**, including two of the section's three checks. Every static gate stayed green, because commented-out code is valid TypeScript. **What caught it was one eslint WARNING** — *`unreadCount` is defined but never used* — which is a report about an import, not about the missing assertions, and which nothing would have printed had the swallowed block not happened to hold the only use of a named import. This is `CLAUDE.md`'s *a Tailwind class that compiles to nothing fails no check* at a second address: the tool cannot tell you, and the shape is invisible in a diff that is mostly prose | any `.ts`/`.tsx` file; found at `scripts/verify-phase10a.ts` §10 | **Open — the instance is fixed, the shape is not.** The cheap guard is a scan for a block comment containing a line that does not begin with `*`, which is what found it; it false-positives on every JSX `{/* … */}` and so is not a lint rule as written. `OPEN — not chosen` |

**Added by AUDIT 1b (§6), ranked by consequence.** Counts are from the live
database on **24 Aug 2026**, after the twenty-eighth migration and after a full
run of all ten verify scripts — 987 companies · 517 projects · 775 quotation
threads · 807 versions · 1,230 dispatches · 466 comments · 952 live company
memberships. All ten scripts, and `typecheck` · `lint` · `build` ·
`check:messages`, were **green**; `verify:routes` passed 714 checks in both
locales. Markers stand at **12 `[CHANGE]` / 24 `[BUILD]`**.

**`AUDIT 1`'s own twenty-five were re-checked from the code and the database,
never from the commit message that claimed them. Nothing that was closed has
become wrong again** — every dropped column, index, type and enum value is
absent from `pg_catalog`; every deliberately kept table, index and flag is
present; and `F4`'s and `F12`'s invariants hold over **every row** rather than
over a fixture (930 companies with a live membership, 0 carrying no primary rep
and 0 carrying two; 946 live memberships, 0 held by an elevated role). Twelve
are closed, nine are open and unchanged (`F3`, `F8`–`F11`, `F22`–`F25`), `F7`
stays undecided by choice, and `F21` is now marked Done above.

**Two of the rows below are classes rather than incidents, and are marked as
such.** `A1`, `A4` and `A5` are **one question** — a marker that does not match
what is built — in three directions. `B1` to `B4` are **one shape** — an
assertion that can pass without reading anything.

| What | Where | Disposition |
|---|---|---|
| **`S8` names three roles for `can_export`; the seed grants it to two.** *"Export, delete approval and duplicate resolution are held by **Executive, Sales Manager and Super Admin**, and in practice the Sales Manager uses them."* The seed sets `canExport: false` for Sales Manager and states the opposite rule in its own header — *"bulk export is super-admin only `[07 B8]`, plus the executive `[12 §3]`"*. `can_approve_delete` and `can_resolve_duplicate` **do** match all three. `S8`'s `[CHANGE]` names only the missing readers — *"All three flags must be read by the code; today none are"* — so reading the flags would leave the Sales Manager still unable to export, and the rule would still say she can. Two sources disagree and the rule is one of them | `SPEC.md` `S8`, `scripts/seed/roles.ts:79` | Needs a founder decision — which of the two is right |
| **`S76` is `[CHANGE]` and every sentence in it is true of the code.** Sight is widened — `visibleProjectsFilter` and `visibleContactsFilter` return `undefined` for `can_dispatch`; `canViewRecord` is deliberately not widened, so she writes to neither; and her `S62` edit right over a submitted request's lines, stock, shipment and payment ships. `verify:routes` §15 drives every clause in both locales. The unbuilt thing the marker appears to be carrying is that **a dispatch names no contact** — `S20` says so and sends the reader to `S76`'s marker for the reason — but `S76` never mentions a contact column. Either the marker is stale or the rule owes the sentence `S20` is standing in for | `SPEC.md` `S76` `S20`, `src/lib/authz.ts:569-572`, `:625-628` | Open — a marker decision, or one sentence on `S76` |
| **`scripts/dispatch-fixture.ts:103-112` writes the value `verify:schema25` §17 tests.** It sets `differed_at_submission` from `dispatchDiffers`, which is the expression §17 asserts against. Its header explains why — a hand-written row has to satisfy `dispatches_difference_flag` at INSERT, before any line exists — and it is the least-bad option available. The consequence stands anyway: §17's *"no submitted dispatch differs with nobody named for it"* **cannot fail on a fixture row**, and 660 of 1,230 dispatches carry the flag pair, most of them fixture rows. The check is not empty — the real path computes the value once at submission and could drift from it — but its coverage is smaller than the count reads. **This one is demonstrated, not inferred**, unlike `B1` and `B3` | `scripts/dispatch-fixture.ts:103-112`, `scripts/verify-schema25.ts` §17 | Recorded, deliberately not changed. **One of four: `B1`–`B4` are one shape** |
| **`S17`'s only restriction has no assertion anywhere, and its column has never held a value.** `assertLeadSourceSelectable` is the whole of *"Marketing is not selectable by an ordinary rep"*. No verify script references a lead source; `verify-routes.ts:1664` and `:1902` post `leadSourceId` **empty** on purpose, with `categoryId`, `vatNumber` and `notes` beside it. After a full ten-script run, **0 of 987 companies carry a category, a lead source or a VAT number**, and 0 of 987 contacts carry an email or a position. This is **not** `AUDIT 1 F3`'s defect: the writers are live at every layer and 176 `company.created` audit rows carry all three keys — every one of them null. A writer that works and has never been given anything, so `S16`'s ten categories and `S17`'s six sources are unexercised end to end | `src/lib/companies.ts:326`, `scripts/verify-routes.ts:1664`, `:1902` | Open — a verification gap, not dead structure |
| ~~**Five doc claims that contradict a current rule.**~~ This project reads comments as authority, so they are listed rather than counted. `quotations.ts:2003-2004` — `confirmPayment` still says *"dispatch is blocked until it is set"*; `S72` and `S73` moved the gate to the approval and replaced it with a payment method, and `dispatches.ts:1320-1335` says so at length. `dispatches.ts:2452-2453` — *"`S31` makes a project won when the payment arrives"*; `S31` wins on an **approved dispatch**, and the code below the sentence is right. `quotations.ts:1985-1987` — `cancelThread` names a `quotation_thread.end_state_set` audit row; `setEndState:1862` writes `quotation_thread.cancelled`, which `schema.ts:1216` names correctly. **Eight references to `recordDispatch`**, a function `dispatches.ts:728` says no longer exists. And `authz.ts:1191` cites *"`SPEC §15`'s F8 row"*; the `F` rows are here in `WORKFLOW §5` | `src/lib/quotations.ts`, `src/lib/dispatches.ts`, `src/lib/projects.ts`, `src/lib/authz.ts:1191`, `src/db/schema.ts` | **Closed — session 55.** Re-measured before touching: two of the five were already gone (`confirmPayment`'s *blocked until it is set* and `S31`'s *when the payment arrives* no longer appear in `src`). The other three fixed: `cancelThread` now names `quotation_thread.cancelled`; six of the eight `recordDispatch` references now say `requestDispatch` — the two in `dispatches.ts:836-842` are kept, because they are the sentence *there is no `recordDispatch` any more* and are the record of why; and `authz.ts` sends the reader to `WORKFLOW §5`'s F8 row |
| ~~**One mutation in `src/lib` sits outside `withAudit`.**~~ `authz.ts:212` deletes a deactivated user's sessions from inside `getSession` — outside a transaction and outside the audit pen, and the only such write in the module. Nothing is lost: the `user.deactivated` row records the act and sessions are Auth.js rows. But `S112` says **every** mutation writes to the audit log, and nothing anywhere names this as the exception | `src/lib/authz.ts:212`, `SPEC.md` `S112` | **Closed — session 55**, the one-sentence way: `S112` now names the exception — `getSession` deleting a deactivated person's sessions (`S101`), Auth.js rows and not a record, the act already carried by the `user.deactivated` row. The code is unchanged |
| **`smac_reference_verification = 'verified'` has never been written.** 0 of 807 versions, and 0 `quotation_version.issued` audit rows carrying it. Not a dead value — `thread-actions.tsx:243-251` renders a real `<select>` over both `SMAC_VERIFICATIONS` — an unexercised one, the same family as `S16` and `S17` above | `src/db/schema.ts`, `quotations/[id]/thread-actions.tsx:243-251` | Open — a verification gap |

**What AUDIT 1b could not check.** Sight — `AUDIT 2` is the interface audit, so
no `D` rule about layout, spacing or prominence was judged by eye, including
`S117`'s *"below square metres in prominence"*, which was read from the markup.
`verify:routes` drove a server on **port 3100**, because the Docker app
container holds 3000; §0's build guard passed, so it drove this build, but
`next start` warned it does not pair with `output: standalone` and the
container's own server was not the thing driven. `.env` was unreadable to the
session, so `DEV_FIXTURE_PASSWORD` and `NODE_ENV` were confirmed only by the
scripts running. `B1` and `B3` are shapes read from the code, not failures
constructed. Migration bodies were not read — the end state was checked in
`pg_catalog` and in the rows, so a migration that reached the right result by a
wrong route would not appear here.

**Added by AUDIT DESIGN (§6), ranked by consequence.** `DESIGN.md` D1–D63
against itself, against `SPEC.md`, against `src/`, against
`.claude/skills/facet-ui/SKILL.md` and against
`docs/design/facet-concept-v5-premium.html`, on **24 Aug 2026**. Read from
files only — no database query, no verify script, no rendered screen.

**The tally is the finding.** Of 63 rules, **17 hold**, **17 are partly true**,
**26 describe something that does not exist**, and **3 are not buildable
statements** (`D1`, `D3`, `D31`). `DESIGN.md` has no marker convention, so
`D46` (built) and `D42` (no such screen) are typographically identical, and
`SPEC §7`'s rule — *a rule that is only partly built says so in its own text* —
has no equivalent here. The full per-rule classification is in the audit's
report; the rows below are what has to be decided or corrected.

**The three lists below are the audit's count as at 24 Aug 2026, and are left as
taken.** An audit's number records what was true when it was read, and
`DESIGN.md` still has no marker convention to read a current one from. Later
slices have moved rules: the token slice alone rewrote `D5`, `D7`, `D8`, `D9`,
`D15`, `D16` and `D19`, deleted `D18`, and made `D8`, `D13`, `D14` and `D19`
true. Re-take the count; do not patch it.

**Holds (17):** `D4` `D10` `D11` `D22` `D23` `D37` `D46` `D47` `D50` `D51`
`D53` `D54` `D57` `D59` `D60` `D61` `D63`.
**Partly true (17):** `D2` `D5` `D6` `D9` `D12` `D20` `D21` `D24` `D27` `D32`
`D33` `D34` `D39` `D52` `D55` `D58` `D62`.
**Nothing makes it true (26):** `D7` `D8` `D13`–`D19` `D25` `D26` `D28`–`D30`
`D35` `D36` `D38` `D40`–`D45` `D48` `D49` `D56` — the last three
**contradicted** rather than merely absent.

**One row was the root rather than an incident, and it is closed.** `AD3` — the
stylesheet was a different generation of the design — is why `D8` and
`D13`–`D19` were unbuilt as a set, and why the file could not be marked rule by
rule until a founder decided which generation wins. **The token slice is that
decision: v5, and the whole palette.** It closed `AD2`, `AD3`, `AD11`, `AD12`,
`AD13` and `AD15` together, and `AD29` in passing. The second sentence here
used to credit `AD11` with `AD3`'s job; `AD11` is the gradient count.

**`AD1` is already corrected**, in `§4` above, and is recorded here so the
correction is checkable.

| What | Where | Disposition |
|---|---|---|
| **Three list tables keep a horizontal scroller below `md`, deliberately.** `D56` is a rule about a phone row and `D55` says which screens are phone-first: `/users`, `/targets` and `/activity?view=by-rep` are laptop-first work for a manager, so `38b` left them as columns rather than designing a phone row for a case nobody has. Each `<Table>` says so at the call site, because an unannotated table and a deliberately unannotated one look identical. **`/targets` has a second reason and it is a rule conflict, not a preference**: `D49` asks that screen for the goal and the attainment TOGETHER — one table, one row per rep — and `D56`'s one kept column carries one of them. `D24` and `D49` already disagree about that screen two rows above; this is the same disagreement reaching a different rule, and it is evidence `D56` does not fit a laptop screen rather than something to bend inside one. **`/activity?view=by-rep` holds the only `<TableFooter>` in the product** — the team totals — which a phone row has nowhere to put | `DESIGN.md` `D55` `D56` `D49`, `users/page.tsx`, `targets/attainment-table.tsx`, `activity/page.tsx` | Recorded, not open. Revisit only if somebody reports working one of the three on a phone |
| **`AD9`. Three status→colour maps ship against `D6`; this section records one.** The recorded one is `quotations/[id]/page.tsx:146-157`. Two more: `quotations/page.tsx:30-32` + `:177`, the same map on the **list** as a separate function; and `_components/project-state.tsx:43`, `lost`→`destructive`, rendered on the projects list, the project detail and the company page's projects card — whose own comment says *"whether a status→colour map belongs here at all is a `D6` question this rule did not open."* A fourth candidate: `dispatches/[id]/page.tsx:283` renders `approved` as `default`, solid brand red on a state, which `D7` also forbids | `src/app/[locale]/(app)/quotations/page.tsx:30-32`, `_components/project-state.tsx:43`, `dispatches/[id]/page.tsx:283` | **Re-owned: `AUDIT 2` and `§6b`, together.** This said each map leaves with its own screen — `/quotations` **26**, the quotation detail **28**, `project-state` **26** and **28**, `dispatches/[id]` **41**. All four sessions shipped and **not one map came out**, which after four passes is evidence the rule is read two ways rather than that four slices forgot. The `D6` row above carries the argument |
| **`AD18`. `D62` cites a rule `DESIGN.md` does not contain.** *"This is the converse of the `dir=\"ltr\"` rule, which only ever covered LTR content."* There is no `D` rule for `dir=\"ltr\"`. It lives only in the skill. `D62` was written to promote a skill-only lesson into DESIGN — `D59`–`D63`'s whole purpose — and left its own counterpart behind. 133 `dir=\"ltr\"` sites in `src/` obey a rule that is not in an authority file | `DESIGN.md` `D62`, `.claude/skills/facet-ui/SKILL.md:158` | Session **47** — one sentence, and it is the class `D59`–`D63` exists to close |
| **`D24` asks a list to be grouped and `D49` asks Targets for one flat table, and a row here is a person.** `D24`'s List archetype is *grouped, never flat*, with group headers naming the group and its count, and *one column says whose move it is*. A row on the merged Targets table is a **rep**, who has no move, and `D25`'s groups are named for companies, quotations and projects — never for people. `D49` is the rule written about this screen and it says *one table, one row per rep*, naming no group. **`28b` shipped it flat on that reading and recorded the conflict rather than inventing a group.** The one honest candidate is **measured / not measured**, which is a real division already in the data — `targetSqm` is null, never `"0"`, precisely because *not measured* is not *measured at nothing* `S83` — and it would be useful to whoever holds `can_set_targets`, which is the identity that opens the screen to find who has no target. No rule names it, so it is `OPEN — not chosen`. The same shape as `D25`'s missing *due soon* two rows above: a group invented in code becomes the division everyone believes in | `DESIGN.md` `D24` `D25` `D49`, `src/app/[locale]/(app)/targets/attainment-table.tsx` | Open — needs a `DESIGN.md` line, not a group picked in code. `§6b`'s rule review, with `D24` |
| **FINDING — the widened `D73` forbids what 102 Arabic table cells ship, and the heading and the number really do disagree.** Session 49 applied `D73`'s container clause and then measured the tree against it rather than assuming. `TableCell numeric` compiles to `num text-end` and callers add `dir="ltr"` **on the `<td>`**; `TableHead numeric` compiles to `text-end` and carries **no `dir`**. `text-align: end` resolves against the element's OWN direction, so on an RTL page the heading's *end* is the LEFT edge and the cell's is the RIGHT one — a right-aligned column under a left-aligned heading, which is the thing `table.tsx:101-102`'s own doc comment says the `numeric` prop exists to stop. **Measured over HTTP, not reasoned**: `/ar/targets` 27 cells under 4 headings, `/ar/activity?view=by-rep` 50 under 5, `/ar/projects?view=table` 25 under 1 — **102 cells on three screens**; `/ar/quotations`, `/ar/dispatches` and `/ar/users` have end-aligned cells but none carrying `dir`, so they are correct and were not counted. **Not fixed here** — `§6b` changes no code and this brief said a rule needing a `src/` change is a FINDING, not an edit. The fix is one place (`TableCell`: isolate the figure in an inner `<span dir="ltr">` and leave the `<td>` the page's direction) and it must be measured screen by screen, because a numeric column that is *already* correct must not move | `src/components/ui/table.tsx:101-102` `:150-165`, `DESIGN.md` `D73`, `A2-13` | **Open — its own slice.** The measurement is repeatable: count `<th>`/`<td>` carrying `text-end` on an `ar` page and compare which of them carry `dir` |
| **FINDING — the widened `D62` says `dir="auto"` never on a block, and 416 block carriers ship on ten Arabic screens, most of them CORRECT.** Measured on the same run: 416 block-level `dir` carriers over ten `ar` screens, **163 distinct shapes**, and the dominant shape is `<span class="block truncate" dir="auto">` around a company or person name. That placement is **deliberate and was argued in writing** by session 47 fixing `A2-20`: on a truncating block the ellipsis lands on the line's END edge, which must be the VALUE's own end, or an English name on an Arabic rail clips from the front — *`l Al-Harbi`*. Wrapping the value in an inner span does not move the ellipsis, because truncation happens on the block. **So the rule as approved condemns the correct fix for a defect the same review's family produced.** `D62` needs the truncation case written into it — *the one block carrier is a block whose own overflow must clip at the value's end* — or the rail and every truncating name cell become standing breaches. Recorded, not resolved: the brief approved the text as written and this is what applying it revealed | `src/components/app-rail.tsx:340-356`, `DESIGN.md` `D62`, `A2-20` | **Open — `§6b`, next review.** A rule whose first application finds 400 counter-examples is under-specified, not disobeyed |
| ~~**FINDING — the bottleneck card's two figures ARE `A2-13`'s defect, still live, and the widened `D73` now names them.**~~ `bottleneck-card.tsx:156` renders `<p class="num mt-1.5 text-2xl font-semibold" dir="ltr">` — a BLOCK carrying `dir`, so `text-align: start` resolves to the paragraph's own left edge and in Arabic the number sits a card's width from the label above it. **Seen, not deduced**: `manager/dashboard-ar-light-1366.png`, session 49's re-shoot — the labels sit at the card's right edge and the figures **47** and **63** at its left. Two more of the same shape: `dispatches/[id]/page.tsx:709`, a `<p class="text-start" dir="ltr">` whose run ends in the translated word `common.sqm` — so it is a WORD run and takes `dir="auto"`, `D73`'s own test — and `credit-split.tsx:61`, a `<dd class="text-start" dir="ltr">`. `A2-13` was recorded as fixed for the KPI tiles and the `SideFigure`; the bottleneck card was never in that list | `src/app/[locale]/(app)/_components/bottleneck-card.tsx:156`, `dispatches/[id]/page.tsx:709`, `projects/[id]/credit-split.tsx:61`, `manager/dashboard-ar-light-1366.png` | **Narrowed — session 55.** `bottleneck-card.tsx` no longer exists (`grep -rln bottleneck src` finds nothing; the overseer's Stuck block replaced it in session 51 and its figures sit in `<span dir="ltr">`), so the sighting this row was named for is gone with the component. The two other block-level `dir` carriers it listed — `dispatches/[id]/page.tsx` (`<p … dir="ltr">` at the metres line) and `projects/[id]/credit-split.tsx` (`<dd … dir="ltr">`) — still ship, plus three in `quotations/[id]/thread-lines.tsx` and one in `companies/duplicates/[flagId]/page.tsx` found by the same grep; they go with the `TableCell` row above, same mechanism, same slice |
| **FORM FIELD ORDER — what a rep fills in FIRST, and nothing decides it.** Recorded in session 49, when the founder read `§6b`'s `D24` sort-clause proposal and said that was not his question: he was asking about the **order of fields inside a form** — making a quotation, raising a dispatch — not the order of rows in a list. `D24`'s Form archetype says *single column, labels above, errors under the control, actions in a footer bar*, and says **nothing about sequence**; no `S`-rule does either. So every form's field order today is its **build order** — the order the columns went into the schema, the order the slice was written — which is a decision nobody took. This is not a defect in any one screen and no screen is wrong against a rule; it is a rule that does not exist. **Founder decision, and it needs a screen-by-screen look with him** — the forms differ, and what a rep reaches for first on a quotation is not what he reaches for first on a dispatch request. **Do not design it.** `D24`'s sort clause stays HELD in the `§6b` block above, unapplied, because it answers a question he did not ask | `DESIGN.md` `D24` (Form archetype), `WORKFLOW §6b`, every screen under `src/app/[locale]/(app)/**/[a-z]*-form.tsx` | **OPEN — the PROCESS is decided, session 50**: propose one form at a time, current order and proposed order side by side, the founder corrects (*"not all at once"*). Each form is its own `§4` row (54…); the answers are still his, per form. Not a code row, and not `§6b`'s: a rule review may not invent a rule for something nobody has asked for |
| **The three figures the founder would open FACET to check, and what each one costs.** Reported in `28b` and deliberately not built there — a fourth block is the failing that session removed — but they are what he wants next, and **each needs a rules slice before a screen slice.** (1) **Issued → signed.** Computable: `quotation_versions.status = 'issued'` against `quotation_threads.end_state = 'accepted'`, which `S64` makes the signature. **`S65` refuses it in those words** — *no count, funnel or conversion figure may treat it as won*. FACET's own `won` is an approved dispatch (`chain.ts`, `chainReached`), and an issued→won **cohort over a month** is honest but is a new derivation: `quotedCount` is a snapshot of who sits at `quoted` now, not a cohort. (2) **Quoted m² versus dispatched m².** **`S68` refuses it** — *quotations are never summed* — and the `D26` row above already worked the same argument for one project. Legal only **per dispatch**, where it ships `S77` `S116`. (3) **Loss reasons ranked.** Computable and unrefused: `projects.end_state = 'lost'` + `lost_reason_id` → `loss_reasons`, windowed by `lost_at`. Nothing derives it and **no rule names it**. Note it is a **project** ranking — a rejected or cancelled thread carries free-text `cancellation_reason` and no reason id, so *loss reasons* and *why quotations die* are two questions | `SPEC.md` `S65` `S68` `S64`, `src/lib/chain.ts`, `src/db/schema.ts` (`projects.lost_reason_id`) | Open — (1) and (2) need `SPEC.md` to say what may be counted; (3) needs a rule before a screen. None is a code row yet |
| **`AD21`. `D52`'s second half is unbuilt in all 21 places and `D52` reads as done.** *"An empty list says what would make it non-empty **and offers the action**."* All 21 are a bare `<p class=\"…border-dashed…\">`; **none renders an action**, and the strings are *"No companies yet."*, *"Nothing logged yet."*, *"Nothing is waiting."* `D60`'s placement half is built and correct | `DESIGN.md` `D52`, `messages/en.json`, 21 sites | Session **47** marks it; the sweep that writes the strings is its own |
| **`AD26`. `D17`'s three motions are unbuilt and `prefers-reduced-motion` is honoured nowhere.** **BLOCKED on the `D17` proposal** (`§6b` block above): the rewrite names the stillness as the design and retires the reduced-motion sentence with nothing left to gate, so there is no code fix to make — building the three motions to satisfy a rule under review would be the backwards move. The dead `tw-animate-css` import went with `AD27`'s sweep. Original finding:  No media query, no `motion-reduce` variant anywhere in `src/`, against *"`prefers-reduced-motion` is respected and is the path that gets tested."* The only motion that ships is `transition-colors`, which `D17` does not name and `D21`'s last clause therefore forbids | `DESIGN.md` `D17` `D21`, `src/` | Untidiness — session **47**; session 23 shipped the shell without it |
| **`AD32`. `D22`'s ranges are met except in two components.** `Card data-size=\"sm\"` is 12px against a 15px floor; `RecordRow` and `TimelineRow` are `py-2.5` (10px) against 11–12px. Read from the markup, not by eye | `src/components/ui/card.tsx:15`, `src/components/page-header.tsx:220`, `src/components/timeline.tsx:115,168` | Untidiness — AUDIT 2 confirms by eye |
| **`AD34`. `background-attachment: fixed` is ignored on iOS Safari, so `D13`'s glow is not fixed on a phone.** The canvas gradient sits on `<body>` and the declaration is correct; iOS Safari has never honoured `fixed` and paints as though it were `scroll`, so the atmosphere travels with the content instead of staying behind it. Found while building the token slice and deliberately not fixed there — every workaround is a positioned pseudo-element or a second painted layer, which is machinery `D13` does not ask for and `D20` would have to name. It matters because `D55` builds rep screens phone-first | `src/app/globals.css`, `DESIGN.md` `D13` `D55` | **Open, and it has no owner.** It said *session 38's phone pass*; `§4` never defined a session 38 — it is `38a`, `38b` and `38c`, and all three have now shipped without touching it. None of them was the wrong place: every workaround is a positioned pseudo-element or a second painted layer, which is machinery `D13` does not ask for. It waits on whoever first opens FACET on an iPhone |
| **`S44-5`. The container behind the tunnel is eleven days behind the repository.** `facet-crm-app:latest` was built `2026-08-19 13:56`; `HEAD` is `c58780f`, today. So the image serving port 3000 predates every session since, and a security review that drove it would have been reviewing code that is not in the tree. **Session 44 therefore built the current source and drove that on port 3100** — every route, action, bridge and expiry result above is measured against `HEAD`, not against the container. What the container was used for is only what it uniquely answers: the published-port and cookie facts in `S44-2` and `S44-3`, which are properties of the deployment rather than of the build. **This is not itself a security defect** — it is a statement of what any claim about "the running system" can and cannot cover, and a reason `docker compose up -d --build` belongs in whatever ships next | `docker-compose.yml`, the `facet-crm-app` image, `README.md` § *Deployment notes* | Open — housekeeping, and a precondition for the pilot. No rule touched |
| **`S44-6`. `/api/health` answers the internet with database reachability and process boot time.** Unauthenticated by design and correctly so — Docker's `healthcheck` polls it — but the healthcheck reaches it at `http://127.0.0.1:3000/api/health` **from inside the container**, so nothing requires it to be reachable through the tunnel or across the LAN. What it discloses to an anonymous caller is `{"ok":true,"app":"up","db":"up","bootedAt":"…","checkedAt":"…"}`: that the app exists, that its database is reachable, and precisely when the process last started. `bootedAt` is deliberate and load-bearing — `verify:routes` §0 refuses a server that booted before its build — so the route cannot simply drop it. Low consequence; named because an unauthenticated endpoint on a public tunnel that reports internal infrastructure state is the kind of thing a review is supposed to write down rather than wave past | `src/app/api/health/route.ts`, `docker-compose.yml:48-54` | `OPEN — not chosen`. Options are narrowing the public body to `{"ok":…}` while keeping the full body on loopback, or leaving it. Both are decisions, not fixes |
| **`S44-7`. An anonymous server-action POST is redirected to an absolute URL naming `localhost:3000`.** Measured across all 31 replayed action forms: an unauthenticated `GET` redirects **relatively** (`307`, `location: /en/login`), while an unauthenticated action `POST` redirects **absolutely** — `303`, `location: http://localhost:3000/en/login` — on a server that was answering on port 3100. The host in that header therefore comes from the app's configured URL and not from the request, which is the same `AUTH_URL`/`PUBLIC_URL` resolution `S44-3` records, and the `authjs.callback-url` cookie corroborates the value. Consequence is availability rather than disclosure: with `PUBLIC_URL` unset in production, a rep whose session has lapsed and who submits a form is sent to `http://localhost:3000/en/login` — their own machine, where nothing is listening. It is not an open redirect: the value is configuration, never attacker-supplied. **The refusal itself is correct** — the action stopped at `requireSession()` and wrote nothing | `src/lib/authz.ts` `requireSession`, `src/i18n/navigation.ts`, `docker-compose.yml:45` | Open, and probably the same one-line fix as `S44-3`: set `PUBLIC_URL`. Worth re-measuring once it is set, because that is the only way to tell configuration from a `redirect()` that always absolutises |
| **`S45-9`. Every `?page=` in the suite is built by the script that then asserts on it — a whole class of coverage nobody has.** `§20`, `§24`, `§31` and `§32` all construct URLs like `` `/${locale}/dispatches?page=${p}` `` and assert against what comes back. Every one of them is therefore **testing the reader, not the screen**: they prove the route answers correctly for a URL the script invented, and prove nothing about whether a person clicking the pager arrives at that URL. The shape is general — **an assertion that builds its own input cannot see a defect in how the input is produced** — and it is not a defect in any one section, which is why it is recorded once here rather than as four rows. **`S45-6` is the demonstration that this gap is real rather than theoretical**: `/follow-ups`' pager dropped `group`, four sections had walked its pages for weeks without noticing, and the defect was found by audit rather than by the suite. **`§33` is the worked example of the fix** — it reads the rendered `href` out of the markup, follows it, and asserts the result, which is why it could catch a correct-looking link landing on an unfiltered list (`132 of 75, against 132 unfiltered`) | `scripts/verify-routes.ts` §20, §24, §31, §32 (URL construction), §33 (the worked example), `CLAUDE.md` § *Verification* | Open — its own slice, and it is a **survey before it is a fix**: the question is which of the four sections should follow a rendered pager rather than build its own URL, and the answer is probably not all of them, because building the URL is what lets a section reach page 11 of a 13-page walk cheaply. A reasonable shape is one assertion per paginated screen that the pager's own href round-trips, leaving the walks alone — which is what `§33` does for one screen. **Do not convert the walks** |
| **`S45-10`. `§19` hard-codes the page size that `§33` reads from source, and went red on a correct screen because of it.** `verify-routes.ts:4826` asserts `rows === Math.min(25, biggest.count)` under a comment reading *"`FOLLOW_UP_PAGE_SIZE` is 25"* — a copy of a constant that lives in `src/lib/follow-ups.ts` and can move. Measured rather than supposed: during `S45-6`'s second injection, with the constant temporarily at 500, it printed **`tile 39 · list 39`** and failed — the two numbers it prints are **equal**, and it still went red, because the `25` in the assertion is not the page size any more. That is the wrong-red shape `S45-1` records, arriving from a stale copy rather than a wrong equality, and a reader of that line cannot tell what went wrong from the numbers it shows | `scripts/verify-routes.ts:4820-4828`, `src/lib/follow-ups.ts` (`FOLLOW_UP_PAGE_SIZE`), `CLAUDE.md` § *Verification* | Open — row, not fix, and small. The repair is the device `§24`, `§31`, `§32` and `§33` already use: read the constant out of the data module by regex instead of typing it, and print it. It only misfires if the constant moves, so it is latent rather than live — but it is latent in the one direction that produces a red on a correct screen, which is the expensive direction. Worth doing in the same slice as any other change to `§19` |
| **`S45-11`. `§25` is not the only check whose two sides come from one computation — there are five, and one of them is a pure tautology.** Audited after `S45-7` proved `§25` blind, by tracing each sum-or-partition assertion back to what produces both of its operands. **Three degrees, worst first.** **(1) `verify-routes.ts:5142`, `/companies` — algebraically incapable of failing.** `companies.ts:286` returns `groupCounts: { quiet, touched: total - quiet }`, so the two rendered counts sum to `quiet + (total - quiet)`, which **is** `total` for any value of `quiet`, right or wrong. It can only fail if a group header never renders anywhere across the walk. I wrote it in `7f51569` and it reads as a scope check; it is not one. **(2) `verify-routes.ts:6398`, the stream** — the sighting that started this, proven by injection: `streamKindOf` folds five of six event kinds into `observed`, both sides grew by the same 127, and it stayed green while two new assertions went red. **(3) Three that are partially blind — sighted for the render, blind to the source.** `§24` (`:6024`) compares `count()` against three `count(*) filter (…)` from one statement over one `where` (`dispatches.ts:2106-2112`) whose filters partition all five statuses; `§31` (`:7969`) compares chip counts and total, both folded from the one `searched` array (`follow-ups.ts:1244`); `§32` (`:8195`) compares `dayCounts` against rendered rows, both from the one `events` array (`timeline.ts:1261`). Each catches the divergence it was built for — a header counting a page, a count ignoring a search, a fold running after the slice, and each went red under its own injection — but **none can see the shared computation itself being wrong**, because a row lost from that computation leaves both sides equal. **They are not worthless and must not be deleted**; the finding is that they are narrower than they read | `scripts/verify-routes.ts:5142` `:6024` `:6398` `:7969` `:8195`, `src/lib/companies.ts:286`, `CLAUDE.md` § *Verification* (the fourth shape) | Open — its own slice, and it is a **survey with one real repair in it**. `§20`'s tautology is the only one that should simply go or change: a sum over `quiet + (total − quiet)` proves nothing and should be replaced by a count that comes from somewhere else, or dropped in favour of the two assertions beside it that already carry the weight. The other four earn a **second side with an independent origin** rather than a rewrite — `§25` now has one (`S45-7`'s all-time-ranged read), and the same device is available to the rest: compare the screen's aggregate against a differently-derived read, kept apart so a disagreement names which side moved `b88718f`. **Do not merge the new assertion into the old one** — that is what makes a red attributable. **Recorded here too, as a second worked example of `de3ccc2`'s read-your-passing-output rule:** `S45-7`'s replacement kind-set assertion first sampled page one only and printed `ok … saw [comment,dispatched]` — a green line stating that the stream holds two kinds, which is false, over a fixture holding six. It was caught before it shipped by reading the green line and asking whether a reader could re-derive the claim from it; they could, and the claim would have been wrong. It now samples six pages of each side and prints all six kinds |
| **`§0` can be pointed at a different server than the one under test, and cannot tell.** The check whose whole job is refusing a stale server proves only that *a* server is fresh, never that the server it reached is **yours** — and **everything `§0` gates is downstream of that**, so a whole green run can be a reading of code nobody in the session wrote. **The mechanism seen in `25c`:** `docker-compose.yml` publishes the app container on `127.0.0.1:3000` while `next start` binds `0.0.0.0:3000`, and the more specific address wins, so with both up every request to `localhost:3000` reaches the CONTAINER — including `§0`'s own `/api/health` probe and the `bootedAt` it judges freshness by. **The binding rule is Windows-specific and the shadow is not**: the office PC is the same Windows + Docker Desktop arrangement this was found on, so it is live where the product actually runs, and the fix is the same in both places — this is not a laptop quirk to deprioritise. `§0` did fire twice in `25c`, correctly, on a stale *process* holder; it is the second server it cannot see. The tell is a `bootedAt` nobody started, and `netstat -ano` showing two PIDs on 3000; `docker stop facet-crm-app-1` restored the single one | `scripts/verify-routes.ts` §0, `docker-compose.yml`, `CLAUDE.md` § *Verification* | Open, and small. `§0` already reads `/api/health`; it could read a value only the local build knows — the `BUILD_ID` itself — and refuse when the server's does not match the file's, which catches the shadow, the stale process and a half-copied build with one comparison. Until then: **stop the app container before a run**, and check `netstat -ano \| grep :3000` shows one PID |
| **A Markdown row whose cell count does not match its header loses its trailing cells, silently, in a file this project treats as authoritative.** Found in `25c` while placing rows: **six `§5` defect rows were sitting at the foot of `§10`'s snapshot table**, which is two columns — so every one of them rendered without its *Disposition*, the cell that says what is to be DONE about the defect. The day-count row's own open question, *what does her day mean*, was invisible to anyone reading the file rather than its source, for as long as it sat there. A seventh row (`27b`'s comments row) carried an **unescaped pipe inside a code span**, splitting it into four cells and dropping its *Done* the same way. **Both are repaired** — the six moved into `§5`, the pipe escaped, and both tables checked to hold rows of one width — so this row is the failure mode, not the instance. **What makes it worth recording is that nothing looks**: `typecheck`, `lint`, `build` and `check:messages` all read code, `npm run status` reads counts, and no check in the project reads the shape of the three tables the sessions are actually driven from | `WORKFLOW.md` §4 §5 §10, `scripts/status.ps1` | Open as a **question, not a fix**: does a column-count consistency check belong in `npm run status`? It is a few lines — for each Markdown table, every row has the header's cell count — and it would have caught both. Against it: `npm run status` is a reading of the repository for the founder, not a linter, and adding checks to it is how it stops being read. Decide before writing it, and if the answer is no, say where it goes instead |
| **`chain.ts` and `follow-ups.ts` disagree about who owes a returned-for-edit quotation, and `D65`'s first column follows the chain.** A version that is `requested` sits at chain position `requested`, which `chainOwner` says the **coordinator** owes; but `quotationReturned` chases the **rep** on the same version until they touch the lines, because there is no resubmit act in FACET and the status never moves. Measured on `seed:demo`: **11 of the 42** threads in her issuing column have been returned at least once, so it is not a corner. The dashboard slice followed `chain.ts` deliberately — `D27` makes it the only ladder and a second one inside `quotations.ts` is exactly the trap that module exists to prevent — and the founder approved that reading rather than lifting `quotationReturned`'s audit-log predicate out. **The real fix is a resubmit act**, which would move the version's status and let both readings agree; until then one version can legitimately appear on her queue and in his digest at once | `src/lib/quotations.ts` `awaitingIssue`, `src/lib/follow-ups.ts` `quotationReturned`, `src/lib/chain.ts` | Open — needs a rule, and **session 26** is where it is read: `S86`'s three states cannot both be true of one record, so `SPEC.md` has to say which |
| **A planned row speaks in the second person to whoever can see it, and the person who set the date may be someone else.** `next_follow_up_at` is one column per record and `canViewRecord` admits `can_approve_quotation` on a thread `[16 §10]`, so a date a rep set appears on the coordinator's own planned section reading *"You planned to come back to this on …"* — and it is 2 of her 2 planned rows on `seed:demo`. **Pre-existing, not a regression**: the tile it replaced said *"Your date has come"*, which is the same second person; `D34`'s row wording only makes it a sentence. The record's own panel already reads back who set it — `nextFollowUpContext` exists for exactly this — so the fact is derived and merely not on the row | `src/app/[locale]/(app)/_components/waiting-list.tsx`, `src/lib/follow-ups.ts` `nextFollowUpSetBy` | **Session 24**, which owns the list — small, and it costs one query per planned row unless the setter is decorated in `gather`. `D34` says the planned section holds *the dates the rep set himself*, so the rule already implies the answer |

**The concept against `DESIGN.md`** — `AUDIT 1b` found three glows in
`facet-concept-v5-premium.html` that `D16` does not permit. There is a
**fourth**: `.nav .count` carries `box-shadow:0 2px 8px rgba(242,86,107,.4)`
(line 89), a brand-coloured bloom on the rail count badge — a judgement call,
but neither `--shadow` nor anything `D8` names. Beyond `D15`'s five, the
concept uses `--brand-grad` four more times: `.mark` (line 81), **`.tab.on`
`border-image` (line 111, which `D15` forbids outright — *"not on borders"*)**,
`.tile .spark i.hi` (159) and `.act:hover` (173, which is `AD11`). Four
gradients and eight shadows have no token at all — `.big` paints a gradient
**onto text** via `background-clip` (137), `.fill::after` a sheen (143),
`.reason .rb` a fading amber bar (213), four avatar gradients (91–93); five
one-pixel inset highlights sit on `.search`, `.seg button.on`, `.kind` and
`.av`, where `D8` gives `--line-hi` to *a card* and `D14` says the card texture
is *"used on every card, identically"*; three inset depth shadows on `.track`,
`.mbar` and `.gbar`. The concept also renders a **global search with a `⌘K`
hint** (232), which `D51` and `D58` each forbid. Its own unread tokens
`--brand-2` and `--ease` are real design decisions — a second brand hue, and
the motion easing `D17` never states — that no `D` rule carries.

**What AUDIT DESIGN could not check.** **Everything requiring sight — that is
`AUDIT 2`'s, and none of it is inferred above.** No screen was rendered, in
either theme or either locale, at any width. Specifically unjudged: whether the
neutrals read warm on a real display (`D4` — hex channel order only); whether
spacing reads generous (`D22` — numbers read out of the markup, which is
`AD32`'s caveat); whether any hover state changes nothing or an icon is centred
above a heading (`D21`); whether anything wraps at 1366 or 1440 (`D23`);
whether a screen answers `D3`'s question in five seconds; whether the design
looks correct with blur off (`D18`); whether the rail's mobile strip is usable
(`D56`). **Not run:** `typecheck`, `lint`, `build`, `check:messages` or any
verify script — an audit changes nothing and none was needed to read a file.
**Not queried:** the database; every count here is from files, and the comment
counts behind `AD7` are `AUDIT 1b`'s. **Not read:** migration bodies,
`docs/archive/`, `legacy/`; `docs/design/superseded/` is empty, so the v5
concept and the three files in `docs/design/archive/` are the whole design
record. **Judgement calls, flagged as such:** whether `.nav .count`'s coloured
shadow is a glow; whether `transition-colors` is motion under `D17` (`AD26`);
whether `dispatches/[id]:283`'s solid-brand `approved` badge is a status→colour
map (`AD9`).

---

**Added by AUDIT 2 (`§6`) — the visual and data audit, 31 Aug 2026.** The
interface seen as **pixels for the first time**: `@playwright/cli` +
`scripts/audit2/capture.mjs` shot **936 states** — 56 declared states × the
identities that reach each × both locales × both themes × 1366/1440/375 —
against the **built** server on port **3100**, after `seed:demo`; the compose
container held `127.0.0.1:3000` throughout (`§0`'s shadow, live), so every shot
asserted `location.host` before shooting. **The light theme rendered light** —
the first time on any screen. The data half read ~40 figures off the running
screens and re-derived each against **SQL written fresh from the S-rules over
`docker exec psql`** — a different driver, author and connection than the app,
so a disagreement names which side moved (`§35`/`§36`'s discipline). Eight
passes: typography · surfaces · light theme · RTL · 375 · archetypes ·
motion · the `facet-ui` skill against `DESIGN.md`. Per the audit brief,
`DESIGN.md` was **on trial**: every row below carries one of four verdicts —
*deviates-bad* (screen wrong), *conforms-bad* (**rule wrong**, with what it
should say), *deviates-good* (screen found better), or *question* (founder
call). Two states were unreachable and are named, not skipped: `/reports/[id]/edit`
(every seeded report is past `S38`'s same-day window) and `/users/[id]/handover`
(needs a deactivated subject; the seed keeps everyone active) — both `D53`
behaving. Verification note: one probe of this audit's own went green over a
defect — bidi fragment union-boxes said the `A2-19` header was ordered when the
pixels said otherwise — and a 32px crop is what failed correctly; the shape is
`CLAUDE.md`'s *a check can be wrong in the direction that matters*, met live.

**The ten that would most change how FACET feels, ranked:**

1. **Arabic dates are unreadable across the product** — every formatted date
   inside a `num dir="ltr"` run renders scrambled (*"062026/05/"* for May 6):
   the whole `/follow-ups` Since column, every stream day header and byline,
   notification metas, quotation timeline and next-follow-up dates (`A2-1`).
2. **The phone dashboard's Slipping rows lose their subject and their act** —
   no company name, and the Plan control clipped off-screen at x=416 (`A2-2`).
3. **"Text too small" is the value/label confusion, not the base** — 10.5–12px
   faint text carries row-unique values (day headers, bylines, line pricing)
   while `--text-faint` fails WCAG contrast in both themes; `D12` conforms and
   is wrong (`A2-3`).
4. **The light theme fails at the token level, fixably** — `--line` at 7%
   reads as disabled controls, `--surface-2` is white-on-white, the card edge
   composites into the canvas, amber and red converge (`A2-4`).
5. **The dashboard's Quoted figure is wrong** — dispatch-blind fold: rep reads
   9 for 8, manager 15 for 12 — and the same blindness has `/follow-ups`
   telling reps to chase customers who committed weeks ago (`A2-5`, `A2-6`).
6. **Sorting is missing because no rule asks for it** — six of seven lists
   have one fixed order; `D24` has no sort clause (`A2-7`).
7. **Every non-rep identity is told the team's work is personally theirs** —
   *"132 things need you today"* — and the Executive gets the whole rep screen
   `D68` says he never sees (`A2-8`, `A2-9`).
8. **The coordinator's named act is 1.5 folds below the panel that names it**,
   behind two always-open reason-textareas for the rare outcomes (`A2-10`).
9. **Arabic number-order defects form one class** — the pace-bar axis reads
   against its own bar, م² renders unit-first in six surfaces, KPI tiles pin
   the number 200px from its label — and `D73`'s one-question test approves
   the `dir` placements that cause them (`A2-11`–`A2-14`).
10. **"Text looks shadowed" has a named mechanism and a one-minute test** —
    every card carries `backdrop-filter`, which forces grayscale antialiasing
    on Windows; toggle reduced transparency and see (`A2-15`).

| What | Where | Disposition |
|---|---|---|
| **A2-3 · `D12` is obeyed and the founder is still right: the type scale confuses values with labels.** Census over six screens: tables read 14px and are fine; the DASHBOARD's dominant text is 12px+11.5+10.5 against 174 chars of 14px, and the stream's dominant size is **11.5px**. Row-unique VALUES sit at label sizes: stream day headers (data) at 10.5px, bylines ~11px, quotation line pricing ~12px faint. `--text-faint` computes **≈3.1:1 light / ≈3.6:1 dark — under WCAG AA's 4.5:1** — on those same elements. And the floor is breached twice outright: the qualified **"Q" chip and "FA" avatar render at 10.0px**. conforms-bad: the rule constrains a floor but not what may live near it | census in `scripts/audit2/` run, `rep-a/activity-stream-en-dark-1366.png`, `rep-a/quotation-detail-full-en-dark-1366.png`, `DESIGN.md D12` | `§6b` rewrite of `D12`: *a value unique to a row — date, money, count, name — never renders below 12.5px; the 10.5px uppercase tier is for repeated structural labels only; no text tier under 4.5:1 contrast in either theme*. Then session **47** applies it with the `--text-faint` lift. **PROPOSAL WRITTEN — see the `§6b` block below, awaiting the founder**; the four `text-[10px]` breaches of the CURRENT floor are session 47's regardless |
| **A2-7 · Sorting is missing because `D24` never asks for it.** Six of seven lists ship exactly one fixed order — `/quotations`, `/dispatches`, `/projects?view=table`, `/contacts`, `/follow-ups`, `/users`, `/targets` — and only `/companies` offers chips (*Needs attention · Name · Recently added*). Every fixed order is defensible (`D25` attention-first), which is why this is one rule gap and not six defects | `rep-a/quotations-list-many-en-dark-1366.png`, `DESIGN.md D24` | `§6b`: `D24`'s List archetype gains a sort clause — *a list offers its natural orders as chips, the `/companies` shape; at least the attention order and Name*. Founder question attached: which lists earn which orders? conforms-bad. **PROPOSAL WRITTEN — see the `§6b` block below, awaiting the founder** |
| **A2-9 · `D68` contradicts `D64`, and `D64` wins in code.** The Executive's screen: company target, four tiles, the full waiting list, team table, bottleneck card, and a Team rail item — because the role holds `sees_all_reps` (+ `can_manage_users`). `D68` promises *"the team table and nothing else"*; `D64` says *"My waiting list — always"*. Both cannot stand | `executive/dashboard-en-dark-1366.png`, `DESIGN.md D64 D68` | `§6b`: either `D68` is rewritten to say what the exec's flags produce, or `D64`'s *always* gains a condition. conforms-bad (one of the two). **PROPOSAL WRITTEN — `D68` is the wrong one; see the `§6b` block below.** The `D64` *always*-condition half is recorded there as the founder's question, joined with `A2-8`'s |
| **A2-14 · `D73` and `D62` both approve the placements that break.** `D73`'s one question (*is there a word in it?*) blesses `dir="ltr"` on the pace-bar legend (three bare figures — but the dir sits on a flex CONTAINER and reorders children), on date cells (bare figures — but ar-locale output carries RLM marks), and on number+unit cells. `D62` says values take `dir="auto"` — and on `/notifications` the auto sits on a BLOCK, so every Arabic value line flings to the far edge of a full-width card, ~1,000px from its label, in both locales | `DESIGN.md D73 D62`, `A2-1` `A2-11` `A2-12` sightings, P4's notifications crops | `§6b`, conforms-bad twice: `D73` gains *"a `dir` attribute never sits on an element that lays out more than one child, and never wraps locale-formatted text"*; `D62` gains *"`dir=auto` goes on the inline run, never on a block"*. **PROPOSALS WRITTEN — see the `§6b` block below**; the sweep itself is session 47's and applies the discipline without waiting |
| **A2-18 · The dashboard's fixed block order buries the team table at fold 3, and `D64` forbids helping.** For a manager the sequence target → requests → tiles → **uncapped planned rows** → slipping → team table puts `D39`'s block ~1,900px down; `D64` fixes the order and forbids per-person variation, and `D34` caps Slipping but not Planned. The founder's *"too much scrolling"* quantified: detail screens 2.1–2.9 folds at 1366, `/notifications` spends 2.5 folds on 13 flat undated rows | `manager/dashboard-en-dark-1366.png`, `rep-a/notifications-en-dark-1366.png`, manifest scrollHeights | **Folded into the dashboard conversation (SPEC §16), session 50** — `D64`'s book-holder narrowing removes the manager's personal blocks entirely, which answers the fold-order half; the rest (including `/notifications` day grouping) waits for the conversation. As it stood: cap Planned like Slipping? Let flag-added blocks render above the always-blocks? |
| **A2-22 · `D15`/`D16`'s six-use lists are fiction at the named-use level.** Gradient: 2 of 6 real — the active rail marker is a 5% white wash, the rail count badge flat `--red-600`, the rollup unbuilt, the row-hover fill nonexistent. Glow: 1 of 6 — app mark, pace ring, week-strip cell, target bloom, chain-dot ring all carry none (the chain dot rings `tone-amber`). And the app mark + avatar carry **inline hex gradients** (`app-rail.tsx:237,334`) — a hex typed into a component, `facet-ui`'s own *Never*. P2's census otherwise: no nested cards, no stray glass, no stray gradient or glow — `D21` clean | any dashboard shot; `app-rail.tsx:194,152,237,334` | `§6b`: either session 46 builds the four missing uses or the counts are rewritten to what is actually wanted emphasised; the two inline gradients become tokens either way. conforms-bad. **PROPOSAL WRITTEN — counts to two and one; see the `§6b` block below.** The hexes-to-tokens half is session 47's either way |
| **A2-23 · `D17` names motion that has never existed and a test path with nothing to test.** Compiled CSS holds **zero** `prefers-reduced-motion` rules and one `@keyframes` (`spin`, no user); no view fade, no 6px rise, no tile lift, no gradient hover fill. What ships: button/badge colour transitions, `active:translate-y-px`, and an untransitioned hover shadow snap on cards. Nobody has missed it — the stillness suits `D1`'s work tool | compiled `.next` stylesheet; `AD26` | `§6b`, deviates-good: rewrite `D17` to name what ships and delete or re-subject the reduced-motion sentence. `D19`'s `prefers-reduced-transparency` path IS in the compiled output — that half conforms. **PROPOSAL WRITTEN — see the `§6b` block below**; the dead `tw-animate-css` import is session 47's either way |
| **A2-24 · Register and marker hygiene from this audit's sightings.** (1) `D35` (week strip) and `D36` (Recently) describe blocks with no component and carry no `[BUILD]`. (2) `D26`'s quotation lead-cell clause (*"an avatar and whose move — 'Rawan · signatures'"*) was superseded by session 26's pile-plus-position anatomy (`AD16`) and never rewritten. (3) `§5`'s *"D29 draws a `paid` column…"* row is stale — `chain.ts` carries no paid rung and the board renders `S132`'s six; the decision that row waited on has effectively been taken. (4) The dispatch difference fact renders its label twice (a `dt` *"Differs from the quotation"* over a Badge with the identical string) | `DESIGN.md D26 D35 D36`, `§5` above, `dispatches/[id]` | **All four DONE, session 47**: (1) `D35`/`D36` carry `[BUILD]`; (2) `D26`'s clause is in the rewrite proposal; (3) the `paid` row above is closed against `S133`'s slice; (4) the difference `dt` is a neutral *Difference* label (`dispatches.difference.label`, both locales) over the badge that states the claim |
| **A2-25 · Two taste calls only the founder can settle, written as questions.** (1) The silence meter caps at 100%, so the GONE QUIET pile draws identical full bars from 74 to 117 days — deliberate (the component says a bar past its end reads as a fault); should it scale past threshold so worst-first is visible in the cell? (2) No wait-threshold exists for the queues, so the coordinator's 111-day *your move* rows read as calm as 3-day ones (`D6` colours only where a threshold exists, and only `/follow-ups` has them); should her queue take the 5-working-day colour? (3) P1's phrase-mono question: does `num` stop at digit+unit, or is phrase-mono (*"of 800 m²"*, *"Due today"*) the intended voice? `D11` should name the boundary either way. (4) Arabic at equal px reads a step denser than English (Plex Arabic tooth height) — bump AR sizes one step, or accept? | `rep-a/companies-list-many-en-dark-1366.png`, `coordinator/quotations-list-many-en-dark-1366.png` | Founder, at the screen, beside `AUDIT 2`'s rule review |
| **A2-26 · The data half's green side, so the next audit knows what held.** Reconciled exactly against independent SQL: rep 830/800 and Jul 2,438 of 900 (271%) · bell 13 · tiles 4 and 13 (the 13 accounted: 15 stale − 1 parked − 1 owned by its own date, `S90` behaving) · company 4,419/6,000 (74%) and 7,410/5,000 (148%) · team rows 830/800 · 2,594/3,200 · 995/1,000 with quiet 35 = the book scope `D39` names · bottleneck 17/5 · `/quotations` piles 20/1/12 of 33 · `/dispatches` 2/2/21 of 25 with the two cancelled behind `?archive` · board 2/8/4/1/1/8 + 3 lost off-board · stream: nine day headers AND the 406 total against a six-source SQL sum · `/targets` incl. the 679 quoted/151 direct split · line arithmetic (qty×w×l at 4dp, VAT exactly 15%, sums) · `S77`'s 537/523/523 with an honest flag. **Named as not independently derived**: `catalogue_no_response` (3) and `quotation_returned` (2 of the 9) — audit-log + working-day folds taken on the app's word; logged-this-week and waiting-on-them columns (`§35` covers the m²); by-rep counts (`§25`'s family) | `scripts/audit2/fetch.mjs` + `docker exec psql` | Reference row. The two reds it produced are `A2-5` and `A2-6` |

---

**`§6b`'s FIRST RULE REVIEW — session 47's proposals.** ~~Nothing below is
applied.~~ **The founder returned a verdict in session 49 and SIX were applied
there** — `D12` `D15`/`D16` `D17` `D26` `D62` `D73`, each carrying a dated
rewrite note in `DESIGN.md`. ~~**TWO are HELD, still awaiting him**~~ **Both HELD items
closed in session 50**: `D24`'s sort clause DECLINED (sorting waits for the
pilot; his question was form field order, whose process is now decided) and
`D68`/`D64` APPLIED (book-holder narrowing, from his answer). The `D52` rider
alone stays HELD — the empty-fact widening was not put to him and `D52` keeps
its `[CHANGE]` marker. Below is the proposal text as
written; the verdict is marked on each. Seven rules (`D15`/`D16` counted as one), reviewed per
`§6b`'s three questions, each given one of its four verdicts. Every claim below
was re-verified against the code on 1 Sep 2026 — an eight-reader pass over the
current tree, not the audit's shots alone — and where the audit's evidence had
drifted, the fresher number is used and said. **The rewrites are their own
slice, after approval**; work blocked on each is named so nothing ships against
an unapproved rule. Two riders the register assigned to this review (`D52`,
`A2-24`'s hygiene) close the block.

**D12 — REWRITTEN. ✅ APPLIED session 49.** **The load-bearing one.** What it says: a scale, a
floor of 10.5px, weights. What the screens do: tables read 14px and are fine
(`A2-3`'s census) — but the 10.5–12px faint tier carries row-unique VALUES
(stream day headers at 10.5px, bylines at 11.5px, quotation line pricing at
12px), `--text-faint` computes ≈3.1:1 light / ≈3.6:1 dark against its ground —
under WCAG AA's 4.5:1 in BOTH themes — and the floor is breached at four sites
in two kinds (`text-[10px]`: the qualified chip, and one initials-avatar class
in three files). The base is set NOWHERE — no `font-size` on `body`, so 14px is
a per-component achievement and an unsized element renders 16px — and the page
title is two numbers (`PageHeader` 24px, `DetailHeader` `text-[25px]`). **The
founder has now given this tier its second witness**: `A2-15` refuted the glass
at OS pixel level on his machine, so *"text looks shadowed"* lands on the same
faint tier `A2-3` measured — faint under contrast reads as broken rendering,
not hierarchy. The rule was right about its floor and silent about everything
that matters near it. Proposed text, keeping the number:

> **D12.** Scale: base 14px / 1.5, **set once on `body`** — the ground, not a
> per-component achievement; an unsized element must never render 16px. Page
> title 24px / 600 / `-.025em`, and there is ONE page-title size —
> `PageHeader` and `DetailHeader` share it. Card heading 14px / 600. Section
> labels 10.5px / 600 / `.09em` / uppercase — **and that tier is for repeated
> structural labels only: a value unique to a row — a date, a name, money, a
> day count — never renders below 12.5px.** A reader must READ a value; a
> label is recognised by shape. Nothing is smaller than 10.5px, ornaments and
> avatars included. **And a size floor is not enough: no text tier renders
> under 4.5:1 contrast against the surface it sits on, in either theme.** The
> floor binds the TOKEN — `--text-faint` is the tier this catches — checked
> where the token is defined, because faint text under contrast does not read
> as hierarchy, it reads as a rendering defect: *"shadowed"* was this tier,
> not the glass (`A2-15`). Weight 600 for names and headings, 500 for
> emphasis, 400 for everything else.

Blocked on approval: the `--text-faint` lift in both themes, the `body` base,
one title size, the ≥12.5px pass over day headers / bylines / line pricing.
NOT blocked (breaches of the CURRENT rule, taken by session 47): the four
`text-[10px]` sites.

**D17 — REWRITTEN. ✅ APPLIED session 49**, and it is the deviates-GOOD case. What it says:
a view fade + 6px rise at 350ms, a 2px tile lift, a gradient hover fill —
"the complete list" — and `prefers-reduced-motion` "respected and tested."
What ships: **none of the three**, zero `@keyframes` in `src`, zero
`prefers-reduced-motion` anywhere — and what DOES move is not in the list:
`transition-colors` at nine sites, `transition-all` on Button and Badge, a 1px
`active:` press on every button, `hover:brightness-110` on the primary, an
untransitioned hover shadow swap on cards. The rule was never true — written
from the v5 concept before anything existed — and four audits in, nobody has
missed the motion; `button.tsx` even cites `D17` to REFUSE a hover translate
while shipping a press translate `D17` does not name. The stillness is the
design. Proposed text:

> **D17. FACET is still.** No page transitions, no view fades, no scroll
> reveals, no skeleton shimmer, no counting numbers, no hover lifts —
> stillness is the work tool's temperament `D1`, and it was chosen at the
> screen: every session that could have added motion did not, and nobody has
> missed it. What may move: **colour and brightness over a short transition**
> on interactive controls, and the **one-pixel press-down** on an active
> button — feedback that a press landed, never decoration. A card's hover
> shadow swaps states without a transition `D8` `D14`. That is the complete
> list, and `D21`'s last clause holds anything beyond it to this rule. With no
> motion larger than a pixel there is nothing for `prefers-reduced-motion` to
> gate — the design is its own reduced-motion path. `D19`'s
> `prefers-reduced-transparency` handling is separate, real, and stays.

Rides with it (code, session 47, either way): the `tw-animate-css` import in
`globals.css:2` has zero consumers — dead structure, out.

**D15/D16 — REWRITTEN. ✅ APPLIED session 49**: the counts become what is real. Measured
consumers today: `--brand-grad` at **two** uses — the primary button, and the
target bar's fill (the dashboard panel and `D39`'s team-table mini bars are
one fill; the reduced-opacity overage segment is that fill continued, recorded
in `page.tsx` as "not a seventh use") — against `D15`'s six: the active rail
marker is a 5% wash (`bg-rail-active`), the count badge flat `--red-600`, the
rollup unbuilt, the row-hover fill nonexistent. `--brand-glow` at **one** use —
the primary button — against `D16`'s six: the app mark carries none, the pace
figure is a plain `<p>` ("no tone on any of the three", its own comment), no
week strip exists, the bloom was REFUSED in writing (`page.tsx`: "D16 permits
one on this fill and nothing requires it"), and the chain dot rings
`tone-amber`. **Every divergence was a session choosing the quieter option at
the screen** — that is evidence about what the design wants, and the list-form
discipline (`D8`'s preamble) survives with honest lists. Proposed text:

> **D15. The brand gradient has exactly two uses.** `--brand-grad` on the
> primary button, and on the target bar's fill — the dashboard panel and
> `D39`'s team-table bars are the one fill, and the overage segment at reduced
> opacity is that fill continued, not a third use. Nowhere else: not on text,
> not on pills, not on borders, not on the rail. The rule named six from the
> concept; four never shipped, and every session that met one chose the
> quieter option at the screen — the active rail marker is a 5% wash, the
> count badge flat red — until the list described nothing. A new use is added
> by amending this rule first, never by citing it.

> **D16. Glow has exactly one use.** `--brand-glow` on the primary button.
> Nothing else glows: the chain dot rings `tone-amber` `D27`, the pace figure
> is plain text, and the target fill's bloom was considered at the screen and
> refused. A glow is emphasis, and everything emphasised is nothing
> emphasised — one glow means the act a screen most wants taken is the one
> thing that ever carries it. A new use is added by amending this rule first.

NOT blocked (session 47, either verdict): the inline hex gradients become
tokens — the app mark (`app-rail.tsx:237,264`) and the two avatar gradients
(`app-rail.tsx:334`; the `#31527F,#1B2F4C` pair in four files) — the skill's
own *never type a hex into a component*, `A2-22`'s "become tokens either way."

**D24 — sort clause DECLINED, session 50.** The founder: sorting waits for
the pilot — *"the others wait until a real rep says 'I can't find X'"* — and
his real question was FORM FIELD ORDER, now its own `§5` row with a decided
process. No clause is added; the proposal below stands as the record. **The archetypes stand**; the List archetype would gain a
sort clause. Six of seven lists ship exactly one fixed order and only
`/companies` offers chips (`?sort=`: attention · name · recent, resolved in
SQL before the LIMIT). Every fixed order is individually defensible `D25`,
which is why this is ONE rule gap and not six defects (`A2-7`). Proposed
addition to the List archetype's paragraph:

> A list offers its natural orders as **chips** — the `/companies` shape:
> `?sort=` in the URL `D20`, resolved in SQL before pagination, defaulting to
> the attention order `D25`. At least the attention order and Name wherever a
> name exists. **[BUILD]** — which lists earn which orders is the founder's
> pass, recorded here when taken.

FOUNDER QUESTION attached (from `A2-7`): which of `/quotations`,
`/dispatches`, `/projects?view=table`, `/contacts`, `/follow-ups`, `/users`,
`/targets` earn chips, and which orders each.

**D26 — REWRITTEN. ✅ APPLIED session 49. The `§6b` trigger case: argued past
three times.** The three, each in its own slice, none asking whether the rule could
be satisfied at all: the project clause's quoted-vs-dispatched bar is refused
by `S68` (*quotations are never summed* — no numerator exists); the quotation
clause's two worked examples both name things the model cannot produce
("Rawan · signatures" — no thread names a coordinator; "You · confirm
payment" — `S133` deleted the act and its key); and the dispatch clause
answered two of five states until `AD33` widened it. What ships instead, per
the current tree: companies lead with the silence meter (right, built);
projects lead with **days since it last moved** (red when stale `S89`), whose
move in the Turn column; quotations lead with an avatar **only when the rep
owes the move** plus the days waited, the PILE header carrying whose-move
(`AD16`); dispatches lead with the square metres; contacts lead with the name
alone (position is its own column). Proposed text, keeping the number and the
dispatch clause's post-`AD33` paragraph:

> **D26.** Each object type has its own **first column** — a visual that
> answers that object's question before a word is read. Same table component,
> different lead cell.
>
> | Object | Lead cell | The question it answers |
> |---|---|---|
> | **Company** | a **silence meter** — a small bar and a day count, coloured by lateness | have I neglected this? |
> | **Project** | **days since it last moved**, mono, red once `S89` calls it stale | is this stuck? |
> | **Quotation** | the days waited, with the rep's avatar **when the move is theirs** — the `D25` pile header names the move, so the row never repeats it | who does this wait on, and how long? |
> | **Dispatch** | the square metres, mono, large | how much went out? |
> | **Contact** | the name | who is this? |
>
> The first writing of this table promised a six-dot mini-chain and a
> quoted-vs-dispatched bar for projects, and a named coordinator for
> quotations. **Three slices met those clauses and each declined them for a
> reason that held**: `S68` refuses the bar's numerator, no thread names one
> coordinator, `S133` deleted the act the example named. A lead cell drawn
> from data the model cannot produce is fiction; this table now states only
> what a row can know.

The register's two `D26` rows close against this proposal when approved. If
the founder wants the six-dot mini-chain anyway, it is buildable (a `mini`
variant of `ChainStrip`, which today has none) — but it replaces a lead cell
that already answers `D2`, so it is his call, not a defect.

**D62 — WIDENED. ✅ APPLIED session 49**, and it closes `AD18` in the same
breath. The
rule is right and under-specified in the one direction that breaks: on
`/notifications` the `dir="auto"` sits on a full-width `<p>` that also carries
`text-start`, so the paragraph's direction — and its alignment — resolve from
the VALUE: a Latin record name slams to the far edge of an RTL card, ~1,000px
from its label (`A2-14`, the register's third sighting, mechanism now
confirmed in code: eight block-level `dir="auto"` sites exist, five carrying
`text-start`). And `D62`'s last sentence cites "the `dir="ltr"` rule", which
`DESIGN.md` did not contain when `AD18` was filed — `D73` exists now and is
that rule. Proposed text, first two sentences unchanged:

> **D62.** A value that may hold either script carries `dir="auto"`, wherever
> it is entered or displayed — direction is a property of the value, not of
> the page. Since `S12` and `S19` a company or contact name is one field
> written in English or Arabic, so an Arabic name must read correctly on an
> English page. **`dir="auto"` goes on the inline run holding the value —
> never on a block.** On a block it does two things at once: it resolves the
> block's direction from the value and drags `text-start` with it, so a Latin
> value inside an RTL card slams to the card's far edge, a column away from
> its label (`A2-14`). Wrap the value — `<span dir="auto">` or `<bdi>` —
> inside a block that keeps the page's direction. This is the converse of
> `D73`, the rule for runs FACET composes and for raw LTR content —
> references, decimals — inside Arabic.

NOT blocked: the `dir="auto"` sweep itself applies `D62` as it stands (the
run-not-block discipline is how the sweep is done correctly, not a new
requirement on stored values), so session 47 takes it.

**D73 — WIDENED. ✅ APPLIED session 49**: the container clause, from `A2-14`'s
two live mechanisms. The rule's one-question test approves the placements that break:
the pace-bar legend is three bare figures — but the `dir="ltr"` sits on a
`flex justify-between` CONTAINER, so the AXIS reorders against its own bar
(`A2-11`, confirmed at `page.tsx:711`); the KPI tile number is a bare figure —
but the `dir="ltr"` sits on a BLOCK `<p>`, and `text-align: start` resolves
against the element's OWN direction, pinning the number to the tile's left
edge in Arabic, 200px from its label (`A2-13`, confirmed at
`page.tsx:345-350` and the `SideFigure`). Proposed addition, after the
formatted-date paragraph:

> **A `dir` sits on an inline run holding one value — never on an element
> that lays anything out.** A flex or grid container carrying `dir` reorders
> its CHILDREN — the pace-bar axis read against its own bar. A block element
> carrying `dir` drags `text-align: start` to its own edge — the KPI number
> pinned a tile's width from its label. The legitimate carriers are a
> `<span>` or `<bdi>` around one figure or reference, and a form control's
> own value. And a number+unit run FACET composes — *948.3 م²* — is a WORD
> run under this rule's own test: the unit is translated text, so the run
> takes `dir="auto"`, never `dir` on the cell (`A2-12`).

NOT blocked: `A2-11`/`A2-12`/`A2-13`'s fixes are the same run-not-container
discipline applied to existing values — session 47 takes them as the sweep's
second half.

**D68 vs D64 — RESOLVED, session 50. ✅ APPLIED.** The founder answered the
residual question: only company-book holders get the personal list, `D68` is
rewritten to what the flags produce, and the overseer dashboard's exact
contents are the open conversation (SPEC §16). The proposal below was the
right direction and is superseded by the applied text. The proposal below
says `D68` is the wrong one and REWRITES it, with the residual question his. `D68` promises the
Executive "the team table and nothing else"; `D64` says the waiting list
renders always, `sees_all_reps` adds the team table and the bottleneck card,
and a company target row adds the panel — and the code follows `D64` to the
letter (verified: every block gates on a flag or data, no role name appears in
any condition in `src`, and the Executive's seeded row — `sees_all_reps` +
`can_manage_users` + `can_set_targets` among others — produces exactly the
four blocks `A2-9` photographed). **`D68` as written is unsatisfiable
lawfully**: the only mechanism that could give one ROLE a different screen
under `D64`'s fixed model is a role name in code, which `CLAUDE.md` forbids.
It was written from the v5 concept as a role sketch — "short because nobody
holds the role yet" — before `D64`'s flag model existed, and was never
reconciled. Proposed text:

> **D68. The Executive holds a flag row, not a screen of his own.** What his
> flags produce today: `sees_all_reps` gives him the team table, the
> bottleneck card, and the target panel at company scope when a company
> target is set `S136`; the waiting list renders for everyone `D64`. If that
> is more screen than his question — *how is the company doing*, not *what is
> waiting* — the lever is his ROLE ROW and `D64`'s conditions, never a role
> name in code (`CLAUDE.md`).

FOUNDER QUESTION, recorded not decided — and it is `A2-8`'s question wearing
`D68`'s clothes: should `D64`'s *"My waiting list — always"* become *"when
the person holds a company book"* — a data condition the system already knows
(`S9`'s four holders), which would remove the waiting list, the greeting's
*"132 things need you today"* and *"Your day 0·0·0"* from the Executive AND
the Super Admin in one move, and make every possessive on the dashboard
honest? That is a who-sees-what change: his, not this review's.

**Rider — `D52` WIDENED (proposed). ⏸ HELD — NOT applied**, and `D52` keeps
its `[CHANGE]` marker. From the register's own row. The
dispatch detail's *"Recorded when it is approved"* (session 41) extended
`D52` to a Facts grid without the rule saying so, and every detail screen has
fact cells absent by rule rather than unfilled. Proposed addition: **"A fact
absent by RULE says so. A dash reads as *missing*, which is a different claim
from *not yet asked* — an absence the rules guarantee renders as the sentence
naming what would fill it: *Recorded when it is approved* `S73`."** And per
`AD21`, `D52` gains its honest marker now (applied by session 47, since a
marker records reality rather than changing a requirement): the action half —
*"and offers the action"* — is unbuilt in all 21 empty states.

**Rider — `A2-24`'s hygiene, dispositions.** (1) `D35`/`D36` gain `[BUILD]`
markers — session 47 applies, factual. (2) `D26`'s stale quotation clause is
covered by the rewrite above. (3) The `§5` *"`D29` draws a `paid` column"*
row is closed by evidence: `CHAIN_COLUMNS` is `S132`'s six, no `paid` rung
exists, and `D29`'s own text already reads *"the board's columns are `S132`'s
six positions"* — the decision that row waited on was taken when `S133`
removed the rung; session 47 closes the row in place. (4) The doubled
difference label is a one-liner, session 47.

---

An audit is its own session, produces a findings list, and **changes nothing**.
Fixes are separate sessions afterwards.

**AUDIT 1 — the model.** Does the database say what `SPEC.md` says? Every table
and column against `S12`–`S129`. No column without a writer. No flag without a
reader. All verify scripts green. And: do the `[CHANGE]` markers for phase 1
now read as plain rules? **An audit that stops at `S85` does not look at the
dispatch and payment rules at all** — that was the old range, and it now misses
`S116`–`S129` entirely.

**AUDIT 2 — the interface.** Every screen against `DESIGN.md`, in both themes
and both locales, at 1366px. The `D21` checklist run by eye on each. Does any
screen still not answer `D3`'s question in five seconds? Is the waiting-list
predicate resolved in SQL before pagination — check the query, do not trust the
screen. That predicate now resolves **dispatch requests sitting with the
coordinator** (`S89`) alongside the other three kinds — a different join, and
exactly the shape §7 warns fails silently.

**AUDIT 3 — pre-pilot.** The full route walk with realistic data volumes. A
real phone in a real hand. Restore the backup onto a second machine and confirm
it comes up. Deactivate a user and confirm their session dies. Then the five
questions from `D3`, `D25` and `D31`, asked of a rep who has never seen it.

---

## 6b. The rule review

The three audits in `§6` ask whether the code matches the rule. **Nothing asks
whether the rule is still right.** `D20` is why this section exists: it was
correct when it was written, and became a constraint on things nobody had
thought of when it was written. Three sessions argued with it before one
rewrote it.

**What it asks of each rule** — three questions, in order:

1. **Does the thing it constrains still exist?** A rule about a mechanism that
   has been replaced is not a rule, it is a fossil.
2. **Is it a boundary or a waiver list?** A rule that names exceptions makes
   every new case an argument about whether it earns another one. A rule that
   names a test answers cases nobody has thought of. **The tell is a session
   asking permission rather than applying the rule.**
3. **What would it forbid that we now want, and what breaks if it goes?** A rule
   forbidding nothing anybody wants is dead weight. A rule whose reasons still
   hold is **rewritten, never loosened**, and the reasons are written into it —
   which is what stops a rewrite reading as permission.

**How often.** Once beside each audit in `§6`, over the file that audit has just
read — `SPEC.md` with `AUDIT 1`, `DESIGN.md` with `AUDIT 2` — and at any point a
rule has been **argued past twice**. The second argument is the trigger. `D20`
reached three.

**What it may conclude — four things, and no others.**

- **Stands**, recorded with the date, so the next review does not re-open it.
- **Rewritten**, keeping its number and its reasons, and stating a test rather
  than a list.
- **Narrowed or widened**, with what changed in the world written into the rule.
- **Deleted**, the number kept as a tombstone and never reused — `D18`'s
  precedent — so no citation shifts.

It may not invent a rule for something nobody has asked for, and **it changes no
code**: `§6`'s discipline holds here too. Its output is a list of rule numbers
and which of the four each got. The rewrites are their own slice.

---

## 7. Rules that keep this from going wrong again

- **No third-party agent frameworks or swarm tooling.** claude-flow was
  installed twice by accident and removed twice, and its memory store loaded
  into twenty sessions unaudited. It is now **hook-blocked by name** (H11),
  and writes outside the repository are hook-blocked too (H1). The word
  "hooks" in this rule's old text meant THAT framework's hook system —
  Claude Code's own hooks and subagents are the platform's sanctioned
  mechanisms and are in use since session 50.
- **New decisions go into `SPEC.md` or `DESIGN.md`.** Never a new document
  (hook-enforced, H2: a new `.md` outside the sanctioned locations is denied
  at the write). The 27 in `docs/archive/` exist because this rule did not.
- **When a rule replaces an old mechanism, the old mechanism comes out in the
  same slice.** Otherwise you end up with both.
- **A rule that is only partly built says so in its own text.** A marker is one
  bit and a rule can be three quarters done — `S118` read as finished while two
  of its three clauses were unbuilt. `S81` and `S129` are the model: they name
  which half is true today. The marker says whether work remains; the sentence
  says what.
- **Derived conditions are resolved in SQL, before pagination.** This has
  already shipped broken once, and it fails silently.
- **One task per session**, and the plan reviewed here before approval.
- **There is no production data.** Every row in every database is a fixture or
  verify residue. A migration never preserves, backfills or merges data — it
  clears, and `db:reset` is always available. Where clearing is not available
  — a NOT NULL column losing an enum value, a row whose deletion would break an
  invariant — the migration may map onto a value the surviving vocabulary
  already names, and the header says why. That is not a backfill: nothing is
  being preserved that a reset would not recreate. This stops being true at the
  pilot, and this line comes out then.

---

## 8. Repository access at milestones

The repo is **private**. `legacy/` contains real colleague names and email
addresses, so it stays that way.

At each audit point, Jerom flips it public for the duration of the review, and
back to private afterwards. Claude clones it, reads the whole tree, and reports.
This has been done once already and works cleanly. Between audits, the status
block below is enough.

---

## 9. The status block

Run this at the end of a session and paste the output. It is what Claude uses
instead of repository access.

```powershell
cd C:\Projects\facet-crm
Write-Host "=== BRANCH & LAST COMMITS ==="
git log --oneline -5
git status --short
Write-Host "=== ROOT ==="
Get-ChildItem -Name -File
Write-Host "=== ROUTES ==="
Get-ChildItem -Recurse src\app -Filter page.tsx | ForEach-Object {
  $_.FullName.Replace("$PWD\src\app\","").Replace("\page.tsx","") }
Write-Host "=== TABLES ==="
Select-String -Path src\db\schema.ts -Pattern "^export const (\w+) = pgTable" |
  ForEach-Object { $_.Matches[0].Groups[1].Value }
Write-Host "=== SIZE ==="
(Get-ChildItem -Recurse src -Include *.ts,*.tsx |
  Get-Content | Measure-Object -Line).Lines
Write-Host "=== OPEN MARKERS ==="
# SPEC.md:10-11 are the legend, not rules.
@(Select-String -Path SPEC.md -Pattern '^(?!- \*\*\[).*\[CHANGE\]').Count
@(Select-String -Path SPEC.md -Pattern '^(?!- \*\*\[).*\[BUILD\]').Count
```

The last two numbers are the real progress bar. The count started at **26
`[CHANGE]` and 24 `[BUILD]` — 50 open**, rose to **55** when the dispatch and
payment rules were written, and falls as they ship. A rise is only wrong when no
rule was added.

Better still, add the script to the repo once (see §11) and run `npm run status`.

---

## 10. Current state, verified from the repository

**A snapshot, not a live reading.** Every figure below was taken by hand at the
commit named, and none of it updates itself. Re-take it rather than trusting it;
the previous version of this table was carried forward until nearly every row
disagreed with the repository, one of them a corrupted cell sitting beside a
number people trust.

Taken from `main` in session 50 (THE FIXATION), 1 Sep 2026 — the whole table
re-measured rather than patched, which is what the paragraph above asks for.

| | |
|---|---|
| Branch | `main`, clean at each of the session's six pushed commits |
| Authority files | `CLAUDE.md` **125** lines (was 332 — it is an INDEX now) · `SPEC.md` 1,255 · `DESIGN.md` 1,672 · `WORKFLOW.md` 1,130 · `README.md` 285 (the entry document) — re-counted session 52 |
| Rules | **142 `S` numbers · 81 `D` numbers**, contiguous (re-counted session 52: `S139`–`S142` and `D76`–`D81` arrived in 51; 52 added none and closed `S139` `S140` `S142` `D44`). Tombstones: `D18` `S115` `S135` `D35` `D36` `D71` — deleted rules keep their numbers, never reused |
| The layers | `.claude/rules/` six path-scoped files (proven to load on-path and not off) · skills `facet-ui` `facet-verify` `facet-audit` `facet-register` · agents `classifier` `conformance-sweeper` `shot-looker` · hooks H1–H11 + permissions P1–P7 (each fed its defect) · `facet-plugin/` the generated mirror (`build-plugin.mjs --check` holds it) |
| Docs | `docs/archive/` holds 27 numbered documents plus `INVENTORY.md`, `28-fixation/` (this rebuild's record) and `29-closed-register.md` (§5's 124 closed rows, moved verbatim) |
| claude-flow | fully removed — and hook-blocked by name (H11) |
| Routes | **37** — 36 under `(app)`, plus `(auth)/login` |
| Migrations | **35** `.sql` files under `drizzle/` |
| Tables | **43** exported `pgTable`s. Two lost their keeper rule in session 50 and are sweep drop candidates: `attachments` (S115 deleted) and `product_specifications` (§16 closed) |
| Register | **~90 open rows in §5** (prose context lines included in the count); 124 closed rows in `docs/archive/29` |
| Open markers | **10 `[CHANGE]`, 24 `[BUILD]` — 34 open** on `npm run status`'s reading, 2 Sep 2026 (it counts `SPEC.md` alone). Session 51 added `S139`–`S142` and `S138`'s rewrite as BUILD; session 52 closed `S139` `S140` `S142` in SPEC and `D44` in DESIGN, which the script does not count — `D72` is DESIGN's one remaining BUILD. A rise is only wrong when no rule was added |
| Blocking | **nothing.** The stale SPEC §15 Docker line is deleted; bulk import (34) is the pilot's precondition |

**How the code figures were measured.** `wc -l` over every `.ts` and `.tsx`
file. **`npm run status`'s own `SIZE` line is a different number** — 41,157
against 43,986 over the identical 157 files — because `Measure-Object -Line`
does not count blank lines. Neither is wrong; they are not comparable, and a
later session comparing one against the other would read a drift that is not
there.

**Session 24 is larger than it looks.** It deletes `follow-ups.ts` (1,522) and
`notifications.ts` (1,190), and with them `verify-phase10a.ts` (1,540) and
`verify-followups.ts` (1,132) — **5,384 lines** — plus the two routes,
`/follow-ups` (270) and `/notifications` (385). Against a much smaller waiting
list. Expect that, or it will look like something went wrong.

~~**Session 1 head start.**~~ **Spent.** The Docker build was fixed in `d7a7e7a`
+ `c00bcc9` by deferring the Auth.js config so `AUTH_SECRET` is not read at
module load — the import-time environment read this note was hunting. The three
ruled-out causes it listed are kept nowhere, because the cause is known.

---

## 11. Repository housekeeping

~~Four small changes. Do them in one commit before session 1.~~ **All four are
done** — `835909c` and `bd3a943`. Kept as the record of what was changed and
why, not as work outstanding: `.claudeignore` holds all three paths,
`.dockerignore` holds the three authority files, `INVENTORY.md` is in
`docs/archive/`, and `scripts/status.ps1` is wired to `npm run status`.

**1. Stop Claude Code reading the archive by accident.** `.claudeignore`
currently holds only `legacy/`. Add the archive, so the rule in `CLAUDE.md` is
enforced by the tool rather than by good intentions:

```
legacy/
docs/archive/
docs/design/archive/
```

**2. Keep the authority files out of the build image.** Add to `.dockerignore`:

```
SPEC.md
DESIGN.md
WORKFLOW.md
```

**3. Move `INVENTORY.md`** to `docs/archive/` — its content is in `SPEC.md` now.

**4. Add a status script** so §9 is one command. Save as `scripts/status.ps1`:

```powershell
Write-Host "=== BRANCH & COMMITS ==="
git log --oneline -5
git status --short
Write-Host "`n=== ROUTES ==="
Get-ChildItem -Recurse src\app -Filter page.tsx | ForEach-Object {
  $_.FullName.Replace("$PWD\src\app\","").Replace("\page.tsx","") }
Write-Host "`n=== TABLES ==="
Select-String -Path src\db\schema.ts -Pattern "^export const (\w+) = pgTable" |
  ForEach-Object { $_.Matches[0].Groups[1].Value }
Write-Host "`n=== SIZE ==="
(Get-ChildItem -Recurse src -Include *.ts,*.tsx | Get-Content |
  Measure-Object -Line).Lines
Write-Host "`n=== OPEN MARKERS ==="
# SPEC.md:10-11 are the legend, not rules. A rule's marker never opens a
# bullet, so excluding "- **[" is what makes these counts rule-level.
"CHANGE: " + @(Select-String -Path SPEC.md -Pattern '^(?!- \*\*\[).*\[CHANGE\]').Count
"BUILD:  " + @(Select-String -Path SPEC.md -Pattern '^(?!- \*\*\[).*\[BUILD\]').Count
```

Then in `package.json` scripts:

```json
"status": "powershell -NoProfile -File scripts/status.ps1"
```

`npm run status` at the end of every session. Paste the output into chat.
