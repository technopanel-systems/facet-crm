/**
 * Drizzle schema.
 *
 * Intentionally empty. Tables are designed in phase 5 (docs/05-roadmap.md),
 * starting with the shared core — organizations, people, users, roles,
 * permissions, notifications, audit — before any module builds on top.
 *
 * Do not add tables here ahead of a rule written down in docs/.
 */

export {};
