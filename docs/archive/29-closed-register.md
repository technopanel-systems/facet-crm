# The closed register — WORKFLOW §5's history

Moved here in session 50 (THE FIXATION). Every row below is CLOSED — the
finding, the measurement and the closure are preserved verbatim, single-line
rows as they stood. Open rows live in `WORKFLOW.md` §5. This file is
history, never authority (`CLAUDE.md` § Authority), and rows are appended by
later sessions moving their own closures — never edited.


## Rows from the main register

| What | Where | Disposition |
|---|---|---|
| ~~**`verify:phase9` §11 fails four checks on a clean tree, and every one of them is day arithmetic reading LOW.**~~ **Closed with `S46-1` in session 47 — one mechanism, and it was NEITHER of this row's hypotheses.** The four failures were `coverage.ts` reading "today" as **`current_date`** — the server's UTC day, one behind Riyadh between 00:00 and 03:00 — against fixture days the script computes Riyadh-correctly, so the row's clean-tree sightings and `S46-1`'s day-1 sighting were the same defect at different hours: the failures follow the CLOCK of the run, not the day of the seed. Not seed time-shifting, not residue, and not a regression — `companySilence()` carried it from birth, silent on every daytime run. The "one experiment" this row prescribed was never run because psql at 00:45 Riyadh settled it faster: `current_date` read `2026-08-31` beside `(now() at time zone 'Asia/Riyadh')::date` reading `2026-09-01`. The guidance below — *read §11 as environmental unless §1–§10 also move* — retires: a red §11 is a real red now. Measured in `38c`: *a company logged yesterday reads back `daysSince=0`*, *a never-logged company at 61 days reads `quiet=false`*, *at 61 days it is quiet* fails, and *a qualified company at 31 days against its own 30-day threshold reads `quiet=false threshold=30`* — the threshold is read correctly and the age is not. **Proven unrelated to the slice**: the tree was `git stash`ed to `HEAD` and the identical four failed, so it is not the log form, the strip, the board or the shell. **What is NOT established is whether it predates `seed:demo`**, because this session ran the seed first and phase9 only after it, so a green run before the seed was never observed. That ordering is the leading hypothesis and it has a named mechanism in two rows below: `seed:demo` **moves time** over every column in `scripts/seed/demo/clock.ts`'s `SHIFTED` list, and the verify suite is **not idempotent against the record tables** — phase9's companies survive from earlier runs, so an assertion that finds its fixture by stamp can read a PREVIOUS run's row whose dates the shift has since moved. That would produce exactly this: ages low, thresholds right. The alternative is a real regression in `companySilence()`, which nothing in the last several sessions touched. **The two are distinguished by one experiment** — `db:reset`, then phase9 before any seed | `scripts/verify-phase9.ts` §11 (lines ~1202-1270), `src/lib/coverage.ts` `companySilence`, `scripts/seed/demo/clock.ts` | **Done — session 47**, with `S46-1`. It was never the `§17` family after all — the readers were wrong and the harness was right |
| ~~**`D73` is written and 140-odd `dir` sites are unread.**~~ **CLOSED by session 47's sweep, found unstruck by session 50** — `§4` row 47 closed the whole direction family by an eight-reader re-inventory, and the `common.sqm` judgement this row held open was answered by `A2-12` (a word run; `dir="auto"`). Original finding: The rule landed in session 42 with its five unambiguous breaches fixed — `followUps.fields.days` (`"{count} يوم"`) on `turn.tsx`, `/quotations`, `/dispatches` and `/follow-ups`, and `today.target.of` (`"من {target} م²"`) on the dashboard — every one of them `dir="ltr"` → `dir="auto"`. **The family to look for is a `dir` on a container rather than on a value.** The next thing to judge is `common.sqm`, which is `"م²"` in Arabic and therefore strong-RTL: it sits inside `dir="ltr"` runs on the quotation and dispatch line rows, where the run reads *qty × w × l = sqm m²*. That is a judgement about a two-character unit rather than about a word, and it wants looking at rather than a blanket replace. This is the converse sweep to the `D62` row below, and the two want one session | `DESIGN.md` `D73` `D62`, ~140 `dir="ltr"` sites under `src/app` | Session **47**, with the `D62` sweep |
| ~~**`D29` draws a `paid` column that `D32` and `D42` have both already concluded cannot hold anything, and the board is where it becomes visible.**~~ **CLOSED as stale, session 47, per `A2-24`(3)**: `CHAIN_COLUMNS` carries `S132`'s six with no `paid` rung — its own doc says *the rung, the two acts behind it and the three columns they wrote left together* (`S133`) — and `D29`'s text already reads *the board's columns are `S132`'s six positions*. The founder decision this row waited on was taken when `S133` removed the rung; nothing here describes the tree any more. Original finding:  *"Paid-not-yet-out is not one of them. Payment is recorded on the dispatch (S70) and no route to approved bypasses it (S72, S73), so **no interval exists between paid and dispatched** for a figure to measure"* — `D32`, deciding the dashboard's side figures. `D42` says the same for the rollup: *"Paid is not a stage."* `CHAIN_COLUMNS` still carries `paid` as a rung and `D29` names it as one of the six columns. **Measured on the board slice's own data: `paid` holds 0 projects and `waitingPayment` holds 0, for every identity** — and the two empty columns are not an artefact of how a project's position is reduced from its threads. Three derivations were compared over every project (project-level `won`; per-thread `hasDispatch`; both) and **all three give the identical six figures**, so this is the chain, not the aggregation. `waitingPayment` is empty for a softer reason and will populate — its 6 live threads all sit on projects already won by another thread — but `paid` is the structural one: **every thread in the database that reaches `paid` already has an approved dispatch against it**, which is exactly the interval `D32` says does not exist. The rung is not wrong on a *quotation*, where the chain strip still draws six and `chainOwner` still says the rep owes the dispatch request; it is wrong as a *column*, because a column is a place things sit and nothing sits there. Three answers: `D29` drops it to five columns, `S70`/`S72` gain an interval, or the rule says out loud that the column is a marker on the ladder rather than a place | `DESIGN.md` `D29` `D32` `D42`, `src/lib/chain.ts` `CHAIN_COLUMNS`, `src/lib/projects.ts` `chainByProject` | **Done — closed against `S133`'s slice, which dropped the rung; verified from `chain.ts` and `D29`'s own text in session 47** |
| ~~**The dashboard's Requests headings run their count into the label in Arabic.**~~ **Done in session 42**, by the slice that next opened `D65`'s block, exactly as this row predicted. `margin-inline-*` resolves against **the element's own** direction and the count span carries `dir="ltr"`, so `ms-1.5` compiled to `margin-left` — the count's OUTER edge in an RTL line, with the number touching the label. Each heading is now a baseline flex row with a `gap`, which has no side to get wrong `D57`, the same fix `/companies`, `/dispatches` and `/quotations` took in session 28. `list-controls.tsx:130` still carries the class and still does not show it: the chip is already a flex row with its own `gap`, so the margin is additive | `src/app/[locale]/(app)/_components/requests-block.tsx`, `DESIGN.md` `D57` `D62` | Done |
| ~~**`SPEC §16` asks that project names be decided *before the projects slice*, and the projects slice has now shipped without it.**~~ **Closed by migration `0030`.** The founder decided: **one name field**, written in whichever language the rep prefers, and `S26` now says so — the rule that describes what a project shows, since no rule had ever asked for the pair, which is why `§16` could cite none to amend. `name_en` became `name` by RENAME, `0009`'s recipe. **The one departure from `0009` was measured, not inherited**: `0009` dropped `name_ar` outright on 2 of 90 companies, where here **49 of 50 projects carried a distinct Arabic name**, so the Arabic one wins and the fixture is no longer 121 Arabic company names beside 50 English project names. That made `name_normalized` move too, which `0009` never had to do — it was only ever `normalizeName(name_en)` — so the migration carries `normalizeName` translated into SQL (Postgres 17 has `normalize()` for both NFKC and NFD, so no step is approximated), **proven row for row against the function itself before it was written**: 99 project values and 126 company names, 0 mismatches. `verify:schema25` **§22** is what holds it from here, over every company, contact and project row. The **latent defect it closed** was not in the plan: `name_normalized` came from the English half alone, so **an Arabic-named project could never be found by its Arabic name** on `/projects` or the board. `lookupName` survives for roles, countries and cities and lost all 20 of its project callers; `threadLabelAr` and `FollowUpRow.anchorNameAr` are gone entirely, and with them the last reason the follow-up anchor was a pair | `SPEC.md` `S26` `§16`, `drizzle/0030_one_name_field_on_projects.sql`, `src/db/schema.ts`, `src/lib/normalize.ts`, `scripts/verify-schema25.ts` §22 | Done |
| ~~**`companyQuiet()` in `follow-ups.ts` is a third copy of the silence derivation.**~~ **Done, session 48** — it JOINS `companySilence()` now and filters on `silence.isQuiet`, so the queue, the coverage figures and `/companies` read one derivation; the cutoff arithmetic (`quietSince < today − threshold` against `silentDays > threshold`) was checked equivalent before the copy went, on-hold exclusion moved into the same expression (`gather` had been doing it a step later, so no row a rep sees changed), and `verify:followups` + `phase9` + `phase10a` — the three scripts that drive the silence boundary — are green on it. Original finding:  The companies slice moved `coverage()` and `listCompanies` onto one definition in `companySilence()` — and found the two it merged **had already drifted**: `coverage()` counted a never-logged company as quiet **from the day it was registered**, `companyQuiet()` counts from registration **plus the threshold**, while both carried the same sentence in prose. On rep-a that was **100 of 125** companies marked Quiet against **36**. `companyQuiet()` keeps its own copy for now because it feeds the waiting list and the daily digest, where a silent mistake reaches every rep's notifications | `src/lib/follow-ups.ts:703` (`companyQuiet`), `src/lib/coverage.ts` (`companySilence`) | **Done — session 48** |
| ~~**`verify:routes` §24's pile-sum assertion is page-dependent and goes red on a database the suite has run against.**~~ **CLOSED as stale, session 48** — the assertion this row describes no longer exists: `§24` was rewritten to WALK the whole scope (*"the pile counts sum to the card's own total, over the whole scope"*, page-by-page, `NOT MEASURED` below two pages, the outran-its-page positive control standing beside it), which is the rewrite `CLAUDE.md` § Verification already documents complete with both injections (6 of 1680 red on a page-local count; `NOT MEASURED` on a one-page scope) and the read-on-the-green-line lesson. The candidate fixes this row offered were both superseded by a better third. Original finding:  *"the pile counts sum to the card's own total"* compares the three `D25` pile headers on page one of `/dispatches` against `ListCard`'s `data-total`. A header renders only for a pile with a row **on that page**, while its count is the whole scope's — so the sum equals the total exactly while every pile fits on page one, and not afterwards. **Measured in session 27: at 212 dispatches it read `piles summed 33, card says 209`**, and the same walk was green at 1,429 checks an hour earlier. It is residue, not a screen defect — 212 dispatches against `seed:demo`'s 48, and `verify:slice3` writes **55 per run** — and `npm run seed:demo` returns it to green, which is the `§17` row's recipe arriving at a second address. **The assertion is not wrong about `D24`**; it is right about a state the fixture no longer reaches. Two candidate fixes, neither taken here: sum only the piles the page actually renders, or assert the identity per pile against a `?group=`-scoped list | `scripts/verify-routes.ts` §24, `scripts/seed/demo/`, `DESIGN.md` `D24` `D70` | **Done — closed as stale by session 48**; the scope-walking rewrite superseded it |
| ~~**`D38` is not built, and `25a` makes it visible for the first time.**~~ The panel read the viewer's **own** row — `attainment.find(row => row.userId === session.user.id)`, which for a manager is his personal credit `S78` — so Abdulrahman's panel said *0 of 400* beside a team table saying his reps had dispatched thousands. Both figures were correct and only one was the question `D38` asks | `DESIGN.md` `D38` `D32` `D64`, `SPEC.md` `S136` `S83` `S84` `S78`, `src/app/[locale]/(app)/page.tsx` | **CLOSED in `25b`, and the answer is a rule rather than arithmetic.** This row asked whether the denominator was a target row of its own, the sum of the measured reps' targets, or something `SPEC.md` had not been asked for. The founder answered the first, and **`S136`** now says it: *a company target is one figure for one month, set as a decision of its own*, and **neither it nor the reps' targets derives from the other** — six reps at 1,000 against a company figure of 6,000 is a coincidence, not an identity. **A manager who sells carries no personal target**, because his metres are already inside the company total `S78`, so a personal denominator would measure the same work twice. It is set by a **new** flag, `can_set_company_target`, super admin alone, and `S136` says on its face that it is new and why no existing flag drew the line: the Sales Manager holds `can_set_targets`. Built to that rule — `company_targets` its own table, the panel widened as **one component** rather than a copy, `/targets` gaining the control above its table, and `verify:routes` §36 driving all of it. Commit `9d48bbd` |
| ~~**No deliberately-long company name in `seed:demo` is quiet**, so `/companies` under its default attention order exercises no RTL truncation at all — and that is the screen a rep looks at.~~ **Done, session 48, the other way round**: rather than making a long name quiet — a CLOCK change that moves every count the suite reads — an already-quiet company took a fourth deliberately-long Arabic name (`مؤسسة الصرح الوطنية…`, 116 days old, last logged ~110 back). No date moved, no group count moved, and the name's two references in `activity.ts` and `plan.ts` moved with it — the seed's own preflight caught the first, which is what a fixture that checks itself is for. Read on re-shots: row 4 of the DEFAULT `/ar/companies` at 1366 (renders whole), the 375 waiting list (truncates on the value's own end), and `/follow-ups`, where it promptly widened the auto-layout table past the card and earned the name cells their cap — the gap this row wanted exercised, caught being exercised | `scripts/seed/demo/companies.ts`, `activity.ts`, `plan.ts` | **Done — session 48** |
| Search filters `ilike` a raw name, so the same Arabic name is findable on one screen and not another. **Corrected by AUDIT 1: three of six, not five.** `companies.ts`, `projects.ts:134` and `contacts.ts:77` all normalise. **`quotations.ts` done in session 26**, and the gap was measured against real rows rather than argued: `مكاتب ادارية` found **0 → 4** threads, `دره الشرق` **0 → 1**, and a hyphen typed as a space **0 → 2**, while the already-matching forms were unchanged at 4 and 1 — so the fold widens without moving anything that worked. **The SMAC reference stays raw**, deliberately: it is an identifier, not a name, and `normalizeName` would collapse `RE-9592` into `re9592`, the same reason `companies.ts` leaves `phone` out of the folded half | ~~`quotations.ts:261`~~, ~~`dispatches.ts:372`~~, ~~`reports.ts:527`~~ | **Done — 26, 41 and 27.** The reports half went with the screen: `/reports`' `searchFilter` is deleted and the stream's is `timeline.ts`'s `matchesQuery`, folding the SUBJECT's name on both sides. Measured the same way as its two predecessors — `مؤسسه` **0 → 24** reports, `شركه ابراج الشمال` **0 → 1** and `انماء` **0 → 1**, while `مؤسسة`, `شركة أبراج الشمال` and `أنماء` were unchanged at 24, 1 and 1. **The note half got stronger rather than folded**: it was an `ilike` that had to compose `readableNoteFilter` into its own `or`, and it is now `.includes` on a string `withNotes` has already set to `null` for a viewer who may not read it — same gate, asked once, no second term to forget. `verify:phase9` §15 asserts that directly. `dispatches.ts` folded its COMPANY half in session 41 and measured the same way: the Arabic normalised form of a company name on a dispatch found **0 → 1** against the seeded fixture, while the already-matching form was unchanged. **Its REP half did not fold and could not** — there is no `users.name_normalized` column and `CLAUDE.md` forbids landing one without its writer. That half is a row of its own below |
| ~~**A `data-slot` prop on `TableHead` silently replaces the component's own marker.**~~ **Done, session 47** — `/projects`' owner column carries `data-column="project-owner"` now, session 26's exact fix, with the reason at the line; `verify:routes`' two owner-column assertions moved with it and the run is green. Two slices passed a one-word fix; the third stopped walking past. Original finding:  `table.tsx` sets `data-slot="table-head"` and then spreads props over it, so `<TableHead data-slot="project-owner">` renders **only** `project-owner` — `D24`'s marker is gone from that cell, and `/companies` asserts that marker as a route marker. Found in session 26 when the quotations raiser column did the same thing; fixed there by using `data-column` instead, and `verify:routes` now asserts `data-slot="table-head"` on `/quotations` so the shape cannot come back unseen. **`/projects` still overrides it** on its owner column — one word, but it belongs to that screen's session rather than this one. **`28b` was named here and did not touch `/projects`** — it merged Targets and deleted a route, and reaching sideways into a third screen for one word is the scope creep this file exists to stop | `src/app/[locale]/(app)/projects/page.tsx`, `src/components/ui/table.tsx` | **Open, and it has no owner.** It was *whichever slice next opens `/projects`*; `38b` opened it — the board and the table both — and `projects/page.tsx:233` still carries `data-slot="project-owner"`. Two slices have now passed a one-word fix, which is what a disposition naming no session buys |
| ~~Eight `SelectField`s should carry `required`; each validates server-side, so the placeholder costs a round trip, not correctness~~ **Done — session 47, and it was a DIFFERENT eight.** The register's eight counted three raw `<select>`s in the quotation line rows (`classId`, `fireRatingId`, `thicknessId`) that CANNOT take native `required`: an empty select in a deliberately-spare row would block the whole submit, and the empty `supplierId` IS the spare-row sentinel (`line-form.ts:56`). The eight that gained it: `roleId`, contact `companyId`, report `channel`/`outcome`/`category`, and three the register never saw — dormancy `toUserId`, sharing `sharedWithUserId`, handover `toUserId` — every one mandated unconditionally by its action. Driven through `verify:routes` §23 (no required field behind JavaScript, still true) and §17's real POSTs, all green. **Recorded, not fixed**: `dispatch-form`'s three `FormField` wrappers say `required` while their route-dependent fields are conditionally required — a form cannot express that natively, and the asterisk is honest about the common case | user, contact, report, dormancy, sharing and handover forms | **Done — session 47** |
| ~~Participant add/remove forms never answer a no-JS POST — `Failed to parse body as FormData`; the write lands, the response does not~~ **Closed by the LEAD below.** Both actions are bound in `ProjectLinks` and passed to `LinkRow` and `AddLinkForm` as props. `verify:routes` §17 drives add-then-remove on one row in both locales and asserts the count returns to where it started `S27` — the add is driven twice, once with an empty `companyId` so a reply on a path that writes **nothing** is proved | `projects/[id]/project-links.tsx` | Done |
| ~~`confirmPaymentAction` never answers a raw form POST. Same shape, different action~~ **Closed by the LEAD below.** `PaymentForm` is its own component and the bind moved to the call site. Two assertions now stand behind it: `verify:routes` §17 drives it on the **empty-date path that writes nothing**, and section 3’s payment POST is waited for rather than abandoned — the `fireAndForget` helper that stood there is gone, and `post()` in that chain now carries an 8s abort returning `status: 0`, which no call site accepts | `quotations/[id]/thread-actions.tsx` | Done |
| ~~**A LEAD for the two rows above, found in session 13 and not acted on.** Both build the action inline inside `useActionState`~~ **Closed, and the cause is narrower than the row guessed.** Moving the `.bind()` out to the call site and passing the action in as a prop fixes it: measured on this build at `10s` abort before and `111ms` after. **Two refinements, and they are why this survived from session 4.** **First, it is not the inline expression.** Hoisting the `.bind()` to a `const` inside the hook’s own component was measured too and still hung, so what matters is *which component evaluates the bind*, not whether it sits inside the hook call. Why that is so is **not** established — only which shape answers. **Second, the hang is a property of the data, not only of the code.** The service-line remove answered while it was the only such row on the page and hung on every attempt once a second row rendered — 7 of 8, then 3 of 4, answering only on the last row remaining. So **any earlier attempt to reproduce it could have failed for reasons unrelated to the code**, and "it does not reproduce" was never evidence the shape was sound. `verify:routes` §17 is the standing guard: every such form is driven with an abort, and a missed reply is a named failure carrying its elapsed time | `quotations/[id]/thread-actions.tsx`, `quotations/[id]/thread-lines.tsx`, `projects/[id]/project-links.tsx`, `scripts/verify-routes.ts` §17 | Done |
| ~~**Four `useActionState` forms keep a `.bind()` inside their own component, deliberately.**~~ **Closed in session 28, and the escape clause was the one that fired.** The row's exemption rested on a fact rather than a preference — none of the four reached the HTML, so the hang could not be reproduced against any of them — and the rewritten `D20` made that fact the defect: a form rendering only behind client state means the act does not exist with scripts off, which is *enablement*. All four are native `<details>` now, bound at the call site, and `verify:routes` §17 was **inverted on its own markers**: it asserted these four *stay behind client state*, so it would have gone green over the very thing `D20` forbids. It now asserts each renders server-side, drives three of them with a raw POST — 200 in milliseconds, which is what proves the binding — and prints a note for the two service-row forms where no fixture thread carries a service line. `§23` sees **3,920 forms over 720 fetches**, up from 3,686/692, with **0 required fields behind JavaScript** | `quotations/[id]/thread-lines.tsx`, `scripts/verify-routes.ts` §17 §23 | Done |
| ~~**`verify:schema25`'s `S121` check reads `status <> 'approved'` where the rule means *never approved*.**~~ **CLOSED as already fixed, session 48** — `f301bf6` (the company-detail slice) had taken it: the predicate reads `approved_by_user_id is null`, an ACT rather than a status, with the row's own story in a comment at the line (*the old predicate failed on every cancelled-after-approval row — two in the demo base*). The one remaining `status <> 'approved'` in the script (line ~2333) is a different, correct use: lines on unapproved requests count nowhere, and a cancelled dispatch's metres count nowhere too `S31`, so status IS the right subject there. Green on tonight's runs. Original finding:  Two seeded dispatches were approved, numbered, then **cancelled** — which `S72` says is the only way out (*approval is final; the dispatch is cancelled, never un-approved*) and `S31` counts nowhere but keeps as history. All 7 cancelled dispatches in the database carry an `approved_at`, so the number is legitimately theirs and the assertion is too strict, not the data wrong. **Pre-existing**: introduced with `seed:demo` (`28141cd`), found while verifying the dashboard slice, which touches no writer of `dispatches`. Candidate fix is `approved_at is null` in place of the status test | `scripts/verify-schema25.ts:2306` | **Done — was `f301bf6`'s, found closed by session 48** |
| ~~Comments render on companies, contacts and dispatches.~~ **Closed in `27b` — `fc4500c`.** `0032` narrowed the CHECK to `('project','quotation_thread')` and every layer with it. **This row said nine and the answer was three**, which is the row's own instruction working: it said *re-measure before deciding, the count is a property of when the seed last ran*, and on 29 Aug 2026 it was `3 company · 0 contact · 0 dispatch · 3 project · 15 quotation_thread`. `AUDIT 1b` had said 180. **All three were `verify:routes` residue and named themselves** — `verify-routes en comment`, `verify-routes ar comment`, `verify-routes turn-panel probe` — so the two sections that wrote them are the two that were re-pointed. **`seed:demo` never wrote one**, contrary to the brief: `COMMENTS` in `scripts/seed/demo/activity.ts` has been `project \| quotation_thread` by its own type since it was written, and a clean seed after the migration writes 16 comments, none on a forbidden kind | `src/db/schema.ts`, `companies/[id]/page.tsx`, `scripts/verify-routes.ts` §9 | Done |
| ~~`quotation_threads.closed_at` and `closed_by_user_id` have no writer and no reader outside the schema~~ **Classified by the `0027` sweep and KEPT.** Still 0 of 636, and still unwritten — but `S47` names them in its own words: *losing a project closes every open quotation under it; they inherit the project's reason and are marked as **closed by the project***. That is their writer, and it is `[BUILD]`, not absent. So this is `has_credit_terms`'s pattern in the other direction: landed eleven migrations early, but by a rule that still wants it. The `quotation_threads_closed` CHECK stays with them, and it is `AUDIT 1 F18` entire — the only CHECK in the schema on columns nothing writes. `verify:schema25` §2 now asserts all three present, with `S47` named, so a later sweep cannot take them for being empty | `src/db/schema.ts` | `S47`'s slice, which builds the writer. **Not** a drop — the sweep looked and left them |
| ~~**12 of 66 quotation versions carry no product lines at all, 6 of them issued.**~~ **Closed in the `S60` session** — 13 by the time it was measured, 7 of them frozen past `requested`. **Not reachable through today's code**, which was the guess in this row and was wrong: `createQuotationThread` already refused a version with none and `removeQuotationLine` already refused the last one, neither citing `S60`. Every violating row came from `verify-phase9` and `verify-phase10a` inserting `quotation_versions` directly; both now go through `scripts/quotation-fixture.ts`. `issueVersion` gained the third guard, `verify:schema25` §18 asserts it over every row | `src/lib/quotations.ts`, `S60` | Done. The remaining question is not `S60`'s: **nothing tells a rep why an issued quotation prefills no lines**, which is `S116`'s screen, not this rule |
| ~~`quotation_lines.form_factor` is always written `'sheet'`; the enum's `coil` value is set nowhere~~ **Dropped in the `0027` sweep**, 850 of 850 `sheet` by then. The column and the `form_factor` type went together — it was the type's only column, so a plain `DROP TYPE` rather than the rebuild `record_type` needed. `12 §11`'s scope boundary survives as the rule it always was; it never needed a column to state it, and `dispatch_lines` had already refused to copy one `S116` | `src/db/schema.ts` | Done |
| **`verify:routes` §17 consumes its own preconditions, so it passes once per database and then fails 10 checks for ever.** §17 drives the real POSTs — `confirm-payment`, `issue`, `return`, `remove-line` — and each one moves the only fixture in the state the next run needs. Measured in the shell slice: the first run was **778/778**; the second, against the same build, failed 10, all of them *reachability* preconditions (*"an unpaid quotation is reachable — nothing offered confirm-payment"*, *"the issue form is reachable for the coordinator"*). The database agrees — `accepted_for_processing_at is not null` is **0 of 996 threads**, so the payment form has nothing to render on. **Proven independent of any code change**: `git show HEAD:scripts/verify-routes.ts` run against the same server fails the *identical* 10, at 744 checks against the slice's 762 — a difference of exactly the 18 rail assertions the slice added. So a green §17 is evidence about the **data**, and a red §17 is not evidence about the diff. **`seed:demo` measured it, and the number is six.** Eight consecutive runs against one `npm run seed:demo`: runs 1–6 green (803 · 802 · 801 · 801 · 801 · 801 checks), run 7 red with **exactly these 10**, run 8 identically red. Re-running `seed:demo` returns it to 803/803. The mechanism is now known rather than guessed: each run adds **4 quotation threads under `rep-a` — 2 rejected and 2 paid — and 4 companies and 6 dispatches**, and *none of the four threads offers a §17 precondition*: a rejected thread is not `open` so the coordinator's issue, return and remove-line panels do not render, and a paid one does not offer `confirm-payment`. Page one of `/quotations` is 25 rows ordered `desc(created_at)`, so residue displaces a seeded precondition at 4 per run and the 25th slot is consumed on run 7 — `25 / 4 = 6.25`. **`cancel` is not among the 10 and never was**: it needs only `open`, which an issued thread still is. The drift 803 → 801 over runs 1–3 is the two `removeServiceLine` drives eating the three seeded service lines, one per locale per run | `scripts/verify-routes.ts` §17 **Amended by the `S133` slice: eight, not ten.** Both *an unpaid quotation is reachable for the payment form* checks — one per locale — went with the payment form itself, so two of the ten cannot go red again. The **six-runs-per-seed count is unchanged**: the same four threads per run still displace the same page-one slots, and the mechanism was never the payment form in particular. | **Done, session 48 — the third fix, and it was proven by the budget itself.** `§17`'s two fixture scans walk EVERY page now (`pagedIdsOf`, bounded at twelve pages so a broken pager cannot loop), so a seeded precondition that residue pushes off page one is simply found on page two. The proof was a natural experiment: the fix landed on runs 6–7 of a seed whose measured budget was SIX — the very runs on which the old page-one scan went red — and every reachability check stayed green, twice. The *read the 8 as environmental* guidance retires with the budget; `seed:demo` remains the reset for the OTHER residue class (`§25`'s stream flooding), which is the idempotency row below |
| ~~**Arabic showed a hard vertical seam down the page with a lighter block beside it, and the rail looked wider than in English.**~~ **Both closed in the shell slice, and they were one defect.** `D13`'s glow reads logically now, but the RTL red was first set to the geometric mirror, **92%** — and the red ellipse is 1200px wide against a 1366px viewport, so at 92% its outer fade lands *inside* the frame: a visible edge at ≈537px with everything to its right washed lighter, the rail included. English never showed it because 8% puts that same edge off-frame. **108% is what puts it off-frame on the other side**, and the mirror was right on paper and wrong on screen. **The rail was never wider.** Measured in **Brave itself** over CDP at 1366 and 1440, both locales: the `<aside>` is **236px in all four**, `scrollWidth === clientWidth` on every link, and the widest Arabic label — *عروض الأسعار*, 75.5px — is **narrower** than the widest English one, *Performance* at 82.6px. `md:w-59` emits `width:calc(var(--spacing) * 59)` inside `@media (min-width:48rem)` with `--spacing:.25rem` defined, and flexbox clamps a flex item's automatic minimum to `min(specified, content)`, so a definite 236px cannot be pushed wider by content in either direction. The apparent width was the wash abutting the rail | `src/app/globals.css`, `src/components/app-rail.tsx:127`, `DESIGN.md` `D13` | Done. **The rail width itself is named by no `D` rule** — `w-59` is `AD27`/`AD28`'s class of uncited constants, for whoever writes that rule |
| ~~**The rail carries EIGHT items against `D49`'s seven — deliberate, and temporary.**~~ **Closed in `28b`, and the plan worked.** Performance had sat **last in `track`** precisely so that deleting one line would leave `D49`'s order already correct, and that is what the merge did — one line, no reordering. Both files record the seven now, and `verify:routes` **counts** the `<nav>` rather than trusting the sentence: the whole content of that half of `D49` is a number, and nothing was checking it, so an eighth item could have come back with every existing rail assertion green | `src/components/app-rail.tsx`, `DESIGN.md` `D49`, `WORKFLOW §4` row 28b | **Done** |
| **The rail footer leaves the rail when the viewport is shorter than ≈487px.** Measured, not inferred: the `<aside>` is `md:h-svh` and the `<nav>` cannot shrink — `md:overflow-visible` wins, so `min-height: auto` holds it at content height — so free space goes negative, `mt-auto` resolves to **0**, and the footer overflows the bottom. At a 305px viewport it lands **166px below** the rail's own box, painting over the page with no rail ground under it. Found while chasing the row above and unrelated to it. It needs a genuinely short window at `md` width or wider, which is why nobody has hit it | `src/components/app-rail.tsx:127,147,169` | **Closed, `38a`** — the nav shrinks: `md:min-h-0 md:overflow-y-auto`, so free space stops going negative and `mt-auto` keeps the footer inside the aside. The base `overflow-x-auto` it was fighting is gone with the strip. **The bottom sheet does not make this moot** — the sheet replaces the rail below a *width* breakpoint and this needs `md` width WITH a short window, which is an iPhone in landscape: 852×393 and 844×390 are both ≥768 wide and <487 tall. That is why it was reachable on a phone all along |
| **Three `verify:routes` assertions encoded "the coordinator holds no company" as if it were a rule. Corrected in the `seed:demo` slice.** `FORBIDDEN` listed `/projects/new` and `/contacts/new` as 404s for her and §15 asserted the New button absent, both with the same comment: *"the coordinator holds none"*. That was true of every fixture set ever built and of no rule. `S9` names the coordinator as one of the four an assignment may hand a company to, `companyBookHolderFilter` admits the role, and **`S127` needs her to hold one** — *she may raise a dispatch request against her own company and approve it herself*, which nothing in the project had ever exercised. `seed:demo` gives her four, so both forms correctly answer 200 and the button correctly renders; six checks went red for a rule-legal dataset. The route pair came off `FORBIDDEN` (the walk now asserts the 200), and §15's check became the **biconditional** — the button is offered exactly when the route answers — which is what `D51` actually says and holds whether or not she has a book. `S76` is untouched: `READ_ONLY` still holds her to read-only on every project and contact she can see | `scripts/verify-routes.ts` `FORBIDDEN`, §15 | Done |
| ~~**The linked dispatch route forces a project; the free route forbids one.**~~ **Closed by migration `0031`, exactly as this row's disposition predicted.** `S50`'s amendment was taken: the null case is gone, so the required-but-empty picker is gone with it — `projectForThread` has one branch and no query, and the dispatch form renders no project control on either route. **The free half is not closed and was never this row's to close**: `S75`'s *"a free-entry dispatch may name a project"* is still unbuilt and still carries its own `[BUILD]`, and `dispatches/actions.ts` still strips the field on that route. What is no longer true is the asymmetry, because neither route asks any more | `src/lib/dispatches.ts` `projectForThread`, `src/app/[locale]/(app)/dispatches/dispatch-form.tsx`, `SPEC.md` `S50` `S74` | Done — the linked half. `S75`'s half stays with `S75` |
| ~~**The company picker on the raise form is a native `<select>` at 126 rows, and `D20` names three JavaScript exceptions.**~~ **`D20` no longer names any**, so the fourth-exception question this row was holding open has no rule to be asked of. Decided in the `S50` slice: **it stays a `<select>`.** Recorded because the control deleted beside it was an **undeclared** exception — the project `Combobox` cited an archive doc (`[15 §5]`), never a `D` rule, and `D20`'s three are the ~200-item city list, the view-mode switch and the board's scroll. Deleting it was right; the inversion then made **company** the long list, which is a different question. Whether it earns a fourth exception has a real trigger rather than a feeling: **bulk import** (`SPEC §15` item 2), which changes the number | `DESIGN.md` `D20`, `src/app/[locale]/(app)/quotations/quotation-form.tsx` | **Rewritten by the `D20` rewrite.** There is no exception list to earn a place on any more: a control answers one question now — does it work with scripts off — and a native `<select>` does. What is left is a usability question at bulk-import scale, session **34** |
| ~~**A Saudi company cannot be registered with scripts off, and that ships today.**~~ **Closed in session 40.** `combobox.tsx` is deleted; the city is a native `<select>` grouped by region, in `components/city-field.tsx`. **The list was reported before it was replaced rather than argued from its size**: 171 cities in `scripts/seed/cities.ts`, in 13 admin-region groups the database does not store, falling into the 5 `region` values it does — **center 42 · north 28 · east 26 · west 27 · south 48**. Five `<optgroup>`s of 26–48 is the same reduction a two-step region-then-city form would buy, without the GET round trip mid-form that a scripts-off two-step needs, and the grouping key is not an invention: `S15` already derives a record's region FROM its city. Options are now ordered by the name the reader can SEE — `listCities()` orders by `name_en`, so an Arabic session had been reading a list sorted by an invisible key. **The `Combobox` had exactly four call sites, checked by grep BEFORE the file was deleted rather than discovered by the build**: three cities and `report-form.tsx:155`'s company picker, which was `AD19`'s undeclared use and is `required` — so the operability section could not have gone green without it, which is what put it in this slice rather than its own. It is a plain `<select>` now, `§5` row 269's settled shape at ~126 rows. `verify:routes` §13 keeps its empty-city refusal and gains the **reachability** half — the select offers real uuid options — and §14 scrapes a city id off the form, which deleted `aCityId()` and this file's only reach past HTTP | `src/components/city-field.tsx`, `src/app/[locale]/(app)/companies/company-form.tsx`, `projects/project-form.tsx`, `reports/report-form.tsx`, `scripts/verify-routes.ts` §13 §14 | Done |
| ~~**Every checkbox in FACET is inert with scripts off — seven call sites, one file.**~~ **Closed in session 40.** `ui/checkbox.tsx` is a styled native `<input type="checkbox">`; all seven call sites are unchanged but for `signal-fields.tsx`'s `onCheckedChange` becoming `onChange`. The credit split, the handover buckets, `inProduction`, `committed` `S29`, the report's signals `S43` and both mention pickers now post what is ticked. `D14`'s treatment is kept — same size, radius, border and brand fill. **Two traps were paid for on the way and are written at the line**, because both fail in SILENCE: `::before`/`::after` on an `<input>` is a replaced-element case browsers do not agree on, so the tick is a background image; and Tailwind scans the SOURCE TEXT, so a bare `/` in an arbitrary value reads as the opacity modifier and an escaped `"` reaches the stylesheet as a literal backslash — the first shipped a tickless checkbox through a green `build`, and only reading the compiled CSS back caught it. Every `/` in the data URI is `%2F` and the `url()` carries no quotes | `src/components/ui/checkbox.tsx`, seven call sites, `DESIGN.md` `D14` `D20` | Done |
| ~~**A field note reaches no timeline.**~~ **Closed by the stream, and the row was wider than the defect.** `S33` allows an entry anchored to nobody and `reportEvents` scopes by company or project, so a field note falls into no *anchored* timeline — that half was right and still is. But *"logged and then never read by anybody"* was already false when this was written: `reportEvents` applies **no anchor term at range scope**, so `/activity`'s expanded rep already showed field notes, and `/reports` listed them behind a `field_note` chip. What was true is that neither screen was **the** place they belonged, and one of them said so outright — `reports.detail.fieldNoteHint` read *"appears on no timeline"* in both locales. The stream is unscoped, so a field note lands in it by construction rather than by a new scope; the row carries the **city** where every other event carries its company, since `S33` gives a field note nothing else. The string is corrected. `verify:routes` §25 asserts `data-entry-type="field_note"` in the stream, in both locales | ~~`src/lib/timeline.ts`~~, `scripts/verify-routes.ts` §25 | **Done in session 27.** No new scope and no write-only rule were needed — the answer was a screen that is scoped to nothing |
| **`notifications.record_type`/`record_id` are the one nullable pair with no pairing CHECK**, where three identically-shaped pairs elsewhere in the schema each have one. `notifications/page.tsx:205-212` reaches for `?? "company"` / `?? ""` to render around it, which is a screen inventing a default for a row the database allowed to be half-filled. Found as `E2` | ~~`src/db/schema.ts` `notifications`, `notifications/page.tsx:205-212`~~ | **Done — session 24, `0033`.** Both halves, not one: the CHECK is `notifications_record_pair`, gated before generating on a `select count(*) … where (record_type is null) <> (record_id is null)` that returned **0**, and the screen's `?? "company"` / `?? ""` went with it — the entry now narrows on `row.anchorType && row.anchorId` and cannot guess. `verify:schema25` drives both directions through `databaseRefuses` plus a positive that the same insert with both halves lands, and all three were fed the defect: the constraint was dropped, the two negatives went red, §13's schema-vs-database comparison caught it too, and it was restored |
| ~~**One Phase 2 commit carries no rule citation**, against `CLAUDE.md`'s *cite rules by number in every plan, comment and commit message*.~~ **Closed as permanent, session 50 — the message cannot be fixed without invalidating 62 hashes, and recurrence is now hook-blocked: H10 denies an uncited commit before it exists.** Original finding: `d74f0c6` — the companies list — reads *Refactor Companies Page with Sorting and Silence Meter Integration*: title case, generic, and naming none of `D25`, `D26` or `S89`, which is what it actually built. It is the only one of the 31 Phase 2 commits that breaks the convention | `git log`, `CLAUDE.md` | **None — deliberately not fixed.** Rewriting the message means rebasing, which changes every commit hash after it, and `§4` now carries **62 verified hashes** that would all become wrong. The convention protects a message that has not been written yet; it is not worth invalidating the record of thirty sessions to retrofit one. Recorded so the gap is not mistaken for a slice nobody can identify |
| ~~**The coordinator's approval queue has no signal for which requests need reading.**~~ **Built in session 15.** `S120`'s flag is resolved in SQL before pagination and marks the row in the Source column, so the queue now says which three of thirteen differ from their quotation without any of them being opened. Approve and refuse stay on the detail screen: `S72`'s verb is *checks*, and a per-row approve button would still be the opposite of it | `S72` `S120`, `/dispatches` | **Closed in session 41, by removing it.** `D66` was written after this row and answers it as a rule rather than an observation: *a dispatch's difference from its quotation is recorded, never flagged to the coordinator*, because a warning that fires on a large fraction of rows is one people learn to click past. The marker is off the list and lives on the dispatch, where `D66` says the rep, the coordinator and the manager already see it — now in its own card beside `S77`'s three figures rather than buried under two line tables. **No measurement is cited for the closure**, deliberately: the rate is not knowable from this database, which is the row above |

## Added by AUDIT 1 (§6), ranked by consequence

| What | Where | Disposition |
|---|---|---|
| `S20` says a contact "is visible exactly when its company is". False since `S76`: `visibleContactsFilter` returns `undefined` for `can_dispatch`, so the coordinator reads every contact while every company stays closed to them. `S76` names itself an exception to `S30` and amends `S30` in place; it never amends `S20`. The code is right and the rule is stale | `SPEC.md` `S20`, `src/lib/authz.ts:546-555` | **Done — `5fdea92`.** `S20` now carries it in its own words: *"The coordinator is the one exception, per **S76** — they see every contact without seeing its company, because a dispatch names a contact."* The code was always right; the rule had not caught up |
| The coordinator reads every rep's comment conversation on **every project and every contact**. `visibleCommentsFilter` composes each anchor's own filter, and for those two they are the `S76`-widened ones, so both branches degrade to "the record exists". Company comments stay closed, so the asymmetry is real rather than uniform. No rule states this; `S38` protects a report's note from a share and comments have no equivalent half | `src/lib/authz.ts:878-898` | **Done — `e1b8f8c`.** The founder decided and it became `S131`: a comment follows its anchor, and **`S76` is the one exception that does not carry the conversation** — on a project and a contact her sight stops at the record. The sentence this row asked for exists, so no narrowing was needed |
| ~~`S18` says "the primary rep is always the first rep who had the company". False in both handover paths~~ **Closed in the `S18` session.** The founder decided **primacy follows the company** — the primary rep is whoever holds it now, and the badge means *this is whose customer this is*, not *this is who found them*. `S18` is amended to say so and loses its `[CHANGE]`; who found the company is `created_by` `S123`, which never moves. **The code was right in two writers of three.** Handover's third branch — the recipient ALREADY holds the company, so there is no row to insert — moved primacy nowhere, and that is the case `team.ts` carried as `OPEN [19 §8]`: **12 of 393 companies with a live membership had no primary rep at all**, six from `verify-phase11` §11 driving exactly that branch and six from `verify-phase9` §16 stamping `removed_at` on the primary directly. **None had two** — no writer has ever made a second. `team.ts` now promotes the row that stays, §16 promotes what is left of its company, migration `0026` repaired the twelve, and `verify:schema25` §20 counts both halves over every row. **No index**: a partial unique index could refuse a second primary — `project_companies_one_buyer_key` is that shape for `S26` — but not the missing half, and an unused index is the defect this section already lists ten of | `src/lib/team.ts`, `src/lib/dormancy.ts`, `SPEC.md` `S18` | Done |
| **`S62`'s three acts behave three different ways, and one of three matches the rule.** `S62` requires a written reason for returning, **rejecting** or cancelling, "which becomes a comment on the thread". `returnForEdit` does exactly that — it refuses an empty reason and writes a comment mentioning the thread's raiser. `cancelThread` requires a reason and stores it in `quotation_threads.cancellation_reason`, with **no comment and no mention**, so the rep is never told a signed quotation was killed. **`rejectThread` takes no reason at all** — no parameter, no column, no field on the screen: `rejectThreadAction` passes only a `threadId` and the control is a bare `PlainButton`, so a rejection leaves nothing but an audit row recording the end-state change. Rejection is the worst of the three, because cancellation at least persists the reason somewhere a person could find it | `src/lib/quotations.ts` — `returnForEdit` 1550-1596 (comment at 1589-1595), `rejectThread` 1834-1839, `cancelThread` 1846-1862; `quotations/[id]/thread-actions.tsx:264-268` | **Done in session 17.** All three now behave one way: `setEndState` takes the reason, writes it as a comment on the thread `S62`, and raises `decision.ended_work` to the thread's raiser `S128`. `rejectThread` gained the parameter, the action, the form and its own error key; `cancelThread` gained the comment it never wrote. `returnForEdit` is untouched — a return hands work back rather than ending it. **No `rejection_reason` column**: `S62` makes the comment the reason's home, and a column beside it would be a second one |
| ~~`projects.end_state` is set by hand from the project form and **nothing derives `won`**~~ **Closed in the `S31` session.** `won` is derived from an approved dispatch by `projectIsWon`, resolved in SQL at both readers, and the value is gone from `project_end_state` so no route can hand-set it. `dormant` went with it (below), leaving the type one value. **Two things stay open and are their own rows below**: `end_state` is now equivalent to `lost_reason_id is not null`, and a free-entry dispatch still names no project | `src/lib/projects.ts` | Done |
| ~~Handover and dormancy reassignment accept **any** active user as recipient — only `isActive` is checked and the picker is `listActiveUsers()` unfiltered, so a company book can land on an Executive or a Super Admin~~ **Closed in the `S9` session.** The rule was wrong, not just the code: the founder decided **marketing holds companies exactly as a rep does** — taking calls and passing leads on is work added on top of a rep's, not a different job — which is why marketing and the desk rep carry the same flag vector, and that is correct modelling rather than an accident. `S9` gains a fourth recipient and loses its `[CHANGE]`. **No flag means "holds a company book"** — all twelve name acts — so the test is a deliberate **proxy**: `companyBookHolderFilter` is `sees_all_reps = false`, admitting Sales Rep, Desk Rep, Marketing and Sales Coordinator, refusing Sales Manager, Executive and Super Admin. Five flags give that same partition (`can_manage_users`, `can_set_targets`, `can_approve_delete`, `can_resolve_duplicate` are the others); **`sees_all_reps` was chosen because it is the only one naming a *position relative to the reps* rather than an act** — approving a delete or resolving a duplicate is reaching *into* a book, which does not put you above it — and `visibleMeasuredUsersFilter` already reads it that way. **If a rule ever needs the real thing, that is a flag, not a sixth proxy.** Written once in `authz.ts`; both writers ask it of one user through `isCompanyBookHolder` and keep their own `isActive` check and message, and the two pickers read `listCompanyBookHolders`. `verify:schema25` §21 asserts the partition by role name against the live seed and then every row — **592 live memberships, 0 held by an elevated role** — with an empty-read guard; `verify:phase10a` and `verify:phase11` assert each writer's refusal by its own key | `src/lib/authz.ts`, `src/lib/team.ts`, `src/lib/dormancy.ts`, `SPEC.md` `S9` | Done |
| ~~`users.city_id` has no writer and no reader~~ **Dropped in the `0027` sweep**, 0 of 401 by then. The `cityId` parameter on `createUser` went with it: taking a value and writing it to a column nothing reads is what made the column look alive, and it was the only thing that did. `users.region` still serves `10 §7`, and a rep's city is now a rule away rather than a column away | `src/db/schema.ts`, `src/lib/authz.ts` | Done |
| ~~`quotation_threads.cancelled_at` is written by `cancelThread` and read by nothing~~ **Dropped in the `0027` sweep** — the next dead-structure sweep, which is what this row named as its disposition. Nine rows carried one by then and no reader had appeared. `cancelled_by_user_id` beside it is read and stays. `dispatches.cancellation_reason` cited this column as its counter-example for carrying no stamp of its own; the two tables now say the same thing, and the audit row carries the actor and the moment `S112` | `src/db/schema.ts` | Done |
| ~~`notifications.channel` is written from `notification_types.default_channel` and read by nothing~~ **Dropped in the `0027` sweep**, ahead of session 24 rather than with it: a column with a writer and no reader is this section's business, not `S91`'s. 620 rows, every one `in_app`. **`notification_types.default_channel` went with it**, and is the finding this row did not reach — its only reader existed to write the dead column, so the sweep's rule (*a column with no reader*) reached one level further than AUDIT 1 had looked. The `notification_channel` type had no column left and went too. `04 Q17, C3` wanted the column "from day one so adding a channel is a migration, not a rewrite of every call site"; no rule ever asked for a second channel, and speculative generality is what this section exists to remove | `src/db/schema.ts` | Done. Session 24 still deletes the machinery `S91` names — this took only the channel |
| ~~**Ten indexes nothing uses.**~~ **Corrected by the `0027` sweep: eleven, and four of them dropped.** The eleventh is `audit_log_actor_idx` — every reader that cares who acted asks for `coalesce(acting_as_user_id, actor_user_id)` `[07 A6]`, and a btree on one of the two columns cannot serve a coalesce over both. **Dropped:** that one, `comments_author_idx`, `comment_mentions_user_idx` and `rep_report_signals_signal_idx`. The last was landed in `0005` *"so `20 §4`'s aggregation needs no migration when Phase 12 asks"* — an index built ahead of its query, which is the defect `CLAUDE.md` names for a column built ahead of its writer; `S49` adds it back with the query. **Kept:** the six on the tables above, and `companies_merged_into_idx`, which the earlier count wrongly grouped with the live four — it serves a column `S21`–`S23` wants. **And four more the sweep found that nobody had counted at all**, none of them dropped: `companies_phone_idx`, `companies_name_normalized_idx`, `contacts_name_normalized_idx` and `projects_name_normalized_idx` are only ever read through a leading-wildcard `ilike`, which no btree can serve — but `S23` names phone the primary matching key and the schema header names the normalised columns as what duplicate matching compares, so they are `S21`–`S23`'s too | `src/db/schema.ts` | Four done. The rest with the table or column each serves |
| ~~**Dead enum values.** `record_type.quotation_version` is set nowhere~~ **Dropped in the `0027` sweep**, 0 rows across all seven columns of the type. `ANCHOR_TYPES` excludes it as well as the `comments` CHECK and `SHARED_RECORD_TYPES`, and a version is reached through its thread, so nothing hangs off one. It was the canonical dead value — `dispatchStatusEnum`'s header cites it by name for why `cancelled` waited for `cancelDispatch` instead of landing early. The rebuild hit `0024`'s trap exactly as recorded: `comments_record_type` stores its literals already resolved to the type, so casting the column to `text` first fails with `operator does not exist: text = record_type`. **Proven, not assumed** — the migration was run once without the constraint pair on a clone of the live database, and it aborted on that line. ~~`project_end_state.dormant`~~ gone in `0024`; `form_factor.coil` gone with its column and its type | `src/db/schema.ts` | Done |
| ~~`product_suppliers.code`, `product_classes.code` and `product_fire_ratings.code` exist for the generated product name `S53` says FACET does not produce~~ **Dropped in the `0027` sweep.** The founder decided it: *an `ORDER BY` is not a reader, and `S53` settled the question*. The three lists order by `name_en` now, like every other lookup in `lookups.ts`. **No data was lost and no label changed**: `08 B1` says an invented longer form would be fiction, so the code and `name_en` were the same string in every seeded row — the three seed upserts and five verify scripts simply re-key on `name_en` | `src/db/schema.ts`, `src/lib/lookups.ts`, `scripts/seed-lookups.ts` | Done. This was the row that said "recorded so a later slice decides deliberately"; the sweep is that slice |
| ~~`SPEC §15` lists `accounts` under "Dropped outright". It was **not** dropped~~ **Done, and this row was stale.** The correction is in `SPEC §15` — *"`accounts` was on this list and **stays**"*, with the reason: a non-optional member of the adapter's `DefaultPostgresSchema`, so dropping it fails typecheck. A library requirement is a writer. The `0027` sweep found the fix had landed and the row had never been closed | `SPEC.md` §15 | Done |
| ~~**Three markers name something already built, in whole or in part.**~~ **Closed, and `AUDIT 1b` confirmed each from the file.** `S34` `[CHANGE]` — the marker is **gone entirely**; the enum still matches `S34`'s five channels exactly. `S81` `[CHANGE]` — kept, and the rule now names which half is true: *"The first two sentences are true today; the third is not — `divideEqually` distributes a remainder."* `S11` `[BUILD]` — kept, and says *"Deactivate and re-enable are built; the reset is not."* `S106` `[BUILD]` — kept, and says *"The review, the three outcomes and the one record all exist. Only the second way in — the rep's request — is missing."* All four are `WORKFLOW §7`'s shape now: the marker says whether work remains, the sentence says what | `SPEC.md` | Done. **The class is not closed** — `AUDIT 1b` found three more, in both directions: see `A1`, `A4` and `A5` below |
| ~~**`S90` and `S92` name two surfaces for one message.**~~ **Decided and written, session 24.** Both were right, doing different jobs, and `S90` is amended to say which: **the bell tells the person once**, at the moment it happens `S128`; **the archive holds the record permanently**, with its reason, where `/dispatches?archive=1` already puts it `S122`; and **nothing lingers on the working list**, because a refused row sitting in someone's list is a job that is not a job, which is the whole of `S91`. `S90`'s clause *"shown to the person it was taken from as it leaves"* now names the bell as that surface and stops describing a second list item. `S92` is unchanged — it was already right | `SPEC.md` `S90` | Done |
| ~~**`S122` gates archiving on the rep "having been told"**~~ **Decided and written, session 24: "told" means SENT, and the rule says so in its own words.** Refusal is the one moment — the request leaves the working lists and the bell is raised in the same act — so there is nothing for the telling to gate. No read state, no persistence flag, no dismissal tracking. `S122`'s open half is closed and its `[CHANGE]` marker comes off, checked against `WORKFLOW §7` rather than by reflex: every other clause is built (`reviveDispatchRequest`, `/dispatches?archive=1`, the rep-sees-their-own filter) | `SPEC.md` `S122` | Done |
| ~~**Session 24 deletes the notification machinery per `S91` while `S92` gains two new bell items** (`S128`, `S129`). The deletion must not take them with it~~ **Done — both survived, and it is asserted rather than observed.** Neither ever stood on anything `S91` names: no `RESOLUTION_RULES` row, no sweep branch, no digest date, no anchor. Only `followup.digest` left, taking the type count from seven to `S92`'s **six**. `verify:phase10a` §1 now asserts each of the six seeded AND `followup.digest` **absent** — a count of six would go green on six wrong rows — and `verify:schema25` makes the same claim against the rows | `SPEC.md` `S91` `S92` | Done |
| **`WORKFLOW §4`'s own row 24 was wrong about the code, and had been since Phase 2.** It called session 24 *"a deletion, 3,445 lines overdue"* of four things that *"still ship beside"* the dashboard that replaced them. Two of the four are what the dashboard is BUILT ON — `page.tsx` reads `follow-ups.ts` through `shellCounts()` → `followUpScope()`, and `D33`'s four tiles link into `/follow-ups?group=`, which `D49` already calls *the waiting list, filtered*. The other two are `S92`'s bell, which that same row's rules require be KEPT. **`verify:routes` had been asserting the tile links since Phase 2**, so the evidence was green in the suite the whole time the row said the opposite. Corrected in place rather than silently, because the wrong premise is the finding: a session plan is read by whoever runs next, and this one would have had them delete the dashboard's engine to hit a line count | `WORKFLOW.md` §4 row 24 | **Done — the row is rewritten in session 24.** The general lesson has no owner and is stated once here: **a `§4` row's line counts and its "still ships beside" claims are a snapshot from the session that wrote them, and rot exactly like `§10`'s.** Read the imports before believing one |
| **A negative assertion's non-vacuity guard can be put on the wrong subject, and then it fails a working system.** Two in session 24, opposite directions. `verify-followups` §16 asserts a stranger's scope leaks none of the run's follow-ups; the guard was written as *the stranger's scope is not empty* — but that stranger is a rep who holds nothing, **deliberately**, so an empty scope is the correct answer and the guard went red on correct behaviour. The right subject is the OWNER: what must be non-empty is the set of rows there is something to leak. In the other direction, `verify-phase10a` §9's *no notification carries a digest payload* was written as `payload ? 'counts'` — and `record.handed_over` carries `counts` too `[21 §5]`, so the claim was really *no handover exists*. **It went green on the first run and red on the second**, because §12 raises the handover after §9 reads: a check that passed for the wrong reason and was only caught by running the script twice | `scripts/verify-followups.ts` §16, `scripts/verify-phase10a.ts` §9 | **Done — both fixed in session 24**, and recorded because the pair is a shape, not two typos. `CLAUDE.md` says *a negative guards on a non-empty read*; these add **on a non-empty read OF WHAT**, and **run a new assertion twice** — the second run is what distinguishes a claim from an ordering accident |

## Added by AUDIT 1b (§6), ranked by consequence

| What | Where | Disposition |
|---|---|---|
| **`S114` carries no marker, and its first sentence is false.** *"Comments exist on quotation threads and projects only. Not on companies, contacts or dispatches."* Five kinds are admitted at every layer — the `comments_record_type` CHECK, `COMMENT_RECORD_TYPES`, five branches in `visibleCommentsFilter`, and `CommentBox` on the company, contact and dispatch screens — and the database holds **180 comments on the three the rule forbids**: 140 company, 15 contact, 15 dispatch. `S114`'s own text names one gap, the unread-comment surfacing, and stops; with no marker the rest reads as done. The **disclosure** is real rather than theoretical: a comment follows its anchor `S131`, so a company comment reaches anyone holding a company share, and `S38` gives a report's note a half a share may not read while a comment has no equivalent. The behaviour is already the `S114`/`D48` row further up this section; **what is missing is the marker on the rule** | `SPEC.md` `S114` | **Done.** `S114` carries `[CHANGE]` and names which sentence is the target, from session 27; the behaviour is `27b`'s. **All three of `A1`, `A4` and `A5` are closed** — the other two by the legend amendment below |
| **`S107` states a gate that is wrong, and carries no marker.** *"Archiving is gated by the delete-approval flag (S8). That gate is not built… archiving is gated by `canAssign` instead."* Confirmed: `archiveCompany` checks `canAssign`, and `canApproveDelete` has no reader in `src/` at all. Work plainly remains and the rule carries **neither `[CHANGE]` nor `[BUILD]`**, which `WORKFLOW §7` makes the marker's job. Separately, the rule names the flag **`canApproveDeletion`**; the column is `can_approve_delete` and the TypeScript is `canApproveDelete`, so a grep for the name `S107` gives finds nothing | `SPEC.md` `S107`, `src/lib/dormancy.ts:314` | **Done — `ebe8348`.** `S107` carries `[CHANGE]`, which is what `§7` makes the marker's job when work plainly remains, and the flag is named `canApproveDelete` as the column and the TypeScript both spell it — a grep for the name the rule gave used to find nothing. The gate itself is unbuilt and stays that way; the marker now says so |
| **`S117` is `[BUILD]`, is half built, and its text does not say which half.** `[BUILD]` means *nothing exists yet*. In fact `dispatch_lines.unit_price`, `line_total` and `vat_amount` are all `NOT NULL` at `VAT_RATE` `S57`, and the dispatch detail screen renders all three **below square metres in prominence**, citing `S117` by number three times — to the rep, the coordinator and the manager, which is exactly who the rule names. What does **not** exist is a dispatch-level total: `dispatches` carries no `total_excl_vat` / `total_vat` / `grand_total` the way `quotation_versions` does, and nothing sums the lines into money. This is the failure `WORKFLOW §7` was written for — *"`S118` read as finished while two of its three clauses were unbuilt"* — running the other way | `SPEC.md` `S117`, `dispatches/[id]/page.tsx:337-338`, `:491`, `:630` | **Done, and the marker did not move.** The founder settled the class by amending `SPEC.md`'s legend rather than retagging nine rules: **a marker is one bit and a rule can be three quarters done** — it says whether work remains, and the rule's own text says which half, which is `§7` written into the legend it disagreed with. `S117` already names its half (*the line money ships; the dispatch's own total does not*), so it is correct where it stands. Retagging would have swung the progress bar by nine with no work done |
| **`S123` is `[BUILD]` and is wholly built.** The rule's own text says so: *"Both figures exist, over dispatch requests, and nothing else is counted. `/performance` carries them per rep for a month."* Every clause checks out — `requestOriginForPeriod` computes `raised`, `raisedForThem` and `editedByAnother` in SQL, bounds the month on the **act**, and reads `coalesce(acting_as_user_id, actor_user_id)`; `/performance` renders all three and says they do not add or subtract; `verify:slice3` §30 asserts both figures, the impersonation case and `S127`'s self-raised request. **One of the twenty-four `[BUILD]`s therefore names nothing unbuilt**, which is a reading of the progress bar rather than of the code | `SPEC.md` `S123`, `src/lib/dispatches.ts:2622`, ~~`performance/page.tsx`~~ | **Done.** The marker this row reported had already gone; what was left was the rule naming a deleted route, and `S123` now says `/users/[id]`, where `28b` moved the three figures. The legend amendment under `A4` is what keeps the remaining marked rules honest |
| ~~**An assertion that can pass without reading anything — `verify-comments.ts:963-975`.**~~ **CLOSED — already fixed by session 24 (`05b15e7`)**, found closed by session 48: the check guards `mine.length > 0 &&`, prints *read N of TOTAL* in its label, and carries the register's own sentence as a comment at the line. Original finding:  It takes page 1 of `listNotifications(repB)` (25 rows), filters to unread mentions, marks **those** read, then asserts `unresolvedCount === waitingBefore - mine.length`. Nothing is cleaned up between runs and repB accumulates notifications, so the moment the mention this section raised is not on page 1, `mine` is empty, nothing is read, and the check is `x === x - 0` — under the label *"reading it clears the badge"*. It is the only assertion standing behind `21 §4`. The suite already solves this shape elsewhere: `verify:slice3:2468` guards a negative with `working.total > 0`, and `verify:schema25` §20 and §21 each carry an explicit empty-read guard. **Unproven: read from the code and the page size, not reproduced** — constructing the row count that would make it pass falsely is a write, and an audit changes nothing | `scripts/verify-comments.ts` | **Done — was `05b15e7`'s, found closed by session 48** |
| ~~**`verify-comments.ts:749-753` asserts against whichever mention comes first.**~~ **Done, session 48 — the one of the four still live.** The outsider's mention is pinned by ID DIFFERENCE now: the page-one ids are captured before the tag and the assertion takes the row new to the page, with its own guard check printing how many rows preceded it — the withheld payload carries no record id and no stamped body, so the id set is the only honest handle, and it does not lean on the list's ordering. Green, twice in a row on an accumulating page. Original finding:  `outsiderRows.rows.find(row => row.typeKey === mentionReceived)` pins no record — unlike the coordinator branch 190 lines above, which matches on `payload.recordId === project.id`. It is right today only because the list happens to be newest-first, and only until the outsider carries a second mention. Read from the code; the run was green | `scripts/verify-comments.ts` | **Done — session 48** |
| ~~**Four page-one reads with no total guard, three of them negatives.**~~ **CLOSED — already fixed by sessions 26 and 27**, found closed by session 48: `verify-slice2`'s read is scoped to the project on BOTH sides (its comment records the old shape *"passed for two years because the order happened to be newest-first"*), and `verify-phase9`'s two negatives went through the session-27 rewrite onto `reportsIn`, an UNPAGINATED reader whose docblock says why: *paging would make an absent row ambiguous between withheld and on page two — which is exactly what these checks exist to tell apart*. Original finding:  `verify-slice2.ts:555-558` reads page 1 of `listQuotationThreads(coordinator)`, who sees all **775** threads, and line 558 is `!(…).rows.some(…)` over repB's page 1; `verify-phase9.ts:813` and `:1495` are negatives over page 1 of `listReports`, the second unfiltered. Every list in `src/lib` pages at 25. All four are correct today because the fixture rows are the newest, and none prints `saw N of TOTAL` or guards on a non-empty read — which `verify-slice3.ts:1268-1287` already does, in a comment naming the earlier version's own bug. **Unproven: read from the code and the page size, not reproduced.** Every one of them passed in this audit's run | `scripts/verify-slice2.ts`, `scripts/verify-phase9.ts` | **Done — sessions 26 and 27's, found closed by session 48.** `B4` (`dispatch-fixture.ts`) stays recorded-deliberately, per its own row |
| **`WORKFLOW §10` disagrees with the repository on six of its rows.** Migrations 20 → **28**, tables 41 → **42**, `SPEC.md` 718 → **895** lines, `S` numbers 129 → **131**, routes 38 → **39**, open markers 16/37 → **12/24**. The `D` count (63) and the skills row are still right. The section is explicitly a dated snapshot at `06aeb2b` that says to re-take rather than trust it, so this is a reading of that instruction rather than a defect | `WORKFLOW.md` §10 | **Done — re-taken at `58820b3`.** Every row was measured again rather than patched, and more of them had moved than this row counted: migrations 20 → **32**, tables 41 → **42**, routes 38 → **37**, `SPEC.md` 718 → **1033** lines, `DESIGN.md` 421 → **1198**, `S` 129 → **135**, `D` 63 → **74**, open markers 16/37 → **13/24**. Two rows were not stale but wrong: *Blocking — the Docker build*, fixed before this file existed, and `docs/design/superseded/`, which no longer exists at all |

## Added by AUDIT DESIGN (§6), ranked by consequence

| What | Where | Disposition |
|---|---|---|
| **`AD1`. `WORKFLOW §4`'s session plan cited the wrong `D` rules — nine rows, twelve citations, every one exactly three low.** Session 23's *"seven-item rail … permission boolean"* pointed at `D46` `D47`, which are the Log button and the private note. Session 38's *"phone pass"* pointed at `D52` `D53`, which are empty states and `notFound()`. Session 39's *"RTL pass"* pointed at `D54`, which is *there is no skeleton state*. **Not drift** — `git show 12689b4:DESIGN.md` has `D52` as the empty list and `D25` as grouped lists exactly as today, so `bd3a943` wrote them wrong. `§4`'s prose citations and all 24 backticked `D` citations in `src/` and `scripts/` are correct; only this table was. Every Phase 2 and Phase 4 session would have opened the wrong screen's rule | `WORKFLOW.md` §4 rows 23, 24, 25, 26, 27, 35, 36, 38, 39 — **row 38 has since split into `38a`, `38b` and `38c`, and 39 is dissolved**, so two of the nine no longer exist to look up | **Done, in this audit.** `D46`→`D49`, `D47`→`D50`, `D29`–`D34`→`D32`–`D37`, `D35`–`D38`→`D38`–`D41`, `D22`–`D24`→`D25`–`D27`, `D42`–`D45`→`D45`–`D48`, `D25`→`D28`, `D26`→`D29`, `D39`–`D41`→`D42`–`D44`, `D52`→`D55`, `D53`→`D56`, `D54`→`D57`. The `D5`–`D23` range on row 23 is unchanged — it is correct as written |
| ~~**`AD2`. `D8`'s effect tokens do not exist, and the `facet-ui` skill hands all of them to a build session as live.**~~ **Resolved by the token slice.** All seven are in `globals.css` now, at v5's values, in both themes — plus two the concept has no value for, `--surface-2-solid` and `--rail-solid`, which `D19` needs and `D8` names. `--surface-3` is gone from `D5` (it was v4's, and no stylesheet ever carried one) and from the skill's palette table. The skill's Effects table now names only what exists, points at the `card-face`/`glass` utilities that hold the texture, and writes both six-item lists out one item at a time — which closes `AD29` in passing. `--glow` went with them: no reader, and `D8` never named it | `src/app/globals.css`, `.claude/skills/facet-ui/SKILL.md`, `DESIGN.md` `D5` `D8` | Done |
| ~~**`AD3`. `DESIGN.md` carries two generations of the design in adjacent sections, and they disagree about what a surface is.**~~ **Resolved: v5 wins, and the founder took the whole palette rather than half of it.** `D5`'s table is v5's values in both themes — canvas, the two surfaces as `rgba`, line and line-strong, the dark text trio, the rail and its three text tokens, and the eight tone tokens as tints — and `globals.css` is the same list. The decisive argument was the rail: `--blur` on an opaque hex is a no-op, which is exactly the unused structure `CLAUDE.md` calls a defect. `D14`'s texture is real on all three card surfaces, `D13`'s glow is on `<body>`, and both of `D19`'s fallback paths are built | `DESIGN.md` `D5` `D8` `D14` `D21`, `src/app/globals.css` | Done |
| ~~**`AD4`. `D41` names four things routed to a manager; two have no `S` rule and one rests on a deleted mechanism.**~~ Duplicates `S22` ✓ and archive requests `S105` ✓. **Credit approvals: no rule** — `S80` puts the credit question on the *coordinator* at approval, `SPEC §15` records `companies.has_credit_terms` dropped (*"a credit customer is the `handled by finance` payment method `S71`, not a flag beside a gate"*), and `§4` session 31 is struck out as **Removed**. The concept still draws the card. **Shares: `S96` says there is nothing to route** — a manager initiates a share; nothing arrives to decide. `D41` is what *"makes the manager's version of the waiting list different"*, and half of it does not exist | `DESIGN.md` `D41`, `SPEC.md` `S71` `S80` `S96`, `SPEC §15` | **CLOSED in `25a`, by founder decision, and the answer removes work rather than adding a rule.** **Credit is not a decision anyone makes.** It is **one of six payment methods, chosen per dispatch** `S71` — no company flag, no approval step, and nothing routed to anybody. That is why `companies.has_credit_terms` was dropped and why `§4` session 31 is struck out as **Removed**: the mechanism the card was drawing had already gone, and what stayed was the concept. **Shares route nothing**, as this row already read `S96`: a manager initiates a share, so nothing arrives for one to decide. **So `D41` stands at two things, not four** — duplicates `S22` and archive requests `S105`, both of which have their rule — and it is **25b**'s to build. **No `SPEC.md` rule was added.** The row asked what a manager decides and the answer is *less than `D41` implied*; inventing an `S` rule to make the card fuller is the shape this register exists to prevent. `D41`'s own text is left alone: it already names exactly those two |
| ~~**`AD5`. `D33`'s counts strip cannot express what the data layer computes, and the screen that tried breaches `D21`.**~~ **Resolved: four tiles over six kinds, and the fifth condition is not a tile at all.** `D33` now groups the six `FOLLOW_UP_KINDS` into four — Quotations (`quotation_no_response` + `quotation_returned`) · Gone quiet (`company_quiet` + `catalogue_no_response`) · Not moved · Your dates — so **no condition is dropped** and `D21`'s six-equal-tiles clause is not breached. `S89`'s dispatch request sitting with the coordinator is deliberately **not** a tile: it goes to `D64`'s **Requests waiting on me** block, where it can be acted on rather than counted. `D33`'s old fourth item, *on hold resuming this week*, was never a kind and leaves with the rewrite — the gap it stood for is its own row above. **Nothing in code changed**: the six-card strip still ships | `DESIGN.md` `D33` `D21` `D64`, `src/lib/enums.ts:431-438`, `src/app/[locale]/(app)/page.tsx:132-147` | Done as a rule. The screen is session 24 |
| ~~**`AD6`. `D32`'s "paid, not yet out" and `D42`'s "paid" funnel stage name a state `S70`–`S73` removed.**~~ **Resolved by halves, and the second half is open on purpose.** `D32` closes: **nothing stands in paid's place — two side figures, not three**, and its `OPEN — not chosen` is gone. **`D42` keeps its own**, by founder decision rather than oversight: the rollup is a later phase and the figure is better chosen with the screen in front of him, so it is not closed by inference from `D32`. `D42`'s separate *approved* ambiguity — `S72` gave that word to the dispatch while the concept's funnel means `S65`'s accepted — is untouched and stays with it | `DESIGN.md` `D32` `D42`, `SPEC.md` `S65` `S70` `S72` `S73` | `D32` done. `D42` open by decision, with the rollup |
| ~~**`AD7`. `D48`'s second sentence is false and `D48` carries no marker.**~~ *"Nothing renders a comments card on a company, contact or dispatch."* `CommentBox` rendered on **five** screens. **Closed in `27b` — `fc4500c`**, the session its own disposition named. Session 27 had done the first half by putting `[CHANGE]` on the rule; this did the second, and `D48` is now true as written on all five screens — asserted from both sides, because `verify:routes`' walk gained a `NOT_COMMENTABLE` set beside `COMMENTABLE` and every detail screen is checked either way. `D48` also gained the sentence the removal earned: **a screen whose only timeline events were comments carries no timeline card at all**, which is the contact and the dispatch | `DESIGN.md` `D48`, the five `[id]/page.tsx` screens | Done |
| **`AD8`. `D56` describes the opposite of what ships, in both halves.** *"secondary columns are **hidden, not scrolled**. The rail becomes a bottom sheet."* `Table` is `overflow-x-auto` and **no** page hides a column at any breakpoint — grep for `hidden md:`/`md:table-cell` returns zero. The rail below `md` is a horizontally scrollable strip, and the code defends it: *"no Sheet component, no new dependency, no JavaScript."* A reasoned choice against a rule it does not cite | `DESIGN.md` `D56`, `src/components/ui/table.tsx:11`, `src/components/app-rail.tsx:127,145-147` | **CLOSED, all three clauses.** Rail in `38a`; **table and board in `38b`.** The kept set is `D56`'s own table now — lead cell, name, and the one column per list — and the six rep-facing lists hide their secondary columns rather than scrolling them. The board's *current column* was not given one: it had no referent and every column snaps to the inline start instead. **The row undercounted itself in a second way** — it said the table half was one defect and it was two, the scroller AND the kept set, and the kept set could only be settled by asking what each list's anatomy needs, which is a rule edit rather than a code fix |
| **A logical auto-margin on a `dir="ltr"` run resolves the wrong way in Arabic, and the board still has one.** `project-board.tsx:112` is `ms-auto` on the column count, which carries `dir="ltr"` because it is a bare figure `D73`. `margin-inline-start` resolves against the element's **own** direction, so it compiles to `margin-left: auto` — which in an RTL flex row sits on the badge's main-END side, pushing it back against the label with the slack on its outer edge. English works by accident. This is the defect `companies/page.tsx:220-238` already records in the same words, found a third time. **The rail's copy was fixed in `38a`** — the margin moved onto the label, which carries no `dir` and so inherits the page's — and the board's was left because the board is `38b`'s. Derived, not observed: there is no browser in this repo | `src/app/[locale]/(app)/projects/project-board.tsx:112`, `DESIGN.md` `D57` `D73` | **Closed, `38b`.** The margin moved onto the label, which carries no `dir` and inherits the page's, so it has no side to get wrong — the same fix the rail took in `38a`. Third sighting of one defect: rail, `/companies`, board. The pattern is now written three times in three files and is a candidate for one shared group header, which no slice has asked for yet |
| ~~**`AD10`. The chain says the coordinator owes the move at `paid`; `S72` gives it to the rep.**~~ **Dead before session 26 opened, and closed there.** `S133` removed the rung, the two acts behind it and the three columns they wrote, in `0029`. Checked rather than assumed: `OWNERS` in `chain.ts` has no `paid` key, and `chain.turn.coordinator.paid` is absent from both `en.json` and `ar.json` — `paid` appears nowhere in the `chain` namespace. **Both artefacts this row names were already gone**, so it cost no code change. The lesson is the row's, not the rule's: a defect recorded against two named artefacts should be re-checked against them before it is scheduled, because the slice that removes a mechanism does not always find the register row that depends on it | `src/lib/chain.ts`, `messages/*.json` | Done — nothing to change |
| ~~**`AD11`. `D15` says the brand gradient has exactly five uses; `D17` names a sixth.**~~ **Resolved: the count moves, not the rule.** `D15` says six and names `D17`'s row-action hover as the sixth. `D17` is untouched — it was right, and a rule that names a use joins the list rather than breaking it. Of the six only the primary button is built; the other five belong to screens no slice has reached, and an unbuilt use is not a breach | `DESIGN.md` `D15` `D17` | Done |
| ~~**`AD12`. `D16` says nothing else glows; `D27` gives the current chain dot a soft ring.**~~ **Resolved the same way as `AD11`.** `D16` says six and names `D27`'s chain dot as the sixth, which already ships at `chain-strip.tsx:165`. The list is now written one item at a time, so it can no longer read as four or six depending on how *"primary button and the app mark"* is split — that was `AD29`, closed here | `DESIGN.md` `D16` `D27`, `src/app/[locale]/(app)/_components/chain-strip.tsx:165` | Done |
| ~~**`AD13`. `D18` and `D19` give opposite instructions for the same breakpoint.**~~ **Resolved: `D18` is deleted and `D19` wins.** Below 980px the blur is reduced to 8px, never removed. Under `prefers-reduced-transparency` each blurred surface takes its own solid counterpart — `--surface-solid`, `--surface-2-solid`, `--rail-solid` — and `--blur` is `none`; one fallback token for three surfaces would have collapsed the inset onto the card and left table hover changing nothing, which `D21` forbids. `D18`'s one surviving idea, that the design must look correct with blur off, is `D19`'s last sentence. **The number is kept as a tombstone** so no citation shifts: `DESIGN.md` is now 62 rules across 63 numbers. Both media queries are in `globals.css`, and `D8` lists all three solids | `DESIGN.md` `D18` `D19` `D8`, `.claude/skills/facet-ui/SKILL.md`, `src/app/globals.css` | Done |
| ~~**`AD14`. `D49` enumerates eight rail items and says seven.**~~ **Resolved: the count was right and the sentence was wrong.** `D49` now reads *seven items, **plus** user management for those who hold it* — the eighth is conditional `D50` and never was one of the seven; running it into the same list is what read as eight named against seven counted. Separately, **Performance and Targets merge into one item called Targets** — one table, one row per rep, goal and attainment together, the edit control per row for `can_set_targets` — which is what frees the seventh slot for **Activity**. `D49` now records outright that the built rail carries Performance and lacks Activity and Targets, so the file no longer reads as describing what ships. **Nothing in code changed** | `DESIGN.md` `D49`, `.claude/skills/facet-ui/SKILL.md:91`, `src/components/app-rail.tsx:35-52` | **Done.** Session 23 built the rail: Activity and Targets are on it, and Performance stays one slice longer by decision — the row above carries that, and `28b` closes it |
| ~~**`AD15`. `D7` mandates contact violet; `D21` forbids violet.**~~ **Resolved: violet goes.** `D7` names four identity colours — company blue, project amber, quotation red, dispatch green — and `D21`'s ban stands unchanged, because a rule may not mandate what another forbids. A contact's lead cell is its name and position `D26`, which never needed a colour. **Nothing in code changed**: `D7` was wholly unbuilt and still is, so this settles what a later screen slice may build rather than correcting anything that ships | `DESIGN.md` `D7` `D21` | Done |
| ~~**`AD16`. `D25` names a quotation group the one definition of chain ownership cannot produce.**~~ **Settled in session 26, and the answer is not the one this row expected.** `S132` closed the half this row states: it supplies the *position* `withCustomer` and names its owner outright — *"it waits on somebody who is not a FACET user … whose move it is answers the rep"* — so a *waiting on the customer* group is producible from `chain.ts` by **position**, deriving nothing. `ChainOwner` having no customer value never decided it. **What was actually broken is different**: at thread level the positions are `requested` `quoted` `readyToShip` (hers), `withCustomer` (his) and `won` `closed` (nobody's), so once `S133` removed `paid` **`withCustomer` became the only rep-owed thread position** — and *your move* and *waiting on the customer* select the same rows. `D25` names one pile twice, and producing the third group empties the first. **Founder's call: two honest groups rather than three invented ones**, with `none` — won and closed — added at the bottom because a list must still show every row it returns, where `D29`'s graveyard argument is about a board whose columns compete for width. `chainGroup()` folds `chainOwner` and never restates it | `DESIGN.md` `D25`, `src/lib/chain.ts` | Done. **`D25`'s third name is superseded by `S133`** and `DESIGN.md` has not been asked to say so — a `§6b` question, not a code one |
| ~~**`AD17`. `D28` makes the board the projects default; `D31` builds it second.**~~ **Closed by the board slice.** `?view=` exists on `/projects` — board by default, `?view=table` beside it, as `FilterNav` links carrying the current search `D59` rather than the client switch `D20` then licensed. `D31` was already satisfied: the table shipped first. `?view=cards` is `D31`'s *only if someone asks twice* and is not built; `D30`'s three activity views are still nothing, and are `AD23`'s row | `DESIGN.md` `D28` `D31`, `src/app/[locale]/(app)/projects/page.tsx` | Done. `AD19`'s half about the view-mode switch not existing goes with it |
| ~~**`AD19`. `D20` names three JavaScript exceptions; one exists and is used for two things it does not name.**~~ **Closed by the `D20` rewrite — there is no exception list left to be wrong about.** The row was right on every count: the view-mode switch ships as `FilterNav` links `D59` and the board's scroll is `overflow-x-auto`, so **two of the three were never JavaScript**, and `Combobox` was the only real use. What the rewrite adds is that the real one is **enablement**, not an exception — see the two rows in the section above, which are its consequence. The project picker went with the `S50` slice; the **company picker on `reports/report-form.tsx:155` is still an undeclared use** and is the reason that row survives here rather than closing outright. The skill's sentence goes with the skill's rewrite | `DESIGN.md` `D20`, `reports/report-form.tsx:155`, `.claude/skills/facet-ui/SKILL.md:146-147` | **Done.** The rule closed with the rewrite; **the control closed in session 40** — `combobox.tsx` is deleted and `report-form.tsx:155` is a plain `<select>`, which is what let `§23` go green on a `required` field. The skill's sentence outlived both and is closed here |
| ~~**`AD20`. `D58` forbids inline cell editing; it ships on `/performance`.**~~ **Closed in `28b`: `D58` means it.** The founder's call, and the cheaper one — session 28 had just built the native `<details>` shape for the quotation lines, so reusing it beat arguing about how far the ban reaches. `TargetRow` is now a `Disclosure` in a **second `<TableRow>`** beneath each person's figures: no cell in the table is editable, `D49` still gets its control **per row**, and the Actions column is gone. `D49` gained the sentence. **`Disclosure` and `rejected()` moved to `_components/disclosure.tsx`** — two natives with two `::-webkit-details-marker` rules is the drift `21 §7` names, and this is the second call site rather than the third. `D58`'s other six — drag-and-drop, bulk selection, saved views, command palette, charts beyond bars, toasts — all hold | `DESIGN.md` `D58` `D49`, `src/app/[locale]/(app)/targets/target-row.tsx` | **Done** |
| **`verify:routes` §26 asserted a verbatim CLASS STRING, and `D12`'s floor turned it red on a correct screen — the fifteenth wrong-red, caught in-slice.** The check read `dispatches.body.includes('<span class="num text-faint text-xs font-semibold" dir="auto">')`. `D12` moved that figure to 12.5px, the screen stayed right, and the check went red in both locales. **Exactly `CLAUDE.md`'s fourteenth lesson met again**: *a check that parses a figure out of markup owns every shape that figure may legally take*. Fixed rather than recorded, because the fix is the check's: the span gained `data-slot="dispatch-elapsed"`, the check now finds it by handle, applies the same `holds()` the four checks around it use, prints **`saw 25 of 25 figure(s)`** in the LABEL so the green line carries its own evidence, and guards `elapsed.length > 0` so an empty read cannot pass. **Both injections run.** (1) `dir="auto"` → `dir="ltr"`: 2 red, the label reading `saw 0 of 25` and the detail naming the offending span verbatim. (2) the `data-slot` removed — the check's own precondition: 2 red reading `saw 0 of 0 … no data-slot="dispatch-elapsed" in the page at all`, which is the harder proof, that it will not report something safe when it cannot measure. **The totals moved 1826 → 1818 → 1812 and NONE of it was the injection**: diffed by label, every departure was `§8`'s closed-thread branch, whose sampled thread the run's own POSTs close — 8 and 14 checks, one section, both times. Subtraction would have blamed the injection for eight losses it did not cause | `scripts/verify-routes.ts` §26, `src/app/[locale]/(app)/dispatches/page.tsx:446` | **Done, session 49.** `CLAUDE.md`'s wrong-red count goes to **fifteen** |
| ~~**`AD22`. `D34`'s three kind marks are one short of `S86`'s four anchors.**~~ **Resolved: a fourth mark, a third action, and both chains.** `D34` carries **C, P, Q and D**, four marks for `S86`'s four anchors, as the concept already draws. Its actions are **Log** on a company or project row, **Open** on a quotation or dispatch row, and **Plan** on anything slipping; *Confirm* goes, because confirming a payment is a step inside the quotation rather than a row action. **Approve and refuse are deliberately not row actions** — they belong to `D64`'s **Requests waiting on me** block, which is where a coordinator acts. `D40` gains the second chain: both run through her, the quotation chain and, since `S72`/`S124`, the dispatch chain | `DESIGN.md` `D34` `D40` `D64`, `SPEC.md` `S86` `S88` `S89`, `docs/design/facet-concept-v5-premium.html:348` | Done as a rule. Sessions 24 and 25 build it |
| ~~**`AD23`. `D45`'s one stream is four screens.**~~ **Closed in session 27, and the count of four was two.** `/reports` and `/activity` were the two screens answering different questions and they are now one, at `/activity`, with all four of `D45`'s filters — *who, what kind, outcome, signals raised* — plus a range and a search. **`/companies/[id]/timeline` and `/projects/[id]/timeline` STAY, and that is the answer rather than unfinished work**: `D45`'s own next sentence is *"a record's timeline is the same stream, scoped to that record"*, and they already run the same `gather()` and render the same `TimelineRow`. Deleting them to satisfy a count of one would take `D70`'s *way to the rest* off two detail screens and gain nothing. **Written out so nobody reopens this as half-done.** `D30`'s views: `stream` and `by-rep` ship; `calendar` is `D31`'s second ask and is deliberately not built, which is the row below | `DESIGN.md` `D45` `D30` | Done |
| ~~**`AD24`. `D9`'s radii are not the code's.**~~ **Done, session 47 — `D9` rewritten to what ships.** Three audits and every founder review saw 10px controls and 14px cards without one report against them; a rule and a product disagreeing for thirty sessions with nobody bothered is a rule describing the drawing, not the decision. `D9` now names 14px cards, 8–10px controls, full-pill badges; the concept keeps its own values; `globals.css`'s do-not-fix note stays as history. (The audit's claimed `22 §1` citation was stale — the comment cites `D9` and this row.) Original finding:  Cards render 14px (`rounded-xl`) against *"12px cards, 16px large cards"*; badges render 32px (`rounded-4xl`) against *"20px pills"*. Controls at 10px are right. `globals.css:78-83` defends 14px by citing `22 §1`, an archived document | `DESIGN.md` `D9`, `src/app/globals.css:78-83`, `src/components/ui/badge.tsx` | Untidiness — session **47**; session 23 has shipped without it |
| ~~**`AD25`. `D12` is met on its floor and missed on its title and its base.**~~ **The code half is DONE, session 47, conforming to the CURRENT rule**: base 14px/1.5 set once on `body` (an unsized element no longer renders 16px), `DetailHeader` joins `PageHeader` at `text-2xl`, and the three stray trackings (`table.tsx`, the rail, the stream filters) joined the ten `.09em` sites. The floor's four `text-[10px]` breaches (the Q chip and the initials-avatar class in three files) went to 10.5px. What remains — the 12.5px value floor and the 4.5:1 contrast floor — is the `D12` PROPOSAL, awaiting the founder. Original finding:  Nothing is under 10.5px ✓. `PageHeader` is 24px ✓ but `DetailHeader` is `text-[25px]`, taking the concept's number over the rule's. *"base 14px / 1.5"* is set nowhere — no `font-size` on `body`, so 14px is achieved per component and an unsized element renders 16px. Section labels use `tracking-wider` (.05em) in tables and `tracking-widest` (.1em) in the rail, neither being `.09em` | `DESIGN.md` `D12`, `src/components/page-header.tsx:65`, `src/app/globals.css:200-207` | Untidiness — session **47** |
| ~~**`AD27`. Six tokens in `globals.css` have no `D` rule and three are dead.**~~ **Done, session 47 — and the audit's inventory was HALF stale.** Re-measured before deleting: `--blue-600`, `--amber-600`, `--green-600` and a bare `--glow` are not declared anywhere (nothing to drop — the claim aged out); `--glow-x-red`/`--glow-x-blue` are LIVE (the canvas gradient reads them). What WAS dead and is out: `--success`/`--success-foreground` plus their two `@theme` mappings (zero consumers), `Badge`'s four tone variants (zero call sites), and the `tw-animate-css` import AND dependency (zero `animate-*` consumers). The read-but-unnamed tokens (`--red-500`, `--red-600`, `--brand-ink`, the three rail tokens) stay: each has consumers, and naming them in `DESIGN.md` rides `D8`'s table whenever it is next opened. Original finding:  `--blue-600`, `--amber-600`, `--green-600` are **declared and never read**; `--success`/`--success-foreground` are aliased to `--color-success` with **no consumer**; `--glow` is **declared in both themes and never read**, and is misnamed against `D8`'s `--brand-glow`. `Badge`'s four tone variants `red`/`blue`/`amber`/`green` have **zero call sites**. `--red-500`, `--red-600`, `--brand-ink`, `--rail-text`, `--rail-text-strong`, `--rail-active` are read but named by no `D` rule | `src/app/globals.css:98-105,158-159,136,197`, `src/components/ui/badge.tsx:25-28` | Untidiness — a dead-structure question, not a design one. Session **47**, with the next sweep |
| ~~**`AD28`. Patterns in use that no `D` rule names.**~~ **Decided, session 47 — none earns a NEW rule.** The avatar/mark hexes became tokens (`--mark-grad`, `--avatar-user-grad`, `--avatar-person-grad`), which was the only defect here — the skill's own *never type a hex into a component*; emitted CSS read to confirm all three compile. `numeric` and `TOUCH_INPUT_CLASS` needed a CITATION, not a rule: `numeric` implements `D24`'s *end-aligned and mono* + `D11`, `TOUCH_INPUT_CLASS` implements `D74`'s 44px floor — both comments now say so. `Absent` rides the `D52` empty-fact proposal (it IS that mechanism). Recorded, not ruled: `text-destructive` on form errors (a validity signal `D6` neither permits nor forbids — colour-as-lateness is about DATA age, and widening `D6` is the founder's pen) and `ListPagination`'s figures-in-prose refusal of `num` (a figure inside a sentence is not a datum; if `D11`'s *every number* ever means prose too, that is a rule change). Original finding:  Three two-stop avatar gradients with hexes typed straight into components — `#7A1020`, `#8A3244`, `#4A1622`, `#31527F`, `#1B2F4C` — against the skill's *"never type a hex into a component"*; `text-destructive` as the colour of form errors and the required asterisk, which `D6`'s *"colour describes how long something has waited"* does not permit or name; the `Absent` component's deliberately-not-yet treatment; the `numeric` prop's mono-**and**-end-aligned pairing, which is the skill's invention rather than `D11`'s; `ListPagination`'s documented refusal to apply `num` to *"Showing 1–7 of 7"* against `D11`'s *"every number"*; and `TOUCH_INPUT_CLASS`, a phone-first affordance applied to one of `D55`'s four named screens | `src/components/app-rail.tsx:130,171`, `_components/turn.tsx:112`, `src/components/form-field.tsx:86,98`, `src/components/page-header.tsx:186`, `_components/list-controls.tsx:230-233`, `src/lib/enums.ts:441` | Session **47** decides which deserve a rule. `D59`–`D63` are the precedent |
| **`AD29`. The skill's `--brand-glow` row reads as four items under a "five uses" label.** *"**five uses**: primary button and app mark · the pace badge's ring · today's cell in the week strip · the target fill's bloom"* — five only if the first is read as two. `D16`'s original error restated in the file whose header rule exists to prevent it | `.claude/skills/facet-ui/SKILL.md:68` | **Done.** The row now reads *six uses* over six items written one at a time — primary button · app mark · the pace badge's ring · today's cell in the week strip · the target fill's bloom `D16` · the current chain dot's soft ring `D27`. Closed with `AD12`, which moved the count on `D16` itself |
| **`AD30`. The skill's pre-flight grep does not cover two of its own table's entries.** Item 7 greps `ml-\|mr-\|pl-\|pr-\|text-left\|text-right\|border-l-\|border-r-` and omits `left-*` and `right-*`, which the Use/Never table lists. Zero today either way | `.claude/skills/facet-ui/SKILL.md:33,180` | **Done.** Item 7 now greps `left-` and `right-` too, and says at the line that they were the two missing. Still zero either way — the point was a checklist that did not check what its own table forbids |
| **`AD31`. `dispatches/page.tsx:48` cites `D28` for something `D28` does not say.** *"One query parameter, no second screen"* on a **status filter**; `D28` is specifically `?view=` on Projects and Quotations and states no general principle. The only mis-citation found: all 24 other backticked `D` citations in `src/` and `scripts/` resolve and are used correctly, and every `D` and `S` citation in `SKILL.md` resolves | ~~`src/app/[locale]/(app)/dispatches/page.tsx:48`~~ | **Closed in session 41**, not by correcting the citation but by the line leaving with the screen: the five status chips became `D25`'s three piles, and the comment that mis-cited `D28` went with them. `verify:routes` §16 carried the same mis-citation on its archive assertion and dropped it in the same pass — `S122` alone backs it. **Session 47 keeps the other seven rows** |
| **`AD33`. `D26`'s dispatch clause cites a `[BUILD]` rule and covers two of four states.** It cites `S88`, which is `[BUILD]`, for behaviour that ships under `S72`. It answers for a submitted request and an approved dispatch, and has nothing for a draft (owes its rep, `S86`) or a refusal (closed). The code renders all four, so the rule is narrower than the screen | `DESIGN.md` `D26`, `src/app/[locale]/(app)/dispatches/page.tsx` | **Done in session 41 — and it was two of FIVE, not two of four.** The row was written before `S73` gave cancellation its own turn line, so the screen already rendered five states while this counted four. The clause now answers for all five and cites `S72` rather than `[BUILD]` `S88`. **Five states, three answers** — approved, refused and cancelled all owe nobody — which is what made the list three piles rather than five, and the row's own state badge is what separates the bottom one |
| **`S44-1`. `verify:phase11` §6 proves the SQL and never touches the bridge — the check standing between this project and a failure `CLAUDE.md` calls SILENT.** It hand-inserts a `sessions` row with a made-up token, calls `deactivateUser`, and asserts the row is gone. It never signs in, never issues a cookie and never makes a request. So it measures that `deactivateUser`'s `DELETE` works — which was never the risk. The risk `CLAUDE.md` names is the `jwt.encode` override in `src/auth/index.ts` silently ceasing to mint a database session, at which point the cookie carries a JWT, `getSession` consults a `sessions` table the login no longer writes to, **and §6 stays green** because it inserted its own row. That is *"a check that passes for the wrong reason"* — the ninth sighting, and the first to land on the one gate that decides whether a sacked employee is still logged in. **Session 44 ran the missing half by hand and the bridge is INTACT**: a real login wrote 1 `sessions` row, the cookie carried a 36-character uuid (not a JWT), `GET /en` answered 200, `deactivateUser` left 0 rows, and the very next request was `307 -> /en/login`. Expiry was measured the same way — a backdated row is refused and deleted. **The bridge is proved as of today and nothing proves it tomorrow** | `scripts/verify-phase11.ts` §6 (lines ~460-505), `src/auth/index.ts` `jwt.encode`, `CLAUDE.md` *Auth bridge* | **Done — `verify:routes` §30, and §6 keeps a narrower name.** The check signs in over HTTP as a throwaway subject it creates through the real `/users/new` form, reads the `sessions` row its cookie names, proves the live session renders a real page, deactivates through the real `/users/[id]` form, and asserts the row is gone and the very next request is `307 -> /{locale}/login`. **It went to `verify:routes` and not `phase11`**, which is in-process by construction and would have had every run depend on a built server. **`verify:routes` was pure black-box HTTP and no longer is** — four names (`eq`, `db`/`closeDatabase`, `sessions`/`users`), two SELECTs, every write still over HTTP; `@/lib/authz` was deliberately NOT imported. The cost was measured rather than assumed: with **both** `AUTH_SECRET` and `DATABASE_URL` absent the graph loads, throws nothing and opens no pool, and a run against a broken `DATABASE_URL` completed all 1,641 other checks before failing at §30's first SELECT — so the pool is lazy and §30 is the only consumer. **Fed both defects, and the first one found a defect in the check itself.** With `deactivateUser`'s `tx.delete(sessions)` commented out the redirect assertion **stayed green**, because `authz.ts:221` re-reads `is_active` on every request and refuses an inactive user whether or not the row died — so the redirect proves the person is locked out, never that the session was revoked. The row count is the half that pins the mechanism, and **it only pins it if read before the victim's next request**, because `authz.ts:222` deletes the row itself on seeing an inactive account: a count taken after the redirect measured that cleanup and went green on the defect too. Ordered correctly it reads `saw 1 of 1` and goes red. Second injection: the control pointed at a dead port fails **on the CONTROL**, prints *THE BRIDGE WAS NOT MEASURED*, and asserts no redirect at all — not a clean sweep of refusals. **Setup is reported apart from the claim** (`setup` / `SETUP FAILED at …`), so a `/users/new` failure or an unreachable database can never read as a broken bridge. `AccountActionForm` gained a `data-slot` because the page renders three forms and the reactivation occupies the same slot once the account is off — the first draft counted forms, saw 3 where it expected 1, and stopped. The subject is named for `cleanup:verify`'s pattern and ends deactivated `S111`: after seven runs the sweep reported `0 account(s) deactivated`, every one having ended itself. **`verify:phase11` §6 stays** — `§7` and `§8` reuse its victim and its impersonation release `[07 A6]` is asserted nowhere else — with its heading narrowed to *"on a row this script INSERTED ITSELF — not the bridge"* and a line naming §30 as the half it cannot see. `CLAUDE.md`'s auth-bridge clause now names §30, and its wrong-reason count goes eight → **nine** |
| **`S44-2`. The app answers on the LAN, so the Cloudflare Tunnel is not the only way in.** `docker-compose.yml` publishes the app as `3000:3000`, which binds `0.0.0.0`, and the `Dockerfile` sets `HOSTNAME=0.0.0.0`. **Measured on this machine rather than inferred**: `docker ps` reports `0.0.0.0:3000->3000/tcp`, and `curl http://192.168.1.86:3000/en` from this host answered `307 -> /en/login` — the app, on the Wi-Fi address, with no cookie. The `db` half is correct and was confirmed the same way: `127.0.0.1:5432->5432/tcp`, loopback only, exactly as `README.md` promises. **The login gate held on every LAN probe**, so this is exposure of the surface, not a bypass of authentication — but anything Cloudflare Access is asked to enforce in front of the tunnel is not enforced on port 3000, and a laptop on the office network reaches the login form directly. `README.md` says *"Only the app is public, through the tunnel"*, which is true of the tunnel and not of the port | `docker-compose.yml:46-47`, `Dockerfile` (`ENV HOSTNAME=0.0.0.0`), `README.md` § *Deployment notes* | **Done** — the app publishes `127.0.0.1:3000:3000`. **The one way this breaks the office PC was checked before it shipped**: `cloudflared` is not a service in `docker-compose.yml` and never has been, so it cannot be in the compose network on either machine; and a host process does reach a loopback-published container port, measured against `db`, which already carries the identical binding. `docker ps` now reports `127.0.0.1:3000->3000/tcp` and the Wi-Fi address that answered `307` refuses the connection outright. `verify:routes` is unaffected — it drives `localhost`, which is loopback. **What is still unchecked is the office tunnel's ingress hostname**: an ingress naming the machine's IP rather than `localhost` stops working under this change, so `README.md`'s pre-pilot list gains that as its own checkbox. The office firewall stays `S44-U2` |
| **`S44-3`. The session cookie is issued without `Secure`, and the variable that decides it is absent from `.env.example`.** Read off a real login response on both servers: `authjs.session-token -> Path=/ ; Expires=… ; HttpOnly ; SameSite=Lax` — `HttpOnly` and `SameSite=Lax` present, **`Secure` absent**, and the cookie carries the unprefixed name rather than `__Secure-authjs.session-token`. Auth.js chooses both from whether its resolved URL is `https`. `docker-compose.yml` sets `AUTH_URL: ${PUBLIC_URL:-http://localhost:3000}`, so an unset `PUBLIC_URL` silently yields `http://localhost:3000` — **and that is what the running container resolved**, corroborated independently by its own `authjs.callback-url` cookie reading `http%3A%2F%2Flocalhost%3A3000`. `PUBLIC_URL` appears in `docker-compose.yml` and `README.md` and **in no line of `.env.example`**, so the one variable that decides whether a session cookie is marked `Secure` is the one a fresh deployment is never prompted to set | `docker-compose.yml:45`, `.env.example` (no `PUBLIC_URL` line), `src/auth/index.ts` (`trustHost: true`), `README.md` § *Known advisory* | **Done** — `.env.example` carries `PUBLIC_URL`, commented out like `BACKUP_DIR`, saying it is production-only, that it is what makes the cookie `Secure`, and that forgetting it is silent. **The mechanism was proved rather than assumed**, which was the point of the row: the same login against the same build, with only `AUTH_URL` changed, sets `authjs.session-token; HttpOnly; SameSite=Lax` under `http` and `__Secure-authjs.session-token; HttpOnly; Secure; SameSite=Lax` under `https`. So the instruction in `README.md` is true, and the `__Secure-` prefix arrives with it — which `getSessionToken` already reads for. Whether the office `.env` actually sets it stays `S44-U4`, now a `README.md` checkbox. Touches no rule — `S101` is about revocation, not transport |
| **`S44-4`. Two anonymous assertions cover a surface of 86.** `verify:routes` drives three identities and asserts the anonymous case exactly twice: §1 (`/en/companies` lands on `/login`) and §26 (an anonymous `/api/updates` poll is 401). A throwaway probe in session 44 drove **86 requests as nobody** — 18 static routes in both locales, 13 detail and sub-routes on harvested real ids, 5 API routes, 2 source-map paths, and **31 distinct server-action forms scraped off authenticated renders and replayed with no cookie and a correct `Origin`** (a wrong `Origin` would have been a refusal for the wrong reason). Result: **81 PROTECTED, 3 answered 200, 2 UNPROVEN**. The three that answered are Auth.js's own public endpoints — `/api/auth/session` (4 bytes, `null`), `/api/auth/csrf`, `/api/auth/providers` — which login cannot work without. The 2 UNPROVEN were source-map 404s, since resolved by construction: the build emits **0** `.map` files into `.next/static`, and a real chunk in the same directory served 200 while its `.map` 404'd, so the directory is served and the maps do not exist. **The gap is the finding**: 2 assertions against 86 reachable things means *"everything is behind login"* was believed rather than proved, and it is now measured once by a script that does not exist any more | `scripts/verify-routes.ts` §1, §26, `CLAUDE.md` § *Verification* | **Done — 30 Aug 2026. The fourth identity is permanent: `verify:routes` §34, and it drives 130 surfaces where two assertions stood.** **Ratio as read, both halves stated:** **126 PROTECTED and 4 answering of 130, 0 UNPROVEN** — against session 44's throwaway 81 / 3 / 2 of 86. The UNPROVEN count is printed beside the pass in its own assertion precisely so a sweep that mostly could not tell can never read as a sweep that mostly passed; session 44's rule survives unchanged — only an unambiguous redirect to `/login` or a `401` counts, and a 404, a 5xx, a dead connection or a 200 with an empty body is named and counted apart. **The surface:** every `STATIC_ROUTES` entry in both locales, every detail, edit, timeline, handover, version and comment-edit shape on harvested real ids in both locales, five API routes, and **53 distinct server actions** scraped as `$ACTION` envelopes off authenticated renders and replayed with **no cookie and a correct `Origin`** — a CSRF refusal would be a refusal for the wrong reason and would pass while proving nothing. **The four that answer are enumerated by identity and asserted as a SET, not a superset**, because a list of exceptions that grows quietly is how this gap reopens: Auth.js's `/api/auth/session`, `/csrf` and `/providers`, which login cannot work without, **plus `/api/health`** — which session 44 did not name and which is unauthenticated **by necessity**, since `docker-compose.yml:56-58` healthchecks it every 30s and `§0` reads it before any login; it discloses up/down and never data. **Coverage is measured against the APP, not against this section's own list** `d66e1a0`: the other side is the filesystem — every `page.tsx` under `(app)` — so a route added tomorrow is covered without anyone remembering, and the check reads **36 of 36**. It earned that on its first run by failing at **33 of 36** and naming `/comments/[id]/edit`, `/reports/[id]` and `/reports/[id]/edit` as not driven. **Two things the run taught that no argument would have.** **(1) The first injection did not work, and that is a finding rather than a false start.** Ungating `/targets`' page left it **still refused**, because `(app)/layout.tsx` gates as well as the page. **Defence in depth, confirmed by experiment rather than read off the code:** a route with a layout above it **cannot be made anonymous from one file**, and removing either gate alone leaves the other standing. That is directly useful to whoever next tries to inject an auth defect — **inject at a surface with no layout above it**, which is what `/api/updates` is, and where this injection went red **and named the route** in the answered set. Injecting at a page under `(app)` will look like a check that failed to catch a defect, when what actually happened is that no defect was created. **(2) That injection exposed the FOURTH SHAPE in code written by the session that recorded the fourth shape.** The sweep's first check read *nobody is refused everywhere* and asserted `protectedCount === driven - answeredPaths.length` — and all three terms come out of **one classification loop**, where protected + answered + unproven always equals driven. It is an identity whenever nothing is UNPROVEN, so it balanced by construction and stayed **green** with `/api/updates` answering. Not the *wrong assertion* shape, which is what this row first called it: **the fourth**, `d66e1a0`. **A sixth instance of it, beside the five `S45-11` catalogues** — and deliberately **NOT** added to `CLAUDE.md`'s count, which stands at **twelve**. That number counts sightings in checks that **shipped**; this one was caught by feeding the section its own defect and never reached a commit, and folding it in would make the number mean two things at once. It now reads *every surface reached a verdict — saw 126 PROTECTED and 4 answering of 130*, with the identity claim in the check below where the exception set is pinned. **The positive control is the injection that mattered.** Pointed at a dead port it printed `SETUP FAILED at CONTROL` and `NOBODY WAS NOT MEASURED — the control did not hold, so a refusal below proves nothing about the gate`, ran **zero** of its four assertions, and moved the total **1722 → 1716**. Accounted by diffing labels rather than assuming: seven lines out — three setups and four claims — and one in, the `SETUP FAILED`, net six. The reasoning is written at the line so nobody removes it as redundant: 130 refusals against a dead server look exactly like 130 refusals against a live one. **No `src/` change was needed** — every refusal is readable from the status and `Location` alone. `CLAUDE.md` untouched at **twelve**: this adds coverage, it does not find a wrong-reason check. Commit `f20af0b` |
| **`S45-1`. `/dispatches` pile headers and the card's own total disagree once the scope exceeds one page — `verify:routes` has been red on it, and it grows.** The `D24`/`D70` assertion *the pile counts sum to the card's own total* is failing in both locales, and the gap widens every run: `26/99`, `28/105`, `32/117`, `60/179` across one session's runs. **`PAGE_SIZE` is 25 and the database holds 120 dispatches**, so the card counts the whole scope while the headers count what the page rendered — which is the exact defect that check's own comment says it exists to catch (*"what catches a header reading a PAGE's count"*), firing for the first time now that verify residue has pushed the dataset past a page. **Not caused by the `S44-1` slice, and proved so rather than argued**: the committed `verify:routes` from `HEAD`, which imports nothing and contains none of that slice's changes, fails the identical two checks against the identical server. The check is behaving correctly; the screen is not | `src/lib/dispatches.ts` (`PAGE_SIZE`, the pile query), `scripts/verify-routes.ts` §? (`the pile counts sum to the card's own total`), `DESIGN.md` `D24` `D70` | **Done — 30 Aug 2026. The premise was inverted: the screen was correct and the CHECK was the defect.** **The headers never counted the page.** `src/lib/dispatches.ts:2102-2126` computes the three pile counts as `count(*) filter (where <groupRank> = n)` beside `count()`, over the same `where`, in ONE statement with **no `limit`** — the row query's `.limit(PAGE_SIZE).offset(...)` two lines above is a separate statement — and `dispatches/page.tsx:352`'s `rows.filter` chooses only which headers RENDER, never the number, which is `counts[group]`. `CLAUDE.md`'s *resolve derived conditions in SQL before pagination* was **honoured** here, and this row's own instruction — decide whether the header counts the scope or the card counts the page — **was answered before it was asked**; the founder decision it produced (headers count the scope) was already the shipped behaviour and is **withdrawn as having no subject**. **What was actually failing:** `verify-routes.ts:5791-5795` asserted `summed === cardTotal` over ONE page's headers. A pile with no row on the page renders no header at all (`page.tsx:353`, `D70`), so page one offered `coordinator + rep` against a card stating the whole scope while `none` sat entirely on later pages. The three counts summed correctly throughout. The recorded runs prove the mechanism rather than a drift — `26/99`, `28/105`, `32/117`, `60/179`, the shortfall always the `none` pile, and **a page-local count could never have exceeded `PAGE_SIZE`**, which is the tell nobody read. **The fix is the check; `src/` is untouched** — confirmed by `git status` at the commit. §24 now **walks every page** of the scope in both locales and asserts across them: a header **outruns its page** (the positive control — a page-local count is `count === run` everywhere and can never outrun), a pile's count is **identical on every page it appears on**, the distinct pile counts **sum to the card's own total**, and the total **does not move between pages**; a fifth prints `walked N of TOTAL` so the others guard on a non-empty read. **The read is in the LABEL, not `detail`** — the first cut parked all four numbers in `detail`, which `check()` prints only on failure, so every green line said nothing re-derivable; that is `CLAUDE.md`'s *an assertion prints what it read*, and it was caught by reading the passing output rather than the code. Setup is reported apart from the claim: a scope of one page prints **NOT MEASURED**, never `ok`. Cost is stated at the line — one GET per page per locale, 11 at the closing run's 264 rows, growing with residue — because summing one page is what it replaced and only the walk proves the sum. **`D70` is no longer cited**: its cap-and-state governs a DETAIL-page block, and a list card's footer total is `D24`'s pagination footer. **Fed the defect, both injections.** (1) `groupCounts` folded off the page's own rows: **6 of 1680 failed**, three per locale — `saw nothing outran its page`, `rep 6\|25\|16 · none 9\|25\|22` (the counts visibly moving page to page), and `= 69 of 197`. Reverted, green again. (2) `PAGE_SIZE` raised to 500 so the scope fits one page: printed `one page of 203 at 500/page — scope-vs-page NOT MEASURED` in both locales and ran **zero** of the five assertions — no `ok`, no `FAIL` — the count moving 1680 → 1672. **Numbers as read, closing run: `coordinator 33 · rep 63 · none 168 = 264 of 264` over 11 pages, both locales; page one's header read `33` over a run of `25`.** An independent hand-check over HTTP, its own sign-in and parsing, at 203 rows: page 1 `total=203, headers coordinator 19 (run 19), rep 49 (run 6)` — summing 68, which is what the old assertion called a defect — and page 9 `total=203, header none 135 (run 3)`; whole scope `19 + 49 + 135 = 203`, total identical on both pages. **`DESIGN.md` unchanged, deliberately** — neither `D24` nor `D70` invited the page reading, and `DESIGN.md:471-472` already says *"its header states the true count"*; editing a rule to record a bug that was never the rule's fault would be the wrong repair. **`CLAUDE.md`'s wrong-reason count goes nine → ten** and gains a third shape, **the mirror**: nine checks passed for the wrong reason, this one FAILED for one, on a correct screen — and a wrong red burns the suite's credibility exactly as a false green does, which is how this row came to open with a decision to rewrite working code. Commit `6c8793e` |
| **`S45-2`. Three consecutive sessions reported writing guidance to a "memory" this project does not have — and the writes were real, just not in the repository.** `0b815db`, `0a8e036` and `8d396ed` each closed by reporting a write to memory: a feedback note after the hardening slice, then *"written to memory as practice"* and *"the memory adds the standing instruction not to collapse or reorder them"*. **Searched rather than assumed.** The working tree is clean: nothing untracked, and the only non-authority documents are the ones `CLAUDE.md` sanctions — `docs/archive/`'s 27, `docs/design/`, and the two project skills. Git history carries the `claude-flow` memory machinery `§7` records as installed twice and removed twice; none of it is present. **What exists is outside the repository**: `~/.claude/projects/c--Projects-facet-crm/memory/`, **55 markdown files, 167,551 bytes**, with an auto-loaded `MEMORY.md` index, accumulating since at least 24 Aug. Six were written during the three sessions above. **This is the 27-document failure at a worse address** — guidance that shapes how sessions work, outside the five files, outside `git`, and invisible to every audit including this project's own. **The shape to watch for is the report, not the store**: a session that says it *recorded* something has not necessarily put it anywhere anybody will read, and *"written to memory"* passed unchallenged three times because it sounds like filing rather than like a new document. The two items from `8d396ed` are now where they belong — the three holds in `CLAUDE.md` § *Verification*, the do-not-collapse instruction at `verify:routes` §30 — and those two files are deleted | `~/.claude/projects/c--Projects-facet-crm/memory/` (outside the repo), `CLAUDE.md` § *Verification*, `WORKFLOW §7` | **Done — 30 Aug 2026. The writer is off, the output is gone, and the setting that survives a clone is in `git`.** **What was there:** 55 markdown files, 167,551 bytes, in `~/.claude/projects/c--Projects-facet-crm/memory/`, with a `MEMORY.md` index auto-loaded into every session, accumulated across roughly twenty sessions from 24 Aug. **The read-back for founder decisions found nothing recorded nowhere else** — which is what made the deletion free rather than a loss. Every file was skimmed for that one thing. The one that advertised itself hardest, *"three founder choices behind migration 0002 that no doc records"*, dated 9 Aug, was checked item by item and all three had since landed: `can_approve_delete` and `can_resolve_duplicate` in `SPEC.md` and `WORKFLOW.md`, `form_factor` likewise, and the colour-exclusivity CHECK **superseded outright by `S54`** *(colour is typed free text)*, its column and constraint removed in feature slice 6. The remaining candidate, five rules *"awaiting the founder's numbering"*, disqualified itself in its own text — *"these are proposals, not decisions"* — and the two live unknowns cite rules that already carry them, `S133`'s `OPEN — not chosen` and `S105`'s `[BUILD]`. **What closed it, in order:** the founder switched Claude Code's auto-memory toggle **off** in `~/.claude/settings.json`; the directory was **deleted** entirely; and `.claude/settings.json` gained **`"autoMemoryEnabled": false`** beside the `.env` read-deny in `b3bdf58`, which is the belt-and-braces half — the user-level toggle is outside `git`, invisible to any audit here and does not travel with a clone, while this one ships with the repository, reaches the office PC, and can be read by anyone. **The reason for that setting lives on this row and not beside it**, because Claude Code's settings file is strict JSON: a comment was tried and rejected (`Expected property name or '}'`), and the file was confirmed to parse with both keys intact after the edit. **`~/.claude/projects/` holds exactly one directory**, `c--Projects-facet-crm`, so no other project on the machine carried a store, and the store was confirmed absent from disk after the toggle rather than assumed gone. `CLAUDE.md`'s auto-generated-documentation rule was extended in `b1b42f6` and needs nothing further: it forbids the shape wherever guidance is written and requires a session to name the file it wrote to |
| **`S45-3`. `/activity`'s day headers count the PAGE — the defect `S45-1` was written to describe, on the screen it did not name.** `_components/stream.tsx:291-304` renders one header per day from `groupByDay(events)` and prints `entries.length`, while `timeline.ts:1206` hands `StreamList` `events.slice(start, start + TIMELINE_PAGE_SIZE)` — a single page. So the number over a day is the count of that day's events **on this page**, not in the scope. No SQL aggregate for per-day counts exists anywhere; `ListCard total` beside it is correct (`timeline.ts:1207`), so the screen already disagrees with itself the way `/dispatches` was wrongly accused of doing. **The tell, and what the fix asserts against:** a day straddling a page boundary prints **two headers for one day** — *"12 March · 7"* on page 1 and *"12 March · 4"* on page 2 — and `7 + 4` is the true count no reader ever sees. That is provable rather than suspected without knowing any true total: it is a duplicate `data-slot="stream-day"` for one date across two pages, readable over HTTP. This is `CLAUDE.md`'s *derived conditions are resolved in SQL before pagination* genuinely broken, unlike `S45-1` | `src/app/[locale]/(app)/_components/stream.tsx:291-304`, `src/lib/timeline.ts:1197-1211`, `DESIGN.md` `D24` | **Done — 30 Aug 2026. The day header counts the day; the fold moved above the slice.** **The decision, and the repo had already answered it twice.** `A` — a day header states the day's whole size in the filtered scope, and the duplicate header stays: a split day appears at the foot of one page and the head of the next reading the **same true number**. That is what `/companies` §20 and `/dispatches` §24 already assert as *a header outruns its page*, so `A` stops the stream being the exception rather than inventing a third answer. `B` — page by day, so a day is never split — was rejected on measured cost, not taste: `ListPagination` hard-codes `PAGE_SIZE = 25` and computes `lastPage = ceil(total / 25)`, `total` would stop meaning *events*, and a single busy day can exceed any page size, so it cannot be made to hold in general. **Before checking the question afresh, check whether the codebase has already settled it** — that is the move worth repeating. **The fix is four lines and no SQL.** `tallyDays(events)` runs in `streamFor` **between** `filteredStream` and the slice, beside the `total: events.length` that already proved the shape; `Stream` gains `dayCounts`; `stream.tsx` prints it instead of `entries.length`. `groupByDay` **stays** — it still decides which rows sit under which header, exactly as `/dispatches`' `rows.filter` does; only the number changed. **One array, so they cannot drift**: the page is `events.slice(...)` of the very array folded, which is `S45-5`'s property. **Every filter is picked up for free** — only `from`/`to` narrow the SQL, and `who`, `kind`, `outcome`, `signal`, `q` are JS filters, so folding the post-filter array respects all seven by construction rather than by remembering. **`GROUP BY` does not serve, and was not merely unnecessary — it was unavailable:** four of the seven filters never reach Postgres, so no SQL aggregate can see the filtered scope, and giving them a WHERE would be the second definition of the stream that `timeline.ts`'s header, `filteredStream` and `countStreamSince` all refuse in terms. No SQL added, so the correlated-subquery qualifier trap had no site to occur at. **Before and after, on a straddling day:** with the fold on the slice, `2026-09-27` read **1 then 3** across pages 1/2 — two headers for one day and neither number the day's; it now reads **4 then 4**, over 1 row on page 1, summing to 4 across the span. Closing run: `2026-08-18` reads **6 then 6** across pages 2/3, over 5 rows on page 2. **New `verify:routes` §32**, on §20/§24/§31's shape: setup apart from the claim, `NOT MEASURED` when no day straddles a boundary, numbers in the LABEL. It asserts the same count on both pages, the count **strictly exceeding** the rows beneath it on the cut page — the positive control, since a page-local count is `count === rows` everywhere — and the count equalling the day's rows summed across the pages it spans. **Both injections.** (1) fold over the sliced array: **8 of 1700 red**, and §25's `/activity is the stream with no parameter [D30]` **stayed green** in the same run, as it is a presence test on `data-slot="stream-day"` that reads no number and crosses no boundary — nothing covered this before. (2) `TIMELINE_PAGE_SIZE` raised to 5000: `NOT MEASURED` both locales, **zero** `ok`/`FAIL` lines in §32, **1700 → 1696**; accounted rather than assumed — the ten §32 claims went and **six report-detail checks appeared**, because a 5000-row page one exposes more report ids to §3's harvest. **A fourth assertion was written, fired, and was removed as WRONG — `S45-1`'s shape, caught before shipping.** It cross-checked the day's count against `?from=D&to=D`'s own total, on the assumption that narrowing to one day reads the same set. It does not: `timeline.ts:370` opens `companyAddedEvents` with `if (!scope.companyId && !range) return []`, so the **unranged stream omits `company_added` entirely**. Measured: unranged **299** events over five kinds, all-time ranged **426** over six, the whole difference being **127 `company_added`** and nothing else. It went red on a correct fold, and it was attributable only because it was asserted apart from the other three rather than folded into them — which is the argument for keeping a cross-check separate. `S45-7` carries what that omission means; this slice does not re-litigate it. **Two markers, because the claim was otherwise unreadable**: the `stream-day` `<li>` rendered the day only through `format.dateTime` — a locale-formatted string that differs in `ar` — and the count as a bare text node, so `data-day` (raw ISO) and `data-count` were added. Confirmed from the built HTML in both locales that `data-slot="stream-day"` still stands. **A red §25 field-note check was data, not diagnosis**: it failed with its own instruction, `re-run npm run seed:demo`, residue having pushed the seeded note past six pages of typed entries; re-seeded and re-ran rather than investigated. `DESIGN.md` `D45` gains ONE sentence — it was four lines and said nothing about day grouping, headers, counts or pages; `D30` likewise; `D24`'s *"Group headers say the group's name and count"* states no scope, and `D59`'s clause is chip-specific and cannot bind to a header that is not a link. **Flagged for `§6b`, not consolidated here:** three screens now answer `D24:336`'s unstated question identically, which is the signal that `D24` should say it once rather than `D45`, `D24` and `D59` each carrying a piece. `CLAUDE.md`'s wrong-reason count stays at **eleven** — a screen defect. Commit `b259c8e` |
| **`S45-4`. The `/companies` group-header check cannot fail on the defect its own comment says it catches — and has never been shown to.** `verify-routes.ts:5029-5033` asserts `Number(groupCount[1]) >= quietOnPage`, under a comment (`5018-5021`) reading *"a header counting the page would read 25 and be wrong on every page but the last"*. But a header counting the page reads **exactly** `quietOnPage`, and `25 >= 25` holds — so the assertion is green on the precise defect it names, and green on the correct screen too. It distinguishes nothing. **The property that makes it worthless is not the `>=` but that nobody has watched it go red**, which is `CLAUDE.md`'s *a check nobody has seen fail has not been shown to work*; it is checkable in one step by whoever takes it — make `listCompanies`' `quiet` count page-local and watch it stay green. Same family as `S45-1` and the opposite direction: `S45-1` was an assertion too strong to pass, this is one too weak to fail. **The screen itself is correct** — `companies.ts:265` counts `count(*) filter (where <isQuiet>)` over the same `where` as the total and `companies.ts:286` derives `touched` from it, so this is a check repair with no screen change | `scripts/verify-routes.ts:5018-5033`, `src/lib/companies.ts:210-288` | **Done — 30 Aug 2026. The old assertion was demonstrated GREEN under the very injection its comment claimed to catch — that demonstration is this row's evidence, not the rewrite.** Run in one pass with the replacement beside it, so both were read off the same server on the same data: with `companies.ts`' group counts folded off the fetched page rather than the filtered aggregate, `ok en: OLD ASSERTION UNDER TEST — header 25 >= page 25` and `ok ar: …` — green in both locales — while three of the four replacements went red, `6 of 1670 CHECK(S) FAILED`. The condition run was the replaced line VERBATIM; only its label differed, so the two numbers it had parked in `check()`'s `detail` were visible on the pass. **Why it could never fail:** a header counting the PAGE reads *exactly* `quietOnPage`, and `>=` is satisfied by equality — rep-a's page one is 25 quiet rows under a header reading 37, so the true screen compared `37 >= 25` and the defective one `25 >= 25`, both green. It was green on the defect and green on the correct screen alike, distinguishing nothing, which is `CLAUDE.md`'s *an assertion that reads nothing* — the **eleventh** sighting and that named shape, not a fourth one. **The screen was correct throughout and `src/` is untouched** — `companies.ts:265` counts `count(*) filter (where <isQuiet>)` over the same `where` as the total and `:286` derives `touched: total - quiet`; confirmed by `git status` at the commit. **The replacement is `§24`'s shape, deliberately not a second one** — `PAGE_SIZE` read from `src/lib/companies.ts` by regex rather than typed in, setup reported apart from the claim, and a walk of every page asserting: a header **outruns its page** (the positive control — a page-local count is `count === run` everywhere and can never outrun, which is exactly what `>=` could not see), a group's count **identical on every page it appears on**, the counts **summing to the card's own total**, and the total **not moving between pages**; a fifth prints `walked N of TOTAL` so the rest guard on a non-empty read. Every number is in the LABEL, not `detail` — `de3ccc2`'s rule. **The sum-to-total half applies here rather than being forced:** `/companies`' two groups PARTITION the scope, since `companies.ts` returns `touched: total - quiet` off one aggregate. Pages one and two were already in hand from the order assertions above, so the walk pays for `lastPage - 2` extra GETs — 9 pages per locale at the closing run's 208 companies, growing with residue, and stated at the line. **Second injection, the half that proves more:** `PAGE_SIZE` raised to 500 so the scope fits one page printed `one page of 197 at 500/page — scope-vs-page NOT MEASURED` in both locales and ran **zero** of the five assertions — no `ok`, no `FAIL` — with the total moving **1668 → 1656**, twelve: the ten walk assertions plus the two page-boundary order checks a single page also removes. Both injections reverted, green restored. **Numbers as read, closing run, both locales: `quiet 37 · touched 171 = 208 of 208` over 9 pages; page one's header read 37 over a run of 25.** `DESIGN.md` unchanged — `D24` says a group header states its count and never invited the weak assertion, as `S45-1` already established; the fault was the check's alone. `CLAUDE.md`'s count goes ten → eleven, number only, no fourth shape. Commit `7f51569` |
| **`S45-5`. `/follow-ups`' chip badges count a different set than the list under them.** `follow-ups.ts:1197` takes `counts` from `followUpScope(session)` — the **unfiltered** scope — while `rows` and `total` are narrowed at `1206-1209` by `q`, `group` and `kind` before the page slice. So with a search active the chips describe every follow-up the viewer may see while the list beneath shows the matches, and a chip reading 9 lands on a list of 2. Not a pagination defect: the slice at `1215` is correctly after the narrowing, and `D33`'s *a tile showing 9 lands on a list of 9* is stated in the comment at `1199-1201` for the `group` axis only. **It looks right on the default tab with an empty search**, which is the shape that hides — the same reason `S45-1` sat unmeasured until residue pushed the data past a page | `src/lib/follow-ups.ts:1193-1221`, `DESIGN.md` `D33` | **Done — 30 Aug 2026. The chip counts the scope its own click would produce.** **The decision, and the href forces it rather than taste:** `FilterNav` builds every chip's href from an EMPTY `URLSearchParams`, setting the search first and its own parameter second (`list-controls.tsx:105-114`, `D59`), so clicking a chip from `?q=foo&group=X` lands on `?q=foo&group=Y` — **the search survives, the group is replaced**. So the count respects `q` and ignores `group` and `kind`. Counting the whole scope would promise rows the click does not deliver; counting inside the ACTIVE group would read **0 on every chip but the live one**, since `enums.ts:473-478` puts each kind in exactly one group. The old docblock's own reasoning — *show what the filter would reveal before it is applied* — was right for the group, which the click removes, and had simply never been applied to `q`, which the click keeps. **The fix is one array, and that is stronger than the brief's one-query framing.** `filtered` is derived FROM `searched`, so there is nothing to keep in step and nothing a later tidy-up can edit out of step; two counts built side by side over the same condition drift the moment somebody edits one. `followUpScope` is untouched and with no search the fold is skipped, so the rail badge, `D33`'s tiles and the *needs you* line — which reach it through `shellCounts()` and cannot filter — are byte-identical. No SQL was added, so the correlated-subquery trap does not arise; the fold precedes the slice, so nothing is filtered after pagination. **Before and after, on a filtered state** (rep-a, harvested term `مصنع`, scope 67): badges read `quotations 9 · quiet 40 · notMoved 14 · yourDates 4` over lists of `0 · 5 · 0 · 1` — **67 promised over a list of 6**; they now read `0/0 · 5/5 · 0/0 · 1/1`, summing `6 of 6`, with the unfiltered path still `67 of 67`. **This had never been checked.** No script had ever sent `?q=` to `/follow-ups`; the nearest assertion, `verify-routes.ts:4806-4830`, reads the DASHBOARD tile and fetches `?group=` with no search. **It was run beside the new section under the injection and stayed green** — `ok the "quiet" tile lands on a list of its own size [D33]` while four new assertions went red — so it has been green over this defect for its whole life. That demonstration was free: already in the run, no temporary code, unlike `S45-4`'s. **New `verify:routes` §31**, on `§20`/`§24`'s shape: setup apart from the claim, a term harvested from the rows themselves, and **NOT MEASURED rather than `ok`** when no term narrows. It asserts each badge against the list its href lands on, the badges summing to the searched total, the counts **not moving when the group changes**, the unfiltered path unmoved, and no logical margin on the count. Numbers in the LABEL. **Both injections.** (1) counts back to the unfiltered scope: **4 of 1685 red**, printing `quotations 9/0 · quiet 40/5 · notMoved 14/0 · yourDates 4/1` and `saw 67 of 6`. (2) `matchesSearch` made to narrow nothing: `NOT MEASURED` in both locales, **zero** `ok`/`FAIL` lines in §31, total **1685 → 1673**, twelve — six claims times two locales. Reverted; `All 1685 checks passed`. **Two markers, because the claim was otherwise unreadable.** The badge carried no `data-*` at all, so `data-chip`/`data-count` went on the chip — **`data-chip`, never `data-slot`**, because `Button` sets `data-slot="button"` and spreads props after it, so a passed `data-slot` REPLACES it. Confirmed from the built HTML rather than assumed: all five chips in both locales still carry `data-slot="button"` and `data-variant`, with the markers alongside. And `/follow-ups` was the only list screen with no empty-state marker, so a group of nothing rendered no `list-card`, no `data-total` and nothing at all — `data-slot="follow-ups-empty"` now makes a chip reading 0 readable as 0. **A fourth sighting of `CLAUDE.md`'s logical-margin trap, fixed by DELETION**: the count span carried `ms-1.5` while holding `dir="ltr"`, so it compiled to `margin-left` and in Arabic put its space on the number's outer edge — and the chip is `inline-flex` with `gap-1` from `size="xs"`, so the gap was already there and the margin was pure bug. After the rail, `/companies` and the board. §31 guards it. **For the `§47` sweep: the trap is the logical-margin FAMILY, not one utility, and searching for a class name would miss most of it.** Across the four sightings the culprit was `ms-auto` twice (the rail `app-rail.tsx:141`, the board `project-board.tsx:137`), `ms-2` once (`/companies` `page.tsx:230`), and `ms-1.5` here — **three different utilities in four sightings**, so a grep for `ms-auto`, which is the one `CLAUDE.md` names by way of example, would have found neither `/companies` nor this. What they share is not a string but a POSITION: any `ms-*` or `me-*` on an element that itself carries `dir`, because the logical margin resolves against that element's own direction rather than the page's. `/dispatches:368` and `/quotations:327` record the same `ms-2` shape in the same words. **The fix is not always to move the margin**: where a flex parent already supplies a `gap`, the margin is a second spacing mechanism stacked on a working one and deleting it is the smaller fix — one less thing that can be wrong — which is what happened here. `DESIGN.md` `D59` gains ONE sentence — neither `D24` nor `D33` said what scope a count is over, both were silent, and `D59` already owns the chip's link semantics. `D24` and `D33` unchanged; `D33` is the dashboard strip, which is unfiltered. `CLAUDE.md`'s wrong-reason count stays at **eleven** — this is a screen defect, not a check that passed for the wrong reason. Commit `ce70523` |
| **`S45-6`. `/follow-ups`' search box and its pager both drop the `group` filter.** `FilterNav`, `SearchForm` and `ListPagination` all build hrefs from an **empty** `URLSearchParams` (`list-controls.tsx:105-114`, `:36-42`, `:230-239`), so a parameter survives only if the page passes it in. `/follow-ups` passes neither: `SearchForm` gets no `hidden` (`follow-ups/page.tsx:107-111`) and `ListCard` gets no `extra` (`:138-143`). So **searching while `?group=quiet` is active silently widens the list to every group**, and **"Next" from `?group=quiet` lands on page two of the UNFILTERED scope** — `D59`'s stated failure, *the list silently returns the wrong rows*, reached through the pager and the search box rather than through a chip. Four sibling screens already pass both: `/companies:70,120`, `/dispatches:183,251`, `/projects:115,188`, `/quotations:150,188`. The chips themselves are correct — they carry `q` — and `/users:68-72` has the same omission on a screen with no second filter, so it is harmless there | `src/app/[locale]/(app)/follow-ups/page.tsx:107-111` and `:138-143`, `src/app/[locale]/(app)/_components/list-controls.tsx:105-114`, `DESIGN.md` `D59` | **Done — 30 Aug 2026. Two props, following the convention four screens already had.** **`group` was the only parameter dropped, and `kind` was checked rather than assumed.** This screen's `searchParams` is `{ q, page, group }` and it does not read `kind` at all — `page.tsx:72-76` says so outright, *"no screen offers it"* — so a hidden field for `kind` would be a parameter nothing consumes, which is the unused structure `CLAUDE.md` forbids. `page` is dropped by the chips deliberately and correctly (`list-controls.tsx:83-84`: page 3 of one filter is rarely page 3 of another). **The convention, read rather than invented:** `/companies:70,120`, `/dispatches:183,251`, `/projects:115,188` and `/quotations:150,188` all build one `extra` object of the non-`q` filters and pass it as `hidden={extra}` to `SearchForm` and `extra={extra}` to `ListCard`. `/follow-ups` now does the same, taking the **validated** `activeGroup` rather than the raw `group` so a junk value is dropped instead of carried — `/companies` passes its `activeSort` the same way. **Before and after, as URLs:** from `/en/follow-ups?group=quiet`, the pager read `/en/follow-ups?page=2` and the search box carried no hidden field; they now read `/en/follow-ups?group=quiet&page=2` and `<input type="hidden" name="group" value="quiet"/>`. **The URL was never the whole claim.** §33 asserts the href **and then follows it**: under the injection, page two of a filtered list returned **132 of 75, against 132 unfiltered** — a correct-looking href landing on the unfiltered set, which a URL-only check would have passed. Green it reads **82 of 82, against 147 unfiltered**. **Nothing checked this, on any screen.** Every `?page=` in `verify-routes.ts` is hand-built by the script — §20, §24, §31 and §32 included — so no assertion had ever read a rendered pager's href, and none had ever read a search form's hidden fields. **New `verify:routes` §33**, on §20/§24/§31/§32's shape: `FOLLOW_UP_PAGE_SIZE` read from source, setup apart from the claim, and **NOT MEASURED** unless some group's own list spans more than one page — paging is half the claim and cannot be asserted from a single page. **Both injections.** (1) both props removed: **6 of 1701 red**, printing `NO hidden group field`, `/en/follow-ups?page=2` and `132 of 75`. (2) `FOLLOW_UP_PAGE_SIZE` raised to 500 so no filtered set spans a page: `NOT MEASURED` in both locales, **zero** `ok`/`FAIL` lines in §33, **1689 → 1705**. **Accounted by diffing the labels, not by expectation** — the eight §33 claims went out and about **twenty-four** id-keyed *list and detail agree on position* checks came in, because a 500-row page exposes far more ids to §3's harvest; net **+16**, not the −8 the obvious reading gives. **`§31` passes unchanged throughout — 12 `ok`, 0 `FAIL` in every run including both injections**, which is this row's own claim from `ce70523` checked rather than repeated: `S45-5`'s count check hand-builds its URLs and never needed this fixed, so `S45-6` really was a row and not a prerequisite arriving late. **One thing injection 2 exposed, unfixed and not a row:** `verify-routes.ts:4826` asserts `rows === Math.min(25, biggest.count)` under a comment reading *"`FOLLOW_UP_PAGE_SIZE` is 25"* — a hard-coded page size where §33 now reads the constant from source. With the constant at 500 it printed `tile 39 · list 39` and went **red on a screen that was correct**, the wrong-red shape `S45-1` records. It misfires only if the constant moves, so it is noted at the line rather than opened as work. `DESIGN.md` `D59` gains ONE sentence: it owned the chip carrying the search and said nothing about the reverse, so **the obligation now runs both ways** — the search box and the pager carry the active filter as the chip carries the search. `CLAUDE.md` untouched, wrong-reason count still **eleven** — a screen defect. Commit `f3178b7` |
| **`S45-7`. The unranged `/activity` hides a whole event source — 127 of 426 events, 30% of the stream — and no rule says it should.** `timeline.ts:370` opens `companyAddedEvents` with `if (!scope.companyId && !range) return [];`, so **`company_added` reaches the screen only when a date range is set or a company timeline is being read**. Measured in process as the manager on the reseeded database: the default stream returns **299 events over five kinds**, an all-time range returns **426 over six**, and the entire difference is **127 `company_added`** and nothing else. The guard is explicit and its neighbouring comment (*"the range form is the daily view"*) says it is deliberate, so this is recorded as a question rather than asserted as a bug. **What makes it worth a row is that nothing states it.** `D45` says the stream is *"what happened"* — one stream, three kinds — and `company_added` is an **observed** event, so a reader is told the screen shows what happened while a third of it is withheld until they set a range they have no reason to set. Nothing on screen says so, `DESIGN.md` does not mention it, and **§25's *the three kinds PARTITION the stream* cannot see it**, because it measures the unranged totals against each other and they are internally consistent. `S42` makes `/activity` what a manager reads as the daily report, which is the strongest reason to decide this deliberately rather than leave it to an early return | `src/lib/timeline.ts:365-410` (the guard at `:370`), `scripts/verify-routes.ts` §25, `DESIGN.md` `D45`, `SPEC.md` `S42` | **Done — 30 Aug 2026. The guard is gone; the default stream holds all six kinds.** **The gate was checked before the guard was touched, as the row demanded.** `timeline.ts:370` carried **no comment and no docblock note**; it entered inside a bulk commit, `edc5831 feat: activity reporting (phase 9)`, with no rationale of its own; and no rule in `SPEC.md`, `DESIGN.md` or `WORKFLOW.md` mentions it. It was the **only** source in the file so guarded. Nothing structural stood behind it either: the range predicates are plain `gte`/`lte` on the same `day` the query always computes, the `users` join is a LEFT join, and `visibleCompaniesFilter` applies with or without a range — so removing it changes **what is gathered and nothing about how**. Volume alone, which the founder named and accepted: reps adding companies in bulk and never working them is the behaviour the feed exists to make visible, and a feed that hides it hides the problem. Steady-state volume 1–2 a day across the team. **Before and after:** unranged **299 events over five kinds** against an all-time range's **426 over six**; both now read the same set, and the closing run measured **1039 over six** against **1039 over six**. **What was re-checked rather than assumed.** `§25`'s partition assertion was expected to need re-reading against a wider set and **did not** — it is written over `D45`'s **three** stream kinds, not the six event kinds, and `streamKindOf` folds `company_added` into `observed`, so both sides grew by the same amount. That is not a lucky escape but the reason nothing caught this: **under the injection the partition stayed green** while the two new assertions went red, so a check that balances by construction can never see a whole source disappear. `§32`'s day counts moved as predicted (299 → 1039 on the default view, and the straddling day changed) and its assertions are scope-relative, so all 8 held — **proved by running, not argued**; `§33` likewise 8 of 8. `daily-activity.ts`, `countStreamSince` and the `/api/updates` poll all read the same `filteredStream` and now see the wider set consistently. **The assertion is a SET, not a boolean.** *unranged holds what an all-time range holds* keeps working whichever source goes missing next, where *company_added is present* would go green again the day a guard returns for a different kind. It samples **six pages of each** rather than page one — the first cut read page one alone and printed `[comment,dispatched]`, a green line stating the stream holds two kinds, which is false and which a reader would have re-derived wrongly. Caught by reading the passing output, `CLAUDE.md`'s own rule, before it shipped. `data-event-kind` was added to the timeline row for it: `D45`'s three kinds fold five of the six into `observed`, so the existing markers cannot express the claim. **Fed the defect:** the guard restored turned **4 of 1735 red** — `saw 581 over [comment,dispatched,quotation_issued,quotation_raised,report] against ranged 812 over [six]` — and **nothing else in the suite went red**, which is the measured form of *no existing check catches this*. `S45-3`'s wrong fourth assertion was the only thing that ever saw it, and it had been removed. `DESIGN.md` `D45` gains ONE sentence — it described the stream's kinds and filters but never said what the **default** view contains. `CLAUDE.md` untouched at **eleven**; a screen defect. Commit `ffa87b4` |
| ~~**`S45-8`. A company added and never worked has no rule, and the founder wants it caught.**~~ **Done — session 50: `S138` `[BUILD]` written from his answer — both halves (the rep reminder, grouped or capped; the manager's per-rep count), threshold a settings row, starting value proposed 14 and `OPEN — not chosen`. Build `§4` row 53.** The row's caution stands for the builder. Original finding: `S89`'s six conditions all measure silence **after** contact — a quotation sitting, a project not moving, a company gone quiet since its last interaction. **Nothing measures a company that was never contacted at all.** A rep can add forty companies in a day, log nothing against any of them, and no threshold fires, because every threshold is anchored to an event that never happened. `S45-7` removed the guard that was hiding the additions from `/activity`, so the behaviour is now **visible** in the feed — but visible is not the same as measured, and a manager reading a crowded stream is doing by eye what a follow-up kind would do by rule. This is the founder's stated reason for showing the events at all: *reps adding companies in bulk without following them up is the behaviour he wants to see* | `SPEC.md` `S89` (the six conditions), `src/lib/follow-ups.ts` (`gather`'s sources), `DESIGN.md` `D33` (four tiles over six kinds) | **Done — session 50, the rule is `S138`.** What was recorded, deliberately not designed, and is now the builder's checklist: the threshold, whether it counts from the company's creation or from a qualification, whether it belongs to the rep who added it or to whoever holds it now, and whether it is a seventh follow-up kind or a different instrument entirely, are all unchosen. `CLAUDE.md` forbids inventing business logic and `D33` fixes four tiles over six kinds, so a seventh kind is not a free addition. **Do not start by writing the query** |
| ~~**`D65`'s day count is not built — *approved · issued · refused*, below the two columns.**~~ The dashboard slice built `D65`'s heading and both queues and stopped there: the count is a fourth query over the coordinator's decisions today, and no other part of that slice needed it | `src/app/[locale]/(app)/_components/requests-block.tsx`, `DESIGN.md` `D65` | **Done — 31 Aug 2026, in `25c`. It was one query, and the question the row held open answered itself.** **The founder question was *what does her day mean*, and the alternative could not be built**: *since she last cleared the queue* needs a cleared-state, which is the persistence machinery `S91` deleted, and a queue that is worked down by ranking `D34` never reaches one. So it is the **Riyadh calendar day**, `riyadhDayOf`, and `S93`'s weekend does not enter — on a Friday it reads zero honestly. The acts are the **reader's own**, `coalesce(acting_as_user_id, actor_user_id)` `[07 A6]`, because the heading above it says *Requests waiting on you*; today one identity holds both flags, so the company reading would agree and the difference would never be visible, which is exactly why it was decided rather than left to coincide. **Two of the three figures exist nowhere else** — a refusal and a quotation issue have no columns at all, and `TimelineEventKind` has no refusal kind, so `tallyDays`, `dailyActivity` and `/activity` are each blind to one of them; all three come from the **audit log** in one statement, since reading `approved` off `approved_at` and the other two off the log would be two definitions of one figure. **The fourth query it was deferred for is one query, measured rather than feared**: 2,131 audit rows, seq scan, 0.242 ms. `D64`'s asterisk comes off with it — *Requests waiting on me* is now `D65` entire. `verify:routes` §38 covers it, and drives a real refusal to prove the figure moves. Commit `a9e455c` |
| ~~**`D68` describes a screen the flag model does not produce, and `D40` widens the gap without causing it.**~~ **Done — session 50: `D68` rewritten to what the flags produce (team table + company panel), `D64` narrowed to book-holders by the founder's answer, and the bottleneck card's place goes to the dashboard conversation (SPEC §16).** Original finding: *The Executive gets the team table and nothing else* — but a role in FACET is a row of flags and never a name in code `D64` `CLAUDE.md`, and `ROLE_SEED` gives Executive `sees_all_reps`, `can_set_targets`, `can_manage_users`, `can_export`, `can_set_credit_split`, `can_approve_delete` and `can_resolve_duplicate`. So that role **already** gets `D64`'s waiting list (*always*), its counts strip, the target panel at company scope `D38` wherever a company target exists, and the team table — four blocks where the rule says one — and since `25c` it gets *Waiting on the coordinator* as a fifth, correctly, because it holds the flag and neither queue flag. **`D40` did not create this and must not be special-cased to hide it**: naming the role in code to suppress a block is precisely what `CLAUDE.md` forbids, and it would be the first place in the product to do it. `D68`'s own text says it is *short because nobody holds the role yet*, which is the honest reading — it is an intention written before the flags were settled, not a gate | `DESIGN.md` `D68` `D64` `D67`, `scripts/seed/roles.ts:67-83` | Open. **The fix is to `D68`'s sentence, not to any code**: either it states which flags an Executive holds and accepts what they reveal, or the seed changes and the rule follows. Whoever takes it decides with `D67` in hand — Marketing has the same shape, one register smaller |
| ~~**`verify:routes` §15 asserted participants on whatever project sorted first, so any other verify script's residue turned it red.**~~ **Fixed in the dashboard slice.** `S50` allows a project with no live company link; `verify:slice3` and `verify:phase10a` each leave several behind, and `/projects` is newest-first — so running either before `verify:routes` put an orphan at the top and *"the participants render for the coordinator"* failed on a screen that was correct. **Measured: 11 such projects from one afternoon's runs.** Found while verifying the dashboard slice, which touches no writer of `projects`. The check now picks its subject from the **manager's** view — choosing it from the coordinator's would presuppose what it asserts — and compares the two COUNTS, which is the biconditional `S76` actually makes: she reads every project *in full*. It notes and skips where none of the first eight carries a participant | `scripts/verify-routes.ts` §15 | Done — and it is the same family as §17's row above: a walk that assumes a shared database's shape rather than asserting it |

## Added by AUDIT 2 (`§6`) — the visual and data audit, 31 Aug 2026

| What | Where | Disposition |
|---|---|---|
| ~~**A2-1 · Every AR date inside a `num dir="ltr"` run is bidi-scrambled.**~~ **Done, session 46.** *"May 6, 2026"* rendered **"062026/05/"** on all 25 `/follow-ups` Since cells, every `/activity` day header and row byline, notification metas, the quotation timeline's comment dates and the next-follow-up headline — the ar `format.dateTime` output carries U+200F marks and the forced-LTR wrapper reordered the fragments. **The fix was removal at every site, found by grep rather than the nine-path list** (one of whose lines was already stale): a 12-agent classification of all 162 `dir="ltr"` occurrences found **16 wrapping locale-formatted dates** — 13 live elements across ten files (`timeline.tsx` ×2, the `RecordRow` `when` slot, `follow-ups`, `notifications`, the dashboard greeting, `stream.tsx`'s day header, `companies/[id]`'s dormancy history, `reports/[id]` ×2, `users` list + detail ×2, `next-follow-up-controls`) and 3 comment-only. The `RecordRow` slot was the one structural case: its `dir` scrambled the requests block's dates while also covering m² figures and one raw reference, so the slot went bare and the **reference alone** kept `dir="ltr"` at its caller (`reports/[id]`) — a raw `Q-9182` has no U+200F and genuinely wants isolation; that is how dates and references were told apart. No `ms-*`/`me-*` sat on any touched element (checked per `CLAUDE.md`'s trap, all 162 sites). `D73` rewritten: *a locale-formatted date keeps its own direction — never force it; a raw `yyyy-mm-dd` in a form control is a reference, not a date*. **`verify:routes` §39** now scans EVERY fetched page at `get()`'s choke point for the collision itself — U+200F inside a `dir="ltr"` run — self-tested in-run against the caught shape and two must-not-flag shapes, guarded on 7,962 RLM marks / 20,802 LTR runs read, and **fed the live defect**: `dir="ltr"` restored on the stream day header, rebuilt, driven — 57 red (all §39's, naming `/:locale/activity` and printing the RLM-bearing snippet), 1754 others unmoved; reverted. The audit's proposed `/\d{2}20\d{2}\//` regex was NOT used — the scramble exists only at render; the markup-level collision is what a script can see | `rep-a/follow-ups-many-ar-dark-1366.png`, `rep-a/activity-stream-ar-dark-1366.png` (re-shot, read by eye), `scripts/verify-routes.ts` §39, `DESIGN.md D73` | **Done — `4ed64af`** |
| ~~**A2-2 · At 375 the dashboard's Slipping rows lose the company name and the Plan control.**~~ **Done, session 48.** `RecordRow` — the shared row, so every card list takes the same shape — is a GRID below `md`: mark and name on the first line, the figure and the whole control cluster on the second, nothing clipped and nothing truncating a figure run. Read on the re-shot 375 dashboard: names full (the new long seed name truncates with an ellipsis rather than vanishing), *117 days* red at the line's start, Log + date + Plan all inside the card. One DOM, two arrangements `D56`. Original finding:  Each row renders avatar chip + *"117 days"* + Log + a date input flush to the card edge — the name is crushed to nothing and the Plan form sits at **x=416 inside a clipped row** (measured), so the act `D34` gives a slipping row cannot be taken from a phone at all. `D55` names this screen phone-first; the row's subject and exit are both gone. The Today section above keeps its names but truncates its own payload (*"Planned for Aug 31, …"*, and in AR the ellipsis bisects the date to *"26/08/31…"*) | `rep-a/dashboard-en-dark-375.png`, `rep-a/dashboard-ar-dark-375.png`, `_components/waiting-list.tsx` | **Done — session 48** |
| ~~**A2-4 · The light theme's failures are five tokens, not thirty-six screens.**~~ **Done, session 46 — one token pass, judged by rendering and looking, which no previous session could do.** The five, and what each became: (1) `--line` 7% → **13%** and `--line-strong` 13% → **24%** — the `/users` controls now read as enabled quiet controls (A/B crop read at 2×: the before's Search button is a white ghost on white, the after carries a visible fill and hairline). (2) `--surface-2` white-at-55% — invisible over white by arithmetic — → **`rgba(26,22,20,.05)`**, a dark tint that shows over any lighter ground: status pills, banding and the totals inset all render now. (3) The card edge: `--surface` .78 → **.85** plus the doubled border and a slightly deeper `--shadow` (.35→.42, .4→.48); **`--line-hi` was deliberately left white-on-white** — a top-edge sheen has no meaning on a white sheet, and the edge is carried by border and shadow; if `D14`'s light texture ever wants more, that is a design question, not a defect. (4) Amber `#B45309` → **`#8A5A00`** with bg `rgba(138,90,0,.11)` — checked numerically (5.84:1 on the card vs the red's 5.79:1, same weight, unmistakably gold; zoomed A/B: the before's *2 quotations* sits in the red's family, the after's does not) and dark's `#EBB35A` untouched. (5) The atmosphere: **strengthened** to .10/.08 (P2's question answered by looking at both options) — it reads as a wash on the re-shot dashboard, users and detail screens, still under `D13`'s 14%. **Dark proven unregressed from the emitted stylesheet** — the compiled `.dark` block holds the original values for every touched token — not from the source. `D5`'s table rewritten with the five values and a note naming this revision; the concept file keeps its own | `admin/users-list-en-light-1366.png`, `rep-a/dashboard-en-light-1366.png`, `rep-a/projects-board-many-en-light-1366.png`, `rep-a/quotation-detail-full-en-light-1366.png` (all re-shot, read by eye against kept befores), compiled `.dark` block | **Done — `42e921e`.** `D5` amended |
| ~~**A2-5 · The Quoted side figure is dispatch-blind: the rep reads 9 for a true 8, the manager 15 for a true 12.**~~ **Done, session 46.** `quotedCount` folded `chainState({versionStatus,endState})` with both dispatch flags absent, so a thread whose dispatch was submitted or approved still counted as *Quoted* — `Q-9182` read *Ready to ship* on `/quotations` and *Quoted* on the dashboard tile it links beside. **The fix is the two columns the list already selects** — `threadHasApprovedDispatch()` / `threadHasSubmittedDispatch()`, the same two `EXISTS` predicates, so the tile and the list read one ladder over one pair of flags and cannot disagree again. (The row's *"and `/targets`"* was not reproduced: nothing under `/targets` derives a thread position — `grep` finds no `chainState` caller and no `issued` fold there — so the tile was the whole defect.) **`verify:routes` §40 is new and holds BOTH halves apart** (`S45-3`): the tile paired against `/quotations`' own rendered `data-position` rows walked across every page, per identity, and — standing alone — the manager's tile against a count re-derived from `S132`'s rules in raw SQL over Postgres (no `chainState` in the path; manager because `sees_all_reps` needs no visibility fold restated, `§36`'s device). The tile itself gained `data-side="quoted"` — it had no handle, which is WHY nothing could pair it. **Fed twice, and the second run is the attribution proof.** (A) The fold re-blinded (`false` injected for both flags), rebuilt, driven: **3 of 1828 red, all §40's** — tile 16 against the list's 9 (rep), 43 against 18 (manager), 43 against 18 (records) — nothing else moved. (B) The SCRIPT's SQL alone weakened (`submitted` dropped from its `NOT EXISTS`): the two pairings stayed **green** (tile 9=9, 18=18 — the fix live) while the records check went red alone, *tile 18, records 32* — a disagreement NAMES which side moved, which is what keeping the two claims apart buys. The audit-time figures (9/8, 15/12) had drifted to 9/9 and 18/18 by the injection runs — the suite's own writes create threads — and the September re-seed brought the closing run back to the audit's own truths, tile 8 = list 8 (rep) and 12 = 12 = records 12 (manager), which is exactly why §40 asserts equalities, never numbers. **And §40 met the wrong-red shape LIVE on its first clean run**: the month rolled over mid-session, the seeded targets became last month's, `D64` removed the whole panel — correctly — and the pairing went red over `tile NaN` on a right screen (`S45-1`'s exact shape, thirteenth sighting). The section now gates on the panel's own `data-slot="today-side"`: absent panel → NOT MEASURED; panel present without the quoted handle → a real red | `src/lib/quotations.ts:633`, `scripts/verify-routes.ts` §40, `rep-a/dashboard-en-dark-1366.png` (re-shot, read by eye) | **Done — `106ff98`** |
| ~~**A2-6 · `quotation_no_response` is dispatch-blind too: the waiting list chases customers who already committed — and whether it SHOULD stop is an open founder decision, not session 46's.**~~ **CLOSED, session 50 — the founder decided, and his rule is `S137` `[CHANGE]`**: a submitted dispatch pauses the thread's chase (*"a dispatch IS the reply"*), refused or cancelled resumes it, approved finishes it `S31`. The regular-customer question dissolved — no distinction is built, and the if-ever answer (bought more than once, derived) is in `S137`'s own text. Build is `§4` row 51. Original finding: `/follow-ups` lists `Q-9182` as *"Quotation, no response · 42 days"* while its dispatch has sat with the coordinator since Aug 20. **Sharpened by session 46, which fixed `A2-5` and deliberately did not touch this.** The shared root, named exactly: **a dispatch rung is invisible to any thread-level reader that does not join for it** — `chainState` stops at the rung its caller can see, by design (`chain.ts:135-141` says so), so each reader must ask about dispatches itself. The list forgot in session 26, the tile forgot until `4ed64af` — both were **defects against `S132`**, which defines the ladder — but `quotationNoResponse` (`follow-ups.ts:274`) is different in kind: `07 D5` defines *no response* over response events, and **no rule says a submitted dispatch is a response**, so this is missing legislation, not broken arithmetic. **The question is already measured**: `S133`'s deletion recorded it as *worth 3 of the 15 threads in this pool* (`follow-ups.ts:264-272` carries the measurement), and the audit adds `Q-9182`'s 42-day chase. **The options, costed.** (a) *A submitted-or-approved dispatch stops the chase*: one `NOT EXISTS` term in `quotationNoResponse`; the row leaves `/follow-ups`, the Slipping list and the rail count together (one source, `D33` unaffected). One consequence to weigh: a REFUSED dispatch revives as the rep's draft (`S122`), the thread returns to *quoted* with no submitted dispatch, and the chase resumes with the ORIGINAL issue clock — the dispatch weeks count as silence. (b) *Status quo*: the rep is told to chase a customer `S72` already moved to the coordinator's desk. (c) *A new follow-up kind for with-the-coordinator threads*: machinery for a state her queue already shows — `D40`'s card exists — and the founder called the case rare at `S133` time | `rep-a/follow-ups-many-en-dark-1366.png`, `src/lib/follow-ups.ts:264-272` (the measurement), `chain.ts:135-141` | **Done — session 50: `S137` written from the founder's answer; what follows is the question as it stood.** His words: **a customer who buys once is done; a regular customer is still worth chasing for the next thing — and FACET cannot tell those two apart.** So the question is not only *does a dispatch stop the chase* but **does this customer buy again**, which none of the three options above answers: (a) stops every chase identically, and a repeat buyer is exactly who a rep should still be calling. Nothing in `SPEC.md` distinguishes a one-off from a regular — no rule counts a company's dispatches, no rule names repeat business, and `S89`'s silence threshold is per-company but says nothing about why. **Do not design this.** It needs the founder to say what makes a customer a regular before any rule is written; until then (a) would trade one wrong chase for another. If (a) is taken anyway: re-measure the pool first (the 3-of-15 is `S133`-era), write the rule into `SPEC.md` beside `07 D5`, and take the `S122`-revival clock question in the same breath |
| ~~**A2-8 · Every possessive reads flag-scope as personal.**~~ **Answered, session 50 — `D64` narrowed: the waiting list, counts strip and greeting render only for company-book holders; an overseer's dashboard is oversight. The build carries the copy pass (`§4` row 52).** Original finding: The manager greets *"132 things need you today"* over his three reps' customer chases; the coordinator's copy counts every rep's chase as hers; the super admin gets *"Requests waiting on you · Your day 0·0·0"* for a queue he never works. `followUpScope` is deliberately session-scoped (`§25a` used that) and `D64`'s machinery is right — **the words are what lie**. `S87` says *what is waiting on THEM*; `S88`'s grouped-by-rep manager view is `[BUILD]` and this is its absence showing | `manager/dashboard-en-dark-1366.png`, `coordinator/dashboard-en-dark-1366.png`, `admin/dashboard-en-dark-1366.png` | **Done — session 50, by `D64`'s narrowing; the greeting question is answered by the list not rendering at all for a non-holder.** The question as it stood: should a sees-all identity's waiting list render at all, and the greeting count only rows whose next act is genuinely the viewer's? |
| ~~**A2-10 · The coordinator's named act is 1.5 folds below the panel that names it.**~~ **Done, session 48, both halves.** Reject and Cancel are native `<details>` now (`RarePanel`, `§17`'s idiom) — one summary line each until opened, the reason fields still in the MARKUP so `§23` reads them and scripts-off opens them `D20` — and the turn panel's *here* is TRUE: it carries an Actions anchor (`#thread-actions`, a plain fragment link) rendered only while an act exists to point at, on a `TurnPanel` `action` slot any screen can use. Read on the coordinator's re-shot: Accept directly under the totals, the two rare outcomes folded. `verify:routes` §17 drives the cancel form through the closed `<details>` and is green. Original finding:  Her quotation detail reads *"Your turn — accept it here"* at the top; the Accept button sits ~1,050px down, after two **always-open reason textareas** (Reject, Cancel) for the rare outcomes — the page runs 2,820px against the rep's 2,265. `D70` says a card whose content is one empty field and a sentence renders as a control; two of them and an explainer spend a fold before the timeline | `coordinator/quotation-detail-full-en-dark-1366.png` | **Done — session 48** |
| ~~**A2-11 · The AR pace-bar axis runs against its own bar.**~~ **Done, session 47.** The `dir="ltr"` came off the flex CONTAINER (it laid out three children — the proposed `D73` container clause's first exhibit) and each figure isolates itself; in Arabic the legend now reads from the fill's own anchor edge, 0 at inline-start. Re-shot `rep-a/dashboard-ar-dark-1366.png` and READ: axis and bar agree. Original finding:  The fill and tick are logical (`start-0`, `insetInlineStart`) and anchor right in Arabic; the legend beneath is a flex `justify-between` carrying **`dir="ltr"` on the container** (`page.tsx:701`), so 0 stays left and the target right — the axis reads opposite to the fill, and the overage paints above the *0*. Both dashboards (rep 104%, manager 74%) show it | `rep-a/dashboard-ar-light-1366.png`, `manager/dashboard-ar-dark-1366.png`, `page.tsx:701` | Session **46**: drop the container `dir`, wrap each figure. And `§6b` for `D73` — see `A2-14`. deviates-bad |
| ~~**A2-12 · م² renders unit-first for an Arabic reader across six surfaces.**~~ **Done, session 47 — seven surfaces by the time they were counted.** Every number+unit run is a WORD run under `D73`'s own test (the unit is translated), so each takes `dir="auto"` ON THE RUN: `/quotations`' quoted cell, `/dispatches`' m² lead, the board's card figure, the quotation totals card (all four rows, `ر.س` included), `/targets`' of-which cell, the dispatch difference card's three figures, and `/projects`' day-count span (the one site rendering the fixed idiom's own key inside `dir="ltr"`). Pixel-read on re-shots: *2,605 م²* and *476 م²* digits-first in RTL. Original finding:  `/quotations`' quoted column, `/dispatches`' m² lead cell, every board card figure, the quotation totals card (currency too: *ر.س* read before its number), line price runs, `/targets`' of-which cell — all keep LTR glyph order inside RTL context, while the dashboard hero and stream render the same figures correctly, proving the right form ships elsewhere. This answers the standing `§5` row that asked what `common.sqm` does inside `dir="ltr"` runs: it reads backwards | P4 crops; `rep-a/quotations-list-many-ar-dark-1366.png`, `rep-a/dispatches-list-many-ar-dark-1366.png` | Session **47** (the `D73`/`D62` sweep this register already holds): one shared number+unit inline with `dir="auto"`/`<bdi>`, never `dir` on the cell. deviates-bad |
| ~~**A2-13 · AR KPI tiles pin the number to the opposite end from its label.**~~ **Done, session 47, except the founder's plural question.** The tiles and both side figures: `dir` moved off the block `p` onto an inline span — a block's `text-align: start` resolves against its OWN direction, which is what pinned the number a tile's width from its label; re-shot and read, number under label at one edge. `/companies`' reversed *يوماً 117*: the silence meter's two runs hold a translated word, so `dir="ltr"` → `dir="auto"` (`D73`), pixel-read *117 يوماً* digits-first. The en bare plural: `followUps.fields.days`, `movedFor` and `raisedDaysAgo` gained ICU plurals — no manager reads *1 days* again. **What stays open is only the question**: CLDR Arabic plurals everywhere (`يوم/يومان/أيام`, `/companies`' existing form) or the flat house style — FOUNDER. **Answered, session 50: CLDR everywhere, both locales — `D75` `[CHANGE]`, and English gains its plural forms in the same pass; build `§4` row 55.** Original finding:  The four-tile strip mirrors its labels right but the big number stays at the tile's LEFT edge (~200px away), on the rep and manager dashboards and the 375 2×2 grid; the manager's bottleneck card does the same (*للإصدار* right, 17 far left). Related small fry: `/companies`' day-count column renders *"يوماً 117"* (reversed) while `/follow-ups` renders the same value correctly — and `/companies` is the ONLY screen with real Arabic plurals (*117 يوماً · 110 أيام*) while everything else ships `"{count} يوم"`, and `messages/en.json:1048` ships `"{count} days"` with no plural: the manager reads **"1 days"** | `rep-a/dashboard-ar-dark-1366.png` tiles, `manager/dashboard-ar-dark-1366.png`, `rep-a/companies-list-many-ar-dark-1366.png`, `messages/en.json:1048` | Session **47**: numbers inherit `text-start`; one day-count component; the en plural form; founder question — CLDR Arabic plurals everywhere (`يوم/يومان/أيام`) or the flat house style? |
| ~~**A2-15 · "Text looks shadowed" has a named probable mechanism this harness cannot photograph.**~~ **The mechanism is REFUTED on this machine — session 46 photographed the real window and measured the pixels.** The audit's theory: text over `backdrop-filter` lands on a composited layer where Windows Chromium drops subpixel antialiasing. The probe: a page carrying the app's exact card recipe twice per theme — identical text, one card with `blur(18px) saturate(1.1)`, one without — in a **headed real Chrome 152 window** (no `--disable-lcd-text`; flags read from `chrome://version`), captured at the **OS level** via `CopyFromScreen`, because Playwright's own screenshots go through `CDPScreenshotNewSurface` and re-composite offscreen — no CDP capture can see the window's true rasterization, which is WHY the audit called it unphotographable. The metric: per-row R−B deviation from the median — validated first against a GDI+ `ClearTypeGridFit` control, which screams (sd 117, 49% positive and 46% **negative** fringes; subpixel AA swings both ways around a stem). The window: **zero negative deviations on all four cards, sd ≈ 2–4, WITH and WITHOUT the filter statistically identical** (372 = 372 warm-gradient positives at DSF 1.0) — at the machine's native 125% scaling AND re-run under `--force-device-scale-factor=1`. So on this Windows 11 + Chrome 152 machine, ALL of the app's text is grayscale-antialiased, glass or no glass — **the filter changes nothing, because there is no subpixel rendering for it to take away** (consistent with Chromium's Graphite-era retirement of LCD text). **What this means for the founder's report**: if his machine matches, the one-minute transparency toggle will change NOTHING about text sharpness — that prediction is now registered, and running the test still settles it for HIS display. If the text still reads "shadowed", the cause lives elsewhere: the 10.5–12px faint tier under 4.5:1 (`A2-3`, already owned by the `D12` rewrite), or grayscale-Chrome text reading thinner beside DirectWrite apps — neither of which deleting the glass would fix. **No design change proposed and none needed on this evidence**: `D14`'s glass is acquitted on text-rendering grounds here | `scratchpad` probes: `os-capture.png`, `os-capture-dsf1.png`, `cleartype-control.png` (session-local); `aa-probe.html` method reproducible in one file | **Closed as refuted-here — `a39422c`**; the founder's toggle remains the test for his own display, with the prediction on record |
| ~~**A2-16 · Quotation money never groups thousands, and dimension strings carry database precision.**~~ **The money half is DONE, session 48**: `formatMoney` groups the integer half and leaves the stored decimals untouched (SMAC owns money), applied at the totals card, both version-history surfaces, the `Money` line component and the dispatch difference card — read on the re-shot: *389,289.42 SAR* beside *2,849 م²*. The same pass caught three more `dir="ltr"`-over-word-runs (`Money` and the dispatch quoted-lines) and took them to `auto` (`A2-12`'s family). **The factor-zeros trimming stays the FOUNDER's question** — display only, `S5` untouched either way. Original finding:  The totals card reads *389289.42 SAR / 58393.42 SAR / 447682.84 SAR* — raw `toFixed` — beside a grouped *2,849 m²* on the same screen; version history repeats *447682.84 SAR* three times. Line dimensions render *109.0000 × 1.5000 × 5.8000 = 948.3000 m²*. The four-decimal m² is `S5`'s own requirement (document lines reconcile against SMAC) so the m² precision **stands**; the un-grouped money and the ×-run's four trailing zeros on every factor are presentation nobody asked for. The arithmetic itself was verified exact: lines multiply to the 4dp m², VAT is exactly 15%, sums match | `rep-a/quotation-detail-full-en-dark-1366.png` | **Money half done — session 48** (`D11`'s clause rides the next `DESIGN.md` pass; the code and the register carry it meanwhile). **FOUNDER QUESTION answered, session 50: trim them — `D75` `[CHANGE]`, display only, stored precision untouched (`S5`); build `§4` row 55** |
| ~~**A2-17 · `/follow-ups` duplicates COMPANY into RECORD and clips its own ACTIONS header.**~~ **Done, session 48, and the 375 half found a CLASS.** RECORD is blank on company-anchored rows (the test is the ANCHOR TYPE, not name equality — a thread whose label falls back to the company's name links somewhere the company column does not); the name cells are capped so one long name cannot widen the table past the card (session 26's `/quotations` find, met again the day the seed gained its long quiet name); ACTIONS renders whole at 1366, read at 4×. **The 19px side-scroll was not a header at all**: a live probe (`scripts/audit2/probe-overflow.mjs`) measured `scrollWidth` 460 and walked the width joints to the real mechanism — the detail grid's COLUMNS have no `min-w-0`, and a grid item's automatic minimum is its min-content, so one wide descendant pushed both columns to 434 inside a 323 grid. `min-w-0` on all six column divs (companies, projects, quotations details) took it to **scrollWidth 375, zero joints**, measured. Original finding:  21 of 25 page-one rows repeat the identical company name in both columns (a company-anchored row's record IS the company); the doubled width pushes the table past the card edge at 1366 in both themes. And at 375 the **quotation detail body side-scrolls by 19px** (measured: `scrollWidth` 394 — a card-header meta row never wraps), the one thing `D56`'s *hidden, not scrolled* exists to prevent; `/companies`, `/follow-ups`, the dashboard body all measure clean | `rep-a/follow-ups-many-en-dark-1366.png`, probe measurements | **Done — session 48** (the side-scroll's real mechanism was the grid columns' automatic minimum, measured to zero) |
| ~~**A2-19 · The waiting list's planned-group header reads "3 · شركة النهضة" on the English page.**~~ **CLOSED — measured NOT reproducing, session 47, on markup identical to the audit's.** `git diff 1dc307b..HEAD` over `waiting-list.tsx` is empty — the audit photographed exactly this markup — and today's 4× crops in BOTH themes place the NAME box first in page order: *شركة النهضة للمقاولات · 3*, name at the paragraph's start edge, count after the separator. The isolation is intact (`[dir]` spans isolate; the neutral separator takes the paragraph's LTR). What remains is not a reorder: an RTL-literate reader scanning the Arabic name right-to-left meets the count first, and no `dir` placement changes which end a bidi line is entered from — moving the count would break the same line for the LTR reader. Original finding:  The count renders before the name despite the markup placing it after (`waiting-list.tsx:246-270` — name in `dir=auto` span, separator, count in `dir=ltr` span): pixel-confirmed at 32px. The AR page renders correctly. The exact bidi mechanism (isolation defeated across the neutral separator) is session 46's to pin — and this audit's own DOM probe initially **passed the defect** by reading span union-boxes instead of pixels, which is the *wrong-in-the-direction-that-matters* shape live | `probe/hdr-zoom-en.png`, `rep-a/dashboard-en-dark-1366.png`, `waiting-list.tsx:246` | Session **46**. `D34`'s own paragraph warned about this exact line; the fix it prescribes (structure, not `dir`) still stands. deviates-bad |
| ~~**A2-20 · The rail footer truncates the user's own name from the wrong end in Arabic.**~~ **Done, session 47.** `dir="auto"` on the name AND the role label — and this is the one BLOCK that correctly carries it, written into the `D62` proposal as its exception: a truncating single-value block needs its OWN direction so the ellipsis lands on the value's end. Read on `/ar` shots: *Demo Sup…*, front kept. Original finding:  `app-rail.tsx:340` renders `{userName}` under `truncate` with no `dir="auto"`: *"Faisal Al-Harbi"* clips to *"…l Al-Harbi"* on every AR screen — `D62`'s verbatim case | `probe/probe-rail-ar.png` | Session **47** sweep. deviates-bad, trivial |
| ~~**A2-21 · Company detail renders one empty shell and one unbalanced grid.**~~ **The shell half is DONE, session 48**: the Dispatches card is absent at `total === 0` rather than a heading over a sentence — unlike Contacts, whose empty state OFFERS an act, nothing here could offer one, which is the line between `D52`'s empty state and `D70`'s shell. Verified against the LIVE page after a stale shot mislead: the sentence the HTML still contains sits in the serialized message catalogue, not the tree — grep the rendered elements, not the payload. **The balance half is DECLINED, with the reason recorded**: both columns are `1.25fr/1fr` proportional (the audit's "fixed right rail" was not the mechanism), the imbalance is the timeline's five seeded events against six narrow blocks, and a block-mover is machinery whose trigger varies with data — the timeline GROWS with real use, so this is re-judged at the pilot with real volumes, not solved with a mechanism now. Original finding:  *"Dispatches — Nothing dispatched yet."* is a heading over a sentence — the shell `D70` forbids in those words (Contacts escapes: its *Add contact* is an offer). And sparse and full company details both measure exactly **1609px** because the fixed right rail sets the height — the wide column ends at ~680px while the narrow runs on, which is `D70`'s *balanced by height* unmet | `rep-a/company-detail-sparse-en-dark-1366.png` | **Shell half done — session 48; balance half declined, re-judged at the pilot with real volumes** |
| ~~**A2-27 · The `facet-ui` skill has NOT drifted — one enumeration is stale.**~~ **Done, session 47** — the wearers line reads Card, ListCard, the board column and the stream card now, with the tiles' departure dated to `D33`'s rewrite. The six-use transmission updates when the founder answers the `D15`/`D16` proposal, not before. Original finding:  Checked claim by claim against `DESIGN.md` and the code: the palette/effects tables, `D20`'s test, the archetype and phone-row clauses, `D74`, the pre-flight list all match; `DirectionProvider`, `GROUPS`' seven, `Combobox`'s deletion, `card-face glass` pairing all verified in code. The one drift: *"worn by Card, ListCard and the dashboard tiles"* — the tiles stopped wearing it when `D33` became one quartered card; today's wearers are Card, ListCard, the board column and the stream card. The skill's own preamble names this failure mode. **Root-cause note**: the skill faithfully transmits `D15`/`D16`'s six-use lists (`A2-22`) — sessions building from the skill inherit `DESIGN.md`'s fiction, which is where the fix belongs | `.claude/skills/facet-ui/SKILL.md` §Effects | One line in the skill beside `D15`/`D16`'s correction, same commit. conforms-good otherwise |
| ~~**S46-1 · Three verify scripts fail on a database seeded on the FIRST day of a month — found by session 46, not caused by it.**~~ **Closed in session 47 — the mechanism was found, and it is TWO READER DEFECTS in `src`, one class: the app's SQL "today" was the server's UTC clock where the app's contract is Riyadh.** (1) `coverage.ts` computed `silentDays` and `isQuiet` against **`current_date`** — the server's UTC day, one behind Riyadh between midnight and 03:00 — while every stamp beside it in the same expression was already `(x at time zone 'Asia/Riyadh')::date`. So every silence figure in the product read a day low for the first three hours of EVERY day, which is phase9's 4 and phase10a's 2 exactly: 61 days read 60, a 30-threshold company at 31 read 30. (2) `dispatches.ts`'s `S123` month bounds were `${period}::date at time zone 'Asia/Riyadh'` — and a bare `date` takes `AT TIME ZONE`'s WRONG direction: lifted to midnight-UTC `timestamptz` first, the operator STRIPS the zone instead of applying it, returns a naive `03:00:00`, and the comparison re-reads that as UTC — the month started six Riyadh hours late, which is slice3's five: even the section's own just-raised request read `0 → 0`. **Measured in psql before it was believed**: `'2026-09-01'::date at time zone 'Asia/Riyadh'` → `2026-09-01 03:00:00` naive; with `::timestamp` first → `2026-08-31 21:00:00+00`. **The "day-1" framing was HALF the truth**: phase9/phase10a fire on ANY date's run between 00:00 and 03:00 Riyadh — which is how the older §11 row saw the identical four mid-month — and slice3 needs day 1 before 06:00. Fixed at both sites, the rest of `src` swept for the class (nothing else carries it; `verify-phase10a` writes `current_date` into two fixture dates and passes inside the window, so those stayed), and **proven inside the failure window**: all ten scripts and `verify:routes` (1758) green at ~01:00 Riyadh on a Sep-1 seed. Not the fixtures, not the seed, not residue — the readers. The checks were RIGHT to go red, so `S46-1` added nothing to `CLAUDE.md`'s wrong-red count (the fourteenth came later the same night, and it was session 47's own: `§40`'s tile parser met the markup it was coupled to), and the trap joined its `sql`-template family in `CLAUDE.md` § Conventions | `src/lib/coverage.ts:192,200`, `src/lib/dispatches.ts:2837-2838`, `CLAUDE.md` § Conventions | **Done — session 47.** Session 46's record stands as it was: found on the Sep-1 seed, proven pre-existing by a stash A/B (identical counts with the work stashed), the mechanism deliberately not guessed — its "smell of one class, a window-scoped read against fixtures placed relative to *now*" named the right class and the wrong suspect, since the fixtures were Riyadh-correct and the READERS were not. The register's two month-boundary sightings from one midnight (this and `§40`'s wrong red) are now one mechanism and one gate |
