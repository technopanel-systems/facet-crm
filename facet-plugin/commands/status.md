---
description: Run FACET's status block and report position — branch, markers, drift against §10
---

Run `npm run status` and report, in this order: the branch and last commits;
the two open-marker counts (the real progress bar — a rise is only wrong when
no rule was added); and any line of the output that disagrees with
`WORKFLOW.md` §10's snapshot, named as drift to re-take rather than patch.
Report only — fix nothing from here.
